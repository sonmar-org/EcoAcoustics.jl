var documenterSearchIndex = {"docs":
[{"category":"section","location":"explanations/spectrogram/#The-Spectrogram","page":"Spectrogram","text":"A spectrogram — more precisely, a Short-Time Fourier Transform (STFT) — is the result of slicing a long acoustic recording into short, overlapping segments called frames, computing the frequency content of each one, and arranging the results into a matrix. Each column of that matrix is one moment in time; each row is one frequency bin. The value in each cell is a complex number whose magnitude tells you how much energy was present at that frequency during that frame, and whose phase encodes the timing of the oscillation within the window. Plotting the squared magnitudes as a colour image produces the visual spectrograms you see in Raven, PAMGuard, and Triton — EcoAcoustics.jl computes the same underlying object.\n\n","title":"The Spectrogram"},{"category":"section","location":"explanations/spectrogram/#The-pipeline","page":"Spectrogram","text":"Every acoustic metric in EcoAcoustics.jl is built from the spectrogram upward. The structure is:\n\nspectrogram (task 8)  →  complex STFT matrix  [SpectrogramResult]\n      │\n      ▼\nPSD   (task 9)        →  power per Hz bin  [µPa²/Hz after calibration]\n      │\n      ├──→ SPL / TOL  (task 10)  →  broadband or third-octave level  [dB re 1 µPa]\n      │\n      └──→ LTSA       (task 11)  →  long-term spectral average  [dB re 1 µPa²/Hz]\n                │\n                └──→ Soundscape indices  (task 12)  →  ACI, temporal entropy,\n                                                        permutation entropy\n\nspectrogram computes the raw complex STFT and nothing else. Every transformation above it — squaring magnitudes, correcting for window energy, converting to dB, averaging over time, integrating over frequency bands — happens at the next layer. This separation means you can always inspect the intermediate STFT directly, and it means the same SpectrogramResult can feed the metric pipeline and future click/whistle detectors without modification.\n\n","title":"The pipeline"},{"category":"section","location":"explanations/spectrogram/#Why-complex-output","page":"Spectrogram","text":"The stft field of SpectrogramResult is a Matrix{ComplexF64} — each entry has a real part and an imaginary part. Most acoustic metrics only need the magnitude of each entry (written abs(X), or abs2(X) for power). The PSD layer squares the magnitudes and applies the window-energy correction. So why keep the complex form here?\n\n1. Click detection (v1.1 and later) requires phase. A click is an impulsive transient: its energy arrives across many frequencies at nearly the same moment. Phase coherence across frequency bins — the relationship between the imaginary and real parts — distinguishes a genuine impulsive event from broadband continuous noise. Magnitude alone cannot capture this. Storing the raw complex STFT preserves the option without any extra computational cost at the spectrogram stage.\n\n2. Squaring is irreversible. Once you compute abs2.(stft), the phase information is gone and cannot be recovered without rerunning the spectrogram. By keeping the complex form, any downstream algorithm can compute what it needs from a single SpectrogramResult.\n\nIf you only need power spectra, compute abs2.(result.stft) at the call site — that is exactly what the PSD module does.\n\n","title":"Why complex output"},{"category":"section","location":"explanations/spectrogram/#Window-energy-convention","page":"Spectrogram","text":"Before taking the FFT of each frame, the signal samples are multiplied element-wise by a tapering window — a smooth bell-shaped curve that equals zero at the edges and peaks in the middle. This step is necessary to prevent spectral leakage: at a frame boundary, the signal is abruptly cut off, and that sharp edge spreads energy across all frequency bins like a smear. Tapering the samples to zero before cutting eliminates the smear.\n\nThe cost of tapering is reduced energy. A Hann window, for example, downweights the samples near the edges so heavily that the windowed frame carries only about 3/8 of the raw frame's energy. If the PSD layer did not correct for this, every spectrum would read low by that factor.\n\nThe correction factor is:\n\nwindow_energy = Σ wᵢ²\n\nThis is the sum of squared window coefficients — the L² norm of the window vector squared. Merchant et al. (2015) Section 2.1 names this the normalisation factor and gives the PSD formula as:\n\nP_ss[k] = |X[k]|² / (fs × window_energy)     [µPa²/Hz]\n\nStoring window_energy on SpectrogramResult means the PSD layer has everything it needs without recomputing the window or knowing which window was used.\n\nImportant convention note. Some software pre-normalises the window so that Σwᵢ² = 1 before applying it; this absorbs the correction into the window itself. EcoAcoustics.jl does not do this. The window is applied unmodified, and window_energy is the unnormalised squared norm. This matches the Merchant 2015 and PAMGuide convention. If you compare outputs against other tools, check which convention they use.\n\nApproximate window energies for a window of length N:\n\nWindow window_energy Notes\nRectangular N No tapering; maximum leakage\nHamming ≈ 0.397 × N Moderate leakage suppression\nHann ≈ 0.375 × N Good general-purpose choice\nBlackman ≈ 0.305 × N Lowest leakage; widest main lobe\n\n","title":"Window energy convention"},{"category":"section","location":"explanations/spectrogram/#Worked-numerical-example","page":"Spectrogram","text":"Suppose you have a 1-second recording of a 1 kHz pure tone at fs = 48 000 Hz, and you call spectrogram with a 0.1-second Hann window and 50% overlap.\n\nStep 1 — derive the parameters:\n\nfs             = 48 000 Hz\nwindow_seconds = 0.1 s\nwindow_length  = round(Int, 0.1 × 48 000) = 4 800 samples\nhop            = round(Int, 0.5 × 4 800)  = 2 400 samples   (50% overlap)\nnfft           = 4 800   (default: equals window_length, no zero-padding)\n\nnum_frames  = div(48 000 − 4 800, 2 400) + 1\n            = div(43 200, 2 400) + 1\n            = 18 + 1\n            = 19 frames\n\nStep 2 — the output matrix:\n\nresult.stft  →  size (2401, 19)\n                  │     └─ 19 frames (columns)\n                  └─ nfft÷2+1 = 2401 frequency bins (rows)\n\nStep 3 — the frequency axis:\n\nFrequency resolution = fs / nfft = 48 000 / 4 800 = 10 Hz per bin.\n\nThe 1 kHz tone lands on bin 100 (0-indexed), because 100 × 10 Hz = 1 000 Hz exactly. In Julia's 1-indexed arrays, this is result.stft[101, :].\n\nStep 4 — the time axis:\n\nThe PAMGuide convention places each frame's center at sample (i−1)×hop + window_length/2:\n\ntime[1]  = (0 × 2400 + 2400) / 48 000 = 2 400 / 48 000 = 0.050 s\ntime[2]  = (1 × 2400 + 2400) / 48 000 = 4 800 / 48 000 = 0.100 s\n  ⋮\ntime[19] = (18 × 2400 + 2400) / 48 000 = 45 600 / 48 000 = 0.950 s\n\nFrames are spaced hop / fs = 2 400 / 48 000 = 0.05 s apart. The first center is at 0.05 s, not 0 s: the window covers samples 1–4 800, and the center of those samples is at sample 2 400 = 0.05 s.\n\nStep 5 — expected magnitude at 1 kHz:\n\nFor a unit-amplitude sine at a bin-aligned frequency with a Hann window of length N, the STFT magnitude at that bin is approximately N/4 = 4 800/4 = 1 200. This follows from the Hann window's DFT: the window's energy is distributed mainly at DC and at ±1 bin, so a signal that falls exactly on a bin produces a peak of approximately N/4 rather than the rectangular window's N/2.\n\nVerifying in Julia:\n\nusing EcoAcoustics\n\nfs             = 48_000.0\nwindow_seconds = 0.1\nf0             = 1_000.0    # exactly on bin 100\n\nsignal = sin.(2π * f0 .* (0 : 47_999) ./ fs)\nresult = spectrogram(signal; fs = fs, window_seconds = window_seconds)\n\n# Bin 100 (0-indexed) → Julia index 101\nprintln(abs(result.stft[101, 1]))   # ≈ 1200\nprintln(result.time[1])             # ≈ 0.05\nprintln(result.freqs[101])          # ≈ 1000.0\n\n","title":"Worked numerical example"},{"category":"section","location":"explanations/spectrogram/#References","page":"Spectrogram","text":"Merchant, N.D., Fristrup, K.M., Johnson, M.P., Tyack, P.L., Witt, M.J., Blondel, P. & Parks, S.E. (2015). Measuring acoustic habitats. Methods in Ecology and Evolution, 6, 257–265. Window energy convention (Section 2.1), PSD normalisation, and frame-center definition.\n\nSee also the Glossary for definitions of STFT, single-sided spectrum, window energy, PSD, and SPL.\n\nPlanned citations (v1.1 / v1.2). When click and whistle detectors enter the package, additional references will be added: Frasier et al. for click detection (KERNO-F methodology used for MAB validation), Roch & Silbido / Baumgartner et al. for whistle contour tracing (Silbido remora), Martin et al. (2021) / MANTA for millidecade spectra, and Wiggins & Hildebrand (2007) for the Triton / HARP architecture that informed the LTSA design.","title":"References"},{"category":"section","location":"explanations/spl/#Sound-Pressure-Level","page":"SPL","text":"Sound pressure level (SPL) is the primary soundscape metric produced by EcoAcoustics.jl. It quantifies the acoustic power in one or more frequency bands as a function of time, expressed in decibels relative to a reference pressure. Every SPL value in this package is derived from an integrated PSDResult — it is not computed directly from the waveform.\n\n","title":"Sound Pressure Level"},{"category":"section","location":"explanations/spl/#Position-in-the-pipeline","page":"SPL","text":"spectrogram → complex STFT matrix      [SpectrogramResult]\n      │\n      ▼\ncompute_psd → power per Hz bin          [PSDResult, µPa²/Hz]\n      │\n      ▼\ncompute_spl → band-integrated SPL       [SPLResult, dB re 1 µPa or 20 µPa]\n   │   │\n   │   ├── compute_tol / compute_octave / compute_millidecade\n   │   │       (populate the bands Dict automatically)\n   │   │\n   │   └── compute_spl(audio::Audiodata; ...)\n   │           (convenience wrapper; runs compute_psd internally)\n   │\n   └──→ LTSA, soundscape indices    (planned)\n\nThe PSD layer is mandatory. compute_spl does not re-run the FFT — it integrates a PSDResult that already exists. This keeps the two operations independently testable and avoids recomputing the spectrogram when multiple band configurations are needed from the same recording.\n\n","title":"Position in the pipeline"},{"category":"section","location":"explanations/spl/#Integration-formula","page":"SPL","text":"For each frequency band [f_lo, f_hi] and each time frame j, the band power is:\n\nband_power[j] = Σ_k  psd[k, j] × df\n\nwhere:\n\nThe sum runs over all PSD bins k with f_lo ≤ freqs[k] ≤ f_hi.\ndf = freqs[2] − freqs[1] is the uniform bin width in Hz.\npsd[k, j] is in µPa²/Hz (calibrated).\n\nThe result is in µPa² — acoustic power integrated over the band.\n\nThis is a Riemann sum, not a trapezoid approximation. The choice is deliberate: the PAMGuide convention treats each bin as a rectangle of width df centered at freqs[k]. Using the trapezoidal rule would give a different result at the band edges and break numerical agreement with PAMGuide and MANTA. Do not change this without testing against PAMGuide reference output.","title":"Integration formula"},{"category":"section","location":"explanations/spl/#Bin-selection:-searchsortedfirst-/-searchsortedlast","page":"SPL","text":"Bins are selected with Julia's searchsortedfirst(freqs, f_lo) and searchsortedlast(freqs, f_hi). These return indices such that f_lo ≤ freqs[i_lo] and freqs[i_hi] ≤ f_hi — inclusive at both edges. A bin at exactly f_lo or exactly f_hi is included.\n\n","title":"Bin selection: searchsortedfirst / searchsortedlast"},{"category":"section","location":"explanations/spl/#dB-conversion","page":"SPL","text":"SPL[j] = 10 × log10(band_power[j] / pref²)\n\nEnvironment pref pref² Output units\n:water 1 µPa 1 µPa² :dB_re_1µPa\n:air 20 µPa 400 µPa² :dB_re_20µPa\n\nThe reference pressure is squared because SPL is a power-domain quantity (10 × log10, not 20 × log10). A signal with band_power = 1 µPa² in water gives SPL = 10 × log10(1/1) = 0 dB re 1 µPa. In air the same power gives 10 × log10(1/400) = −26.02 dB re 20 µPa.\n\n","title":"dB conversion"},{"category":"section","location":"explanations/spl/#Aggregate-statistics-in-BandSPL","page":"SPL","text":"Each BandSPL stores the per-frame spl_dB time series plus six aggregate statistics computed from it.","title":"Aggregate statistics in BandSPL"},{"category":"section","location":"explanations/spl/#Energetic-mean-(DD-22)","page":"SPL","text":"mean_dB = 10 × log10(mean(10 .^ (spl_dB ./ 10)))\n\nThis converts to linear power, averages, then converts back. It is not the arithmetic mean of spl_dB. The distinction matters whenever the SPL time series has temporal variation — which it always does for real bioacoustic data:\n\nmean(10·log10.(P)) ≠ 10·log10(mean(P))\n\nThe energetic mean is the value L such that a constant level of L dB would produce the same total acoustic energy as the actual time-varying signal. This matches the Merchant 2015 convention and MANTA. Arithmetic averaging in dB underestimates the energetic mean when the signal contains high transients (e.g., impulsive ship noise).\n\nSee DD-16 for the same argument applied to average_psd.","title":"Energetic mean (DD-22)"},{"category":"section","location":"explanations/spl/#Percentiles-—-ISO-18405-exceedance-levels-(DD-31)","page":"SPL","text":"L_n is the exceedance level: the SPL exceeded n% of the frames, following the ISO 18405 terminology standard (also ADEON, OSPAR/JOMOPANS, EU-MSFD). Equivalently, L_n is the (100 − n)th statistical percentile:\n\nLn = quantile(spl_dB, 1 - n/100)     # L5 = quantile(0.95), L95 = quantile(0.05)\n\nSo:\n\nL5 is the loud tail (exceeded only 5% of the time);\nL95 is the quiet background (exceeded 95% of the time — the standard ambient-noise indicator);\nL1 ≥ L50 (median) ≥ L99.\n\nThis is the inverse of raw-percentile labelling. On an SPD plot, a line labelled 5% is the quiet 5th percentile — that corresponds to EA's L95. Keep table columns and SPD-plot labels on the same convention; do not mix L_n (exceedance) with % (percentile).\n\nSuggested methods wording: \"Percentile statistics are reported as exceedance levels following ISO 18405: L_n is the band SPL exceeded n% of the averaging period, so L5 is the loud tail and L95 the quiet background.\"","title":"Percentiles — ISO 18405 exceedance levels (DD-31)"},{"category":"section","location":"explanations/spl/#Single-frame-case","page":"SPL","text":"When the PSD has only one frame, all aggregate statistics equal that frame's value. quantile returns the single value for any percentile; mean of one value is that value. This is correct behavior — there is no temporal distribution to summarize. Do not add length > 1 guards around these computations.\n\n","title":"Single-frame case"},{"category":"section","location":"explanations/spl/#Default-band:-broadband-(DD-19)","page":"SPL","text":"Calling compute_spl(psd) with no bands argument uses a single broadband band:\n\nDict{Symbol, Tuple{Float64, Float64}}(:broadband => (10.0, nyquist))\n\nThe 10 Hz lower floor matches the low-frequency response limit of most calibrated hydrophones. The upper limit is the Nyquist frequency of the recording (psd.fs / 2). Bands below 10 Hz are not rejected — they trigger a @warn listing the offending band labels. The reasoning: some deployments (infrasound, low-frequency baleen whale work) legitimately need sub-10-Hz integration, and silently excluding those bands would be worse than warning.\n\n","title":"Default band: broadband (DD-19)"},{"category":"section","location":"explanations/spl/#Band-validation-(DD-19)","page":"SPL","text":"Before any integration, compute_spl validates all bands in one pass. All offending labels are collected before throwing, so the error message lists every problem at once rather than forcing the user to fix them one at a time:\n\nCondition Response\nlow_Hz ≥ high_Hz ArgumentError (all labels)\nhigh_Hz > Nyquist ArgumentError (all labels)\nlow_Hz < 10 Hz @warn (all labels)\nNo PSD bins in range ArgumentError (one label)\n\nThe \"no bins in range\" case arises when the frequency resolution df is coarser than the band width — for example, millidecade bands below ~435 Hz are narrower than a 1 Hz bin spacing. The error message includes df so the user knows the fix (use a longer window for finer frequency resolution).\n\n","title":"Band validation (DD-19)"},{"category":"section","location":"explanations/spl/#Calibration-requirement-(DD-21)","page":"SPL","text":"@assert psd_units(psd) === :µPa²_per_Hz \"...\"\n\ncompute_spl hard-asserts that the PSD is calibrated before touching any values. An uncalibrated PSD is in full-scale²/Hz — integrating it produces a number with no acoustic meaning, and silently returning it as \"SPL\" would be incorrect. The assertion fires with a message pointing to the fix: use compute_psd with a calibration, or use compute_spl(audio::Audiodata; ...) which resolves calibration automatically via the DD-14 cascade.\n\nUsing @assert rather than throw(ArgumentError(...)) is intentional: this is a programming error, not a user input error. @assert can be disabled with --check-bounds=no in performance-critical contexts (not recommended here, but available).\n\n","title":"Calibration requirement (DD-21)"},{"category":"section","location":"explanations/spl/#Archive-scale-use:-fft_plan","page":"SPL","text":"All Audiodata-accepting methods (compute_spl, compute_tol, compute_octave, compute_decidecade, compute_millidecade) accept an fft_plan keyword argument. It is forwarded without modification through the call chain:\n\ncompute_spl(audio; fft_plan) → compute_psd(audio; fft_plan) → spectrogram(sig; fft_plan)\n\nBuild the plan once with make_spectrogram_plan, then reuse it across process_chunks callbacks to avoid repeated FFTW wisdom lookups. For a 48 kHz recording with 1-second windows:\n\nplan    = make_spectrogram_plan(48000.0, 1.0)    # built once\nresults = process_chunks(src, chunk ->\n    compute_tol(chunk; window_seconds = 1.0, fft_plan = plan);\n    chunk_seconds = 3600)\n\nThe plan's FFT length must match the resolved nfft. A mismatch throws AssertionError (DD-04). Passing fft_plan = nothing (the default) causes spectrogram to build its own plan per call — correct but slower for archive-scale work.\n\nfft_plan is not available on the PSDResult dispatch of any function — the FFT has already been computed at that point, and the plan serves no purpose.\n\n","title":"Archive-scale use: fft_plan"},{"category":"section","location":"explanations/spl/#References","page":"SPL","text":"Merchant, N.D., et al. (2015). Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. DOI:10.1111/2041-210X.12330\n\nDesign decisions: DD-18 through DD-23 in docs/design_decisions.md.","title":"References"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"constant","location":"api/#EcoAcoustics.CALIBRATION_PROFILES","page":"API Reference","text":"CALIBRATION_PROFILES\n\nRegistry of scalar calibration profiles, keyed by lowercase recorder name. Each entry is a CalibrationProfile populated by the per-recorder source file under src/recorders/.\n\nRecorders without a registered profile return NoCalibration from lookup_calibration. Frequency-dependent (TF) calibration is handled separately via TFCalibration and is not stored here.\n\n\n\n\n\n","title":"EcoAcoustics.CALIBRATION_PROFILES"},{"category":"constant","location":"api/#EcoAcoustics.RECORDER_PROFILES","page":"API Reference","text":"RECORDER_PROFILES\n\nRegistry of filename-parsing profiles, keyed by lowercase recorder name (e.g. \"sm3m\", \"rockhopper\"). Each entry is a RecorderProfile populated by the per-recorder source file under src/recorders/.\n\nTo add a new recorder, create src/recorders/yourrecorder.jl, add an entry here, and include the file in src/EcoAcoustics.jl.\n\n\n\n\n\n","title":"EcoAcoustics.RECORDER_PROFILES"},{"category":"type","location":"api/#EcoAcoustics.AbstractAudioSource","page":"API Reference","text":"AbstractAudioSource\n\nAbstract base type for all audio data sources.\n\nA source is a descriptor that knows where audio data lives and how to retrieve it by time. Concrete subtypes implement two required methods and optionally override coverage_fraction.\n\nRequired methods\n\nEvery subtype must implement:\n\ntime_range(source) — the start and end time of the source.\nread_audio_range(source, start, stop) — return audio for the requested time window.\n\nProvided methods\n\nA default implementation is provided for:\n\ncoverage_fraction(source, start, stop) — returns 1.0 (assumes no gaps). Subtypes with gap information should override this.\n\nGap handling\n\nread_audio_range accepts a gap_handling keyword that controls behaviour when the requested range extends beyond or between available audio:\n\n:zero_fill (default) — pad missing regions with zeros; returned signal covers exactly [start, stop].\n:error — throw if coverage is less than 1.0.\n\nSee SingleFileSource for a concrete example.\n\nZarr readiness\n\nAll chunking and time-based access in the package flows through this interface. Adding a new storage backend (e.g. Zarr, S3) means implementing a new subtype here — nothing above this layer changes.\n\n\n\n\n\n","title":"EcoAcoustics.AbstractAudioSource"},{"category":"type","location":"api/#EcoAcoustics.AbstractRecorderProfile","page":"API Reference","text":"AbstractRecorderProfile\n\nAbstract base type for recorder-specific calibration profiles. Each supported recorder defines a concrete subtype in src/recorders/<name>.jl.\n\nUse get_profile(:recorder_id) to retrieve the singleton profile for a recorder.\n\n\n\n\n\n","title":"EcoAcoustics.AbstractRecorderProfile"},{"category":"type","location":"api/#EcoAcoustics.Audiodata","page":"API Reference","text":"Audiodata\n\nContainer for a single mono audio recording and its associated metadata.\n\nFields\n\nsig::Vector{Float64}:   Raw audio samples in linear amplitude. Always Float64, always   uncalibrated. May be empty (length 0) when used as a no-overlap sentinel   by read_audio_range.\nfs::Float32:   Sampling rate in Hz. Must be > 0.\nstarttime::DateTime:   Recording start time as reported by the recorder clock. May reflect local   time rather than UTC — check metadata.timezone.\nis_calibrated::Bool:   false at construction. Set to true only by apply_calibration!.   Metric functions (SPL, PSD, etc.) check this flag and warn when it is   false.\ncalibration::Calibration:   Calibration object associated with this recording. Stored but not applied   at I/O time. One of NoCalibration, ScalarCalibration, TFCalibration.\nmetadata::RecordingMetadata:   Provenance fields (location, recorder identity, site, timezone).\n\nNotes\n\nUse endtime(a), duration(a), and nsamples(a) helpers rather than computing from fields directly.\nCalibration is applied later via apply_calibration!, never at I/O.\n\n\n\n\n\n","title":"EcoAcoustics.Audiodata"},{"category":"type","location":"api/#EcoAcoustics.BandSPL","page":"API Reference","text":"BandSPL\n\nSPL statistics for one frequency band in an SPLResult. Produced by compute_spl; not intended for direct construction.\n\nFields\n\nband::Tuple{Float64, Float64}:   Integration band as (low_Hz, high_Hz) exactly as supplied by the caller.   Edges are in Hz. The PSD bins included are those with centre frequency   low_Hz ≤ freq ≤ high_Hz.\nspl_dB::Vector{Float64}:   Per-frame SPL time series, one value per PSD time frame. Units are   given by the enclosing SPLResult.units field (:dB_re_1µPa or   :dB_re_20µPa).\nmean_dB::Float64:   Energetic mean: 10 × log10(mean(10 .^ (spl_dB ./ 10))). Computes the   mean in linear power, then converts to dB. This is NOT the arithmetic   mean of spl_dB — see Merchant 2015 and DD-16 for why linear-domain   averaging is required for physically correct results.\nmedian_dB::Float64:   Median SPL = L50 (exceeded 50% of frames).\nL1_dB::Float64:   Level exceeded 1% of frames — the loud tail (= 99th statistical percentile).   Extreme-transient / loudest-events proxy.\nL5_dB::Float64:   Level exceeded 5% of frames (= 95th percentile). High-transient indicator.\nL10_dB::Float64:   Level exceeded 10% of frames (= 90th percentile).\nL25_dB::Float64:   Level exceeded 25% of frames (= 75th percentile).\nL75_dB::Float64:   Level exceeded 75% of frames (= 25th percentile).\nL90_dB::Float64:   Level exceeded 90% of frames (= 10th percentile).\nL95_dB::Float64:   Level exceeded 95% of frames (= 5th percentile). Low-ambient indicator.\nL99_dB::Float64:   Level exceeded 99% of frames — the quiet background (= 1st percentile).   Noise-floor proxy.\n\nPercentile convention (DD-31)\n\nL_n is the exceedance level: the SPL exceeded n% of the frames, following the ISO 18405 terminology standard (also ADEON, OSPAR/JOMOPANS). Equivalently, L_n equals the (100−n)th statistical percentile. Consequences:\n\nL5 is the loud tail; L95 is the quiet background; L1 ≥ L99 always.\nThis is the inverse of the raw-percentile labelling used on SPD plots (where a 5% line is the quiet 5th percentile). A 5% SPD line corresponds to EA's L95.\n\nSingle-frame note\n\nWhen the PSD has only one frame, all aggregate statistics (mean_dB, median_dB, L1_dB, L5_dB, L95_dB, L99_dB) equal that single frame's SPL value. There is no temporal distribution to summarize. This is correct behavior, not a degenerate case to guard against.\n\nReferences\n\nISO 18405:2017 Underwater acoustics — Terminology (exceedance level). Ainslie et al. (2021) A Terminology Standard for Underwater Acoustics. Merchant et al. (2015) Measuring Acoustic Habitats, Methods in Ecology and Evolution, 6, 257–265 (energetic mean).\n\n\n\n\n\n","title":"EcoAcoustics.BandSPL"},{"category":"type","location":"api/#EcoAcoustics.Calibration","page":"API Reference","text":"Calibration\n\nAbstract base type for all calibration representations.\n\nThree concrete subtypes form the calibration hierarchy:\n\nNoCalibration — no calibration data available.\nScalarCalibration — a single system sensitivity in dB re 1 V/µPa, valid uniformly across all frequencies.\nTFCalibration — a frequency-dependent transfer function in dB re full-scale per µPa (Raven Workbench canonical form).\n\nCalibration objects are stored in Audiodata at I/O time and applied later via apply_calibration!. The is_calibrated flag on Audiodata records whether calibration has been applied; metric functions check this flag and warn when computing on uncalibrated data.\n\n\n\n\n\n","title":"EcoAcoustics.Calibration"},{"category":"type","location":"api/#EcoAcoustics.CalibrationProfile","page":"API Reference","text":"CalibrationProfile\n\nStatic calibration parameters for a recorder family.\n\nThe total scalar system sensitivity (dB re 1 V/µPa) is computed as:     sensitivity + preampgain + boardgain + 20*log10(1/Vadc_0pk)\n\nFields\n\nlabel::String: Human-readable description.\nsensitivity::Float64: Hydrophone sensitivity [dB re 1 V/µPa].\npreamp_gain::Float64: Preamplifier gain [dB].\nboard_gain::Float64: ADC board gain [dB].\nVadc_0pk::Float64: ADC full-scale peak voltage [V].\ntf_path::Union{String,Nothing}: Path to a transfer-function file for frequency-dependent calibration (v2). nothing for scalar-only recorders.\n\n\n\n\n\n","title":"EcoAcoustics.CalibrationProfile"},{"category":"type","location":"api/#EcoAcoustics.ChunksIterator","page":"API Reference","text":"ChunksIterator\n\nLazy iterator returned by chunks. Advances through an AbstractAudioSource in fixed-duration windows. Use chunks to construct; do not build directly.\n\nBase.length is not defined — the number of emitted chunks depends on gap filtering and cannot be known without iterating. This is declared explicitly via Base.IteratorSize(::Type{ChunksIterator}) = Base.SizeUnknown() so that generic code (e.g., collect) never falls through to a wrong default.\n\n\n\n\n\n","title":"EcoAcoustics.ChunksIterator"},{"category":"type","location":"api/#EcoAcoustics.IndexedFileSource","page":"API Reference","text":"IndexedFileSource <: AbstractAudioSource\n\nAn audio source backed by a timestamp index over a directory of WAV and FLAC files.\n\nWhere SingleFileSource loads one file into memory at construction time, IndexedFileSource works from a pre-built index: a lightweight table (one row per file) that records each file's path, timestamps, and sample count. At query time, only the files that overlap the requested window are read from disk.\n\nThis is the standard source for archive-scale work. Build the index once with build_index, save it with output_path, and construct IndexedFileSource from the saved file on every subsequent run.\n\nFields\n\nindex::Arrow.Table: The loaded timestamp index. One row per audio file, sorted ascending by start_time.\nroot::String: Directory against which relative file_path values in the index are resolved when reading files.\n\nDo not use when: You are working with a single file — use SingleFileSource                  instead. Do not construct directly; use one of the two                  constructor methods below.\n\n\n\n\n\n","title":"EcoAcoustics.IndexedFileSource"},{"category":"method","location":"api/#EcoAcoustics.IndexedFileSource-Tuple{AbstractString}","page":"API Reference","text":"IndexedFileSource(arrow_path; root=dirname(abspath(arrow_path))) -> IndexedFileSource\n\nPurpose:     Construct an IndexedFileSource by loading a previously saved              Arrow index file. This is the standard constructor for production              use.\n\nArguments:\n\narrow_path::AbstractString: Path to an Arrow IPC file written by build_index. Relative paths are resolved against the current directory.\nroot::AbstractString: Directory that file paths in the index are relative to. Defaults to the directory containing the Arrow file, which is correct when the index was saved alongside the audio data.\n\nReturns:     IndexedFileSource ready to serve time-range queries.\n\nConstraints: The Arrow file must contain all eleven columns of the EcoAcoustics              index schema (validated by load_index). The index must have at              least one row. root should point to the directory that was passed              to build_index when the index was created.\n\nFails when:\n\narrow_path does not exist or is not a file.\nThe Arrow file is missing required columns.\nThe index contains zero rows.\n\nExample:\n\nsrc = IndexedFileSource(\"/data/deploy1/index.arrow\")\nt1, t2 = time_range(src)\n\n\n\n\n\n","title":"EcoAcoustics.IndexedFileSource"},{"category":"method","location":"api/#EcoAcoustics.IndexedFileSource-Tuple{Arrow.Table}","page":"API Reference","text":"IndexedFileSource(tbl::Arrow.Table; root) -> IndexedFileSource\n\nPurpose:     Construct an IndexedFileSource from an in-memory Arrow.Table,              typically the value returned directly by build_index when no              output_path was given.\n\nArguments:\n\ntbl::Arrow.Table: A table with the EcoAcoustics index schema. Schema is validated before construction.\nroot::AbstractString: Directory that file paths in the table are relative to. Required; there is no sensible default when no file path is available.\n\nReturns:     IndexedFileSource ready to serve time-range queries.\n\nConstraints: tbl must contain all eleven expected columns. The table must              have at least one row.\n\nFails when:\n\ntbl is missing required columns.\ntbl contains zero rows.\n\nExample:\n\ntbl = build_index(\"/data/deploy1\"; recorder=\"sm3m\")\nsrc = IndexedFileSource(tbl; root=\"/data/deploy1\")\n\n\n\n\n\n","title":"EcoAcoustics.IndexedFileSource"},{"category":"type","location":"api/#EcoAcoustics.LTSAResult","page":"API Reference","text":"LTSAResult\n\nPurpose:     Container for a computed Long-Term Spectral Average — a              frequency × time matrix of linear power spectral density, plus the              axes and calibration provenance needed to interpret and plot it.\n\nFields:\n\nmatrix::Matrix{Float64}: PSD values, shape (n_freqs, n_columns). Row k is frequency freqs[k]; column j is time column j. Linear units: µPa²/Hz when is_calibrated, full-scale²/Hz otherwise. Convert to dB with to_dB.\nfreqs::Vector{Float64}: Frequency axis in Hz, length n_freqs. Identical for every column (same fs, window, and nfft across columns).\ncolumn_times::Vector{Float64}: Start time of each column in seconds relative to the start of the analysed audio, length n_columns. Column j covers column_times[j] .. column_times[j] + average_span_seconds.\nfs::Float32: Sampling rate in Hz.\naverage_span_seconds::Float64: Column width in seconds (the averaging span).\nis_calibrated::Bool: true when matrix is in physical units (µPa²/Hz).\ncal::Calibration: The calibration applied when building the columns.\n\nConstraints: Every column is an energetic (linear-power) mean of the FFT frames              falling inside its span; there is no dB-mean or median form.              Columns never overlap and never cross each other's boundaries.\n\nUnits symbol: query with ltsa_units.\n\n\n\n\n\n","title":"EcoAcoustics.LTSAResult"},{"category":"type","location":"api/#EcoAcoustics.NoCalibration","page":"API Reference","text":"NoCalibration <: Calibration\n\nMarker type indicating that no calibration data is available for this recording.\n\nlookup_calibration returns NoCalibration() when no CalibrationProfile is registered for the recorder. Metric outputs computed from uncalibrated data are in units of dBFS (decibels relative to full-scale digital counts) rather than dB re 1 µPa. The is_calibrated flag on Audiodata remains false.\n\n\n\n\n\n","title":"EcoAcoustics.NoCalibration"},{"category":"type","location":"api/#EcoAcoustics.PSDResult","page":"API Reference","text":"PSDResult\n\nResult of a power spectral density computation from a SpectrogramResult. Units are linear power: full-scale²/Hz when is_calibrated = false, µPa²/Hz when is_calibrated = true.\n\nFor dB values, pass this object to to_dB (deliverable 4); for frame-averaged PSD, pass to average_psd.\n\nFields\n\npsd_linear::Matrix{Float64}:   PSD in linear power units, size (n_freqs, n_frames). Row k is frequency   bin k; column j is frame j. Units: full-scale²/Hz (is_calibrated =   false) or µPa²/Hz (is_calibrated = true). Produced by squaring the STFT   magnitudes, applying the single-sided correction (DD-10), dividing by   fs × window_energy (Merchant 2015 Eq. 1), then calling   apply_calibration! (DD-08).\nfreqs::Vector{Float64}:   Bin-centre frequencies in Hz, inherited from SpectrogramResult.   DC at index 1, Nyquist at the last index.\ntime::Vector{Float64}:   Frame-centre times in seconds from the signal start, inherited from   SpectrogramResult. PAMGuide convention (DD-03).\nfs::Float32:   Sample rate in Hz, inherited from SpectrogramResult.\nwindow_energy::Float64:   Sum of squared window coefficients (Σwᵢ²), inherited from   SpectrogramResult. Carried for downstream band integration and   chain-consistency checks — consumers can verify the PSD was computed with   the expected window without access to the original SpectrogramResult.\nnfft::Int:   FFT length used to produce this PSD, inherited from   SpectrogramResult. Used for frequency-band integration (bin width   = fs / nfft) and zero-padding bookkeeping.\ncal::Calibration:   Calibration object that was applied. One of NoCalibration,   ScalarCalibration, or TFCalibration. NoCalibration() when no   calibration was requested. Query units programmatically via psd_units.\nis_calibrated::Bool:   true when cal is not NoCalibration. Provided for fast Boolean guards   in metric functions; use psd_units(result) when the specific unit symbol   is needed.\n\nReferences\n\nMerchant et al. (2015) Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. Normalisation equation (Eq. 1) and single-sided correction convention.\n\n\n\n\n\n","title":"EcoAcoustics.PSDResult"},{"category":"type","location":"api/#EcoAcoustics.RecorderProfile","page":"API Reference","text":"RecorderProfile\n\nDescribes how a specific recorder family encodes metadata in its filenames. One profile per recorder family. All profiles are registered in RECORDER_PROFILES and looked up by parse_filename.\n\nFields\n\nname::String: Human-readable recorder name.\nfilename_style::Symbol: :tokenized — basename is split on split_char.\nsplit_char::Union{Char,Nothing}: Token delimiter.\ndate_token, time_token: 1-based indices of date and time tokens when they appear separately in the filename.\ndatetime_token: 1-based index of a combined date-time token. Use either (date_token + time_token) or datetime_token, not both.\ndatetime_format::Union{String,Nothing}: Dates.DateFormat string for parsing the combined date-time string.\nsite_token, recorder_id_token: Token indices for site and recorder ID.\ntimezone_source::Symbol: :suffix_Z if a trailing Z marks UTC; :none otherwise.\nhas_xml::Bool: Whether this recorder may supply sidecar XML metadata.\nfix_wav_header::Bool: Whether WAV files from this recorder require in-memory RIFF header correction before reading. Set true for recorders that systematically write a data-chunk size larger than the actual file content (e.g. DMON2). When true, read_audio calls _wavread_corrected silently instead of wavread. Default false for all other recorders.\nparser::Union{Nothing,Function}: Optional function that overrides all generic token parsing. Required when filename rules need custom logic (e.g. 2-digit years, XML lookups).\n\n\n\n\n\n","title":"EcoAcoustics.RecorderProfile"},{"category":"type","location":"api/#EcoAcoustics.RecordingMetadata","page":"API Reference","text":"RecordingMetadata\n\nProvenance fields for a single audio recording: where it was made, with what, and any deployment identifiers. Kept separate from the physics fields on Audiodata so the two concerns don't blur.\n\nFields\n\ntimezone::Union{String,Nothing}:   Timezone the recorder clock was set to, as an IANA name (e.g.   \"America/New_York\", \"UTC\"). nothing if unknown. When timezone   is not \"UTC\", starttime reflects local recorder time and has not   been converted to UTC — the caller is responsible for conversion.\nlat::Union{Float64,Missing} / lon::Union{Float64,Missing}:   Recorder location in decimal degrees. missing if unknown.\nsite_id::Union{String,Nothing}:   Deployment or site identifier (e.g. \"T1-C\"). nothing if unknown.\nrecorder::String:   Recorder family name (e.g. \"sm3m\", \"rockhopper\"). Defaults to   \"unknown\".\nrecorder_id::Union{String,Nothing}:   Recorder serial number or unit ID. Distinct from the hydrophone serial   number, which is tracked separately in v2. nothing if unknown.\n\n\n\n\n\n","title":"EcoAcoustics.RecordingMetadata"},{"category":"type","location":"api/#EcoAcoustics.RockhopperProfile","page":"API Reference","text":"RockhopperProfile <: AbstractRecorderProfile\n\nCalibration profile for the Cornell Rockhopper underwater recorder.\n\nThe shipped calibration (derived from the Cornell RHcalCurves AnalogSensitivity column) is loaded once at module init and bound to `RH_PROFILE. Every call toRockhopperProfile()` returns the same singleton object.\n\nFields\n\nVmax_peak_V::Float64: ADC full-scale peak voltage. 5.0 V for the Rockhopper.\ntf::TFCalibration: The shipped transfer-function calibration in canonical form.\n\n\n\n\n\n","title":"EcoAcoustics.RockhopperProfile"},{"category":"type","location":"api/#EcoAcoustics.SPLResult","page":"API Reference","text":"SPLResult\n\nResult of a band-integrated SPL computation over one or more frequency bands. Contains a per-frame SPL time series and aggregate statistics for each band.\n\nFields\n\nbands::Dict{Symbol, BandSPL}:   One BandSPL entry per frequency band. Keys are the labels from   the band specification Dict supplied to compute_spl; values   hold the per-frame time series and aggregate statistics for that band.\ntime::Vector{Float64}:   Frame-centre times in seconds from the signal start. Inherited from the   PSDResult used in the computation (PAMGuide convention, DD-03).\nfs::Float32:   Sample rate in Hz. Per the package convention (CLAUDE.md), Float32   matches hardware precision.\nunits::Symbol:   Physical units of all SPL values in bands. One of:\n:dB_re_1µPa  — underwater reference (1 µPa, set by environment=:water)\n:dB_re_20µPa — in-air reference (20 µPa, set by environment=:air)\nenvironment::Symbol:   :water or :air. Determines which reference pressure was used.\n\nConstraints\n\nunits must be :dB_re_1µPa or :dB_re_20µPa. Any other value throws ArgumentError at construction.\nEvery BandSPL in bands must have length(b.spl_dB) == length(time). If any band has a mismatched frame count, ArgumentError is thrown naming the offending band label.\n\nFails when:\n\nunits ∉ (:dB_re_1µPa, :dB_re_20µPa) → ArgumentError\nAny bands[label].spl_dB length ≠ length(time) → ArgumentError\n\nExample:\n\nresult = compute_spl(psd; bands = Dict(:full => (10.0, 24000.0)))\nresult.bands[:full].mean_dB    # energetic mean, dB re 1 µPa\nresult.bands[:full].spl_dB     # per-frame time series\nresult.time                         # frame-centre seconds from signal start\n\n\n\n\n\n","title":"EcoAcoustics.SPLResult"},{"category":"type","location":"api/#EcoAcoustics.ScalarCalibration","page":"API Reference","text":"ScalarCalibration <: Calibration\n\nA single scalar system sensitivity, valid uniformly across all frequencies.\n\nFields\n\nsystem_sensitivity_dB::Float32:   Total system sensitivity in dB re 1 V/µPa. Conventionally negative (e.g.   −153 dB for an SM3M at default gain). Computed from the recorder's   hydrophone sensitivity, preamplifier gain, board gain, and ADC full-scale   peak voltage:\n  system_sensitivity_dB = hydrophone_sensitivity + preamp_gain + board_gain\n                           + 20·log₁₀(1 / Vadc_0pk)\nSPL conversion: dBSPL = 20·log₁₀(rmslinear) − systemsensitivitydB\n\nReturned by lookup_calibration when a CalibrationProfile is registered for the recorder. Recorders with frequency-dependent response (e.g. Rockhopper) use TFCalibration instead.\n\n\n\n\n\n","title":"EcoAcoustics.ScalarCalibration"},{"category":"type","location":"api/#EcoAcoustics.SingleFileSource","page":"API Reference","text":"SingleFileSource <: AbstractAudioSource\n\nAn audio source backed by a single WAV or FLAC file.\n\nThe entire file is read and cached in memory at construction time. All subsequent read_audio_range calls slice the in-memory signal — no further disk I/O occurs. This is appropriate for single-file, one-off analyses where the file fits comfortably in RAM. For archive-scale work spanning many files, use IndexedFileSource instead.\n\nFields\n\npath::String: Path to the audio file on disk.\naudio::Audiodata: Full recording, loaded at construction.\n\nDo not use when: The file is too large to hold in RAM, or you are processing                  a directory of files — use IndexedFileSource for those cases.\n\n\n\n\n\n","title":"EcoAcoustics.SingleFileSource"},{"category":"method","location":"api/#EcoAcoustics.SingleFileSource-Tuple{AbstractString}","page":"API Reference","text":"SingleFileSource(path; kwargs...) -> SingleFileSource\n\nPurpose:     Construct a SingleFileSource by reading the audio file at              path into memory. All keyword arguments are passed through to              read_audio.\n\nArguments:\n\npath::AbstractString: Path to a WAV or FLAC file.\nkwargs...: Any keyword accepted by read_audio — recorder, starttime, lat, lon, site_id, strict.\n\nReturns:     A SingleFileSource with the full signal cached in audio.\n\nConstraints: The file must be readable and mono. The entire signal is loaded              into memory; files larger than available RAM will cause an              out-of-memory error.\n\nFails when:  The file cannot be read, is not mono, or read_audio throws.\n\nExample:\n\nsrc = SingleFileSource(\"T1-C__0__20170912_181500.wav\"; recorder=\"sm3m\")\nt1, t2 = time_range(src)\n\n\n\n\n\n","title":"EcoAcoustics.SingleFileSource"},{"category":"type","location":"api/#EcoAcoustics.SpectrogramResult","page":"API Reference","text":"SpectrogramResult\n\nResult of a Short-Time Fourier Transform (STFT) computation. Stores the complex single-sided spectrum for every analysis frame, the time and frequency axes, and the window metadata required for downstream power spectral density (PSD) normalisation.\n\nThis is the lowest-level time-frequency object in EcoAcoustics.jl. All downstream metrics — PSD, SPL, LTSA, and soundscape indices — consume a SpectrogramResult. It does not contain calibrated units; calibration is applied at the metric layer.\n\nFields\n\nstft::Matrix{ComplexF64}:   Complex single-sided STFT. Size: (nfft ÷ 2 + 1) × num_frames. Rows are   frequency bins (DC at row 1, Nyquist at the last row); columns are time   frames. No magnitude correction, single-sided amplitude scaling, or   calibration has been applied.\ntime::Vector{Float64}:   Frame center times in seconds from the start of the input signal. Center of   frame i (1-indexed) is computed as ((i-1) * hop + window_length / 2) / fs.   This matches the PAMGuide convention; the true midpoint of an even-length   window would be (window_length - 1) / 2, but PAMGuide and Merchant 2015   use window_length / 2 for consistency. Users comparing against other tools   (Triton, MANTA) should verify their frame-center conventions.\nfreqs::Vector{Float64}:   Bin-centre frequencies in Hz. Single-sided: 0 Hz (DC) through fs/2   (Nyquist). Length equals nfft ÷ 2 + 1. Bin k (0-indexed) has frequency   k * fs / nfft.\nfs::Float32:   Sample rate in Hz. Stored for reproducibility and downstream use.\nwindow_energy::Float64:   Sum of squared window coefficients: Σ wᵢ². Required for PSD normalisation   per Merchant et al. (2015). The window is applied to each frame without   pre-scaling; window_energy captures the resulting energy reduction so the   PSD layer can correct for it.\nwindow::Symbol:   Window function used: :hann, :hamming, :blackman, or :rectangular.   Stored for reproducibility.\nnfft::Int:   FFT length used. May exceed the window length when zero-padding is requested   (via the nfft kwarg to spectrogram). Stored for reproducibility.\nhop::Int:   Hop size in samples between consecutive frame starts. Equal to   round(Int, (1 - overlap_fraction) * window_length). Stored for   reproducibility and for downstream time-axis reconstruction.\n\nReferences\n\nMerchant et al. (2015) Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. Window energy convention (Section 2.1) and frame-center definition.\n\n\n\n\n\n","title":"EcoAcoustics.SpectrogramResult"},{"category":"type","location":"api/#EcoAcoustics.TFCalibration","page":"API Reference","text":"TFCalibration <: Calibration\n\nFrequency-dependent system sensitivity, stored as a transfer function (TF).\n\nUsed for recorders whose sensitivity varies across the frequency range of interest — for example, the Rockhopper hydrophone, whose TF is supplied per unit by Cornell.\n\nFields\n\nfrequency::Vector{Float64}:   Frequencies in Hz at which the sensitivity is defined. Strictly ascending,   no duplicate values, length ≥ 2.\ntf_dB::Vector{Float64}:   System sensitivity in dB re full-scale per µPa at each frequency in   frequency. This is the Raven Workbench canonical form (DD-12): values are   in dB relative to the ADC full-scale amplitude per µPa. Conventionally   negative (e.g. −229.7 dB at DC for the Rockhopper at 5 V full-scale peak).   length(tf_dB) must equal length(frequency).\nsource::String:   Path to the file from which this TF was loaded. Used for provenance and   reproducibility; allows test_psd_manta_validation.jl to verify that the   package-shipped TF matches the validation copy pointwise.\nformat::Symbol:   Identifies the file format the TF was loaded from.   :rockhopper_calcurves_csv for files loaded via load_tf_calcurves.   Other symbols defined per future recorder loaders.\nconversion_notes::String:   Human-readable description of any conversion applied when loading raw file   data into canonical form. For load_tf_calcurves, records the dB shift   used to convert from AnalogSensitivity to dB re full-scale per µPa.\n\nCalibration application\n\nTo convert full-scale PSD (fs²/Hz) to physical PSD (µPa²/Hz):\n\nPSD[µPa²/Hz] = PSD[fs²/Hz] × 10^(−tf_dB(f) / 10)\n\nThe linear-power sensitivity tf_lin(f) = 10^(tf_dB(f) / 10) is interpolated at each target frequency using linear interpolation in linear-power space (DD-11; see _interp_linear_power). Constant extrapolation applies outside frequency.\n\nSee load_tf_calcurves for the public loader that constructs this type from a Cornell-format CSV. See DD-12 in docs/design_decisions.md for the canonical-format rationale.\n\nTFCalibration objects are self-contained: no file needs to remain accessible at runtime.\n\n\n\n\n\n","title":"EcoAcoustics.TFCalibration"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{AbstractArray, AbstractArray, ScalarCalibration}","page":"API Reference","text":"apply_calibration!(out, signal, cal::ScalarCalibration)\n\nPurpose:     Apply scalar (frequency-independent) calibration to signal              in-place, multiplying every sample by the linear equivalent of              cal.system_sensitivity_dB. Dispatches to a              KernelAbstractions kernel whose backend is determined by the array              type of out at runtime.\n\nArguments:\n\nout::AbstractArray: Output buffer, same shape as signal. For CPU execution pass Array{Float64}; for GPU pass the corresponding GPU array type (e.g. CuArray{Float64} after using CUDA).\nsignal::AbstractArray: Input in normalised ADC units [−1, 1]. Must have the same element type and length as out.\ncal::ScalarCalibration: Calibration holding system_sensitivity_dB in dB re 1 V/µPa (conventionally negative). The linear multiplier is 10^(−systemsensitivitydB / 20). For SM3M at −153 dB this is ≈ 4.47 × 10⁷.\n\nReturns:     out, modified in-place.\n\nConstraints:\n\nlength(out) must equal length(signal).\nout and signal must reside on the same device (both CPU arrays or both GPU arrays of the same backend). Mixing backends causes a KA error.\nGPU backends are opt-in: using CUDA (or AMDGPU / Metal) must be called before passing GPU arrays. KernelAbstractions is the only hard dependency.\n\nFails when:\n\nlength(out) ≠ length(signal).\nout and signal are on different devices.\n\nExample:\n\n# CPU (always works)\napply_calibration!(out, signal, cal)\n\n# GPU (requires using CUDA)\nusing CUDA\nout_gpu    = CuArray{Float64}(undef, length(signal))\nsignal_gpu = CuArray(signal)\napply_calibration!(out_gpu, signal_gpu, cal)\n@assert Array(out_gpu) ≈ Array(out)   # CPU and GPU results identical\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{AbstractMatrix{Float64}, Vector{Float64}, NoCalibration}","page":"API Reference","text":"apply_calibration!(psd_linear, freqs, ::NoCalibration)\n\nPurpose:     No-op for the PSD matrix calibration path. psd_linear is              returned unchanged. Units remain full-scale²/Hz, not µPa²/Hz.              Called by compute_psd when no calibration is available.\n\nArguments:\n\npsd_linear::AbstractMatrix{Float64}: PSD in linear units. Not modified.\nfreqs::Vector{Float64}: Bin-centre frequencies. Not used.\n::NoCalibration: Calibration marker (no data).\n\nReturns:     psd_linear unchanged.\n\nConstraints: None. Both arguments are accepted at any size.\n\nFails when:  Never.\n\nExample:\n\napply_calibration!(psd_matrix, freqs, NoCalibration())   # no-op\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{AbstractMatrix{Float64}, Vector{Float64}, ScalarCalibration}","page":"API Reference","text":"apply_calibration!(psd_linear, freqs, cal::ScalarCalibration)\n\nPurpose:     Apply scalar (frequency-independent) calibration to a linear PSD              matrix in-place, converting from full-scale²/Hz to µPa²/Hz.              Every element is multiplied by 10^(−system_sensitivity_dB / 10).              Because system_sensitivity_dB is conventionally negative (e.g.              −153 dB), the multiplier is large (e.g. ≈ 2×10¹⁵ for SM3M).\n\nArguments:\n\npsd_linear::AbstractMatrix{Float64}: PSD in full-scale²/Hz. Modified in-place.\nfreqs::Vector{Float64}: Bin-centre frequencies in Hz. Not used directly; accepted for dispatch consistency with the TFCalibration method.\ncal::ScalarCalibration: Holds system_sensitivity_dB in dB re full-scale per µPa (conventionally negative).\n\nReturns:     psd_linear, modified in-place. Units after return: µPa²/Hz.\n\nConstraints: The same scale factor is applied to all frequency bins (scalar).              For frequency-dependent calibration use the TFCalibration method.\n\nFails when:  Never (no dimension constraints — scalar broadcast always succeeds).\n\nExample:\n\ncal = ScalarCalibration(-153.0f0)      # SM3M\napply_calibration!(psd_matrix, freqs, cal)\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{AbstractVector, AbstractVector, NoCalibration}","page":"API Reference","text":"apply_calibration!(out, signal, ::NoCalibration)\n\nPurpose:     Copy signal into out unchanged. Called when no calibration              profile is available; preserves the in-place API contract without              applying a spurious transformation.\n\nArguments:\n\nout::AbstractVector: Output buffer. Same length as signal.\nsignal::AbstractVector: Input signal in normalised ADC units [−1, 1].\n::NoCalibration: Calibration marker (no data).\n\nReturns:     out.\n\nConstraints: length(out) must equal length(signal) (enforced by copyto!).              Output is in the same units as input — normalised ADC, not µPa.\n\nFails when:  length(out) ≠ length(signal) (thrown by copyto!).\n\nExample:\n\napply_calibration!(out, signal, NoCalibration())\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{AbstractVector{Float64}, AbstractVector{Float64}, TFCalibration}","page":"API Reference","text":"apply_calibration!(out, signal, cal::TFCalibration; fs, plans=nothing)\n\nPurpose:     Apply frequency-dependent (transfer-function) calibration to a              time-domain signal in-place, converting raw ADC amplitudes to              calibrated physical-unit amplitudes (µPa). Calibration is applied              in the frequency domain: the signal is FFT'd, each bin's amplitude              is multiplied by the interpolated TF magnitude, and the result is              inverse-FFT'd. No FIR filter is constructed.\n\n         This is the time-domain path. For PSD-only calibration use\n         `apply_calibration!(psd_linear, freqs, cal::TFCalibration)` instead.\n\nArguments:\n\nout::AbstractVector{Float64}: Output buffer, same length as signal. Modified in-place.\nsignal::AbstractVector{Float64}: Input in normalised ADC units [−1, 1].\ncal::TFCalibration: Calibration object with frequency and tf_dB arrays.\nfs::Real: Sampling rate in Hz. Must be > 0.\nplans::Union{Nothing, Tuple} = nothing: Pre-computed FFT plan tuple (forward_plan, inverse_plan). When nothing (default), plans are computed internally on each call — correct for one-off use. For chunked processing, pre-compute once per chunk length and pass here to avoid repeated overhead. See the example below.\n\nReturns:     out, modified in-place.\n\nConstraints:\n\nlength(out) must equal length(signal).\nfs must be > 0.\nIf plans is provided, both plans must have been created for arrays of length(signal). A size mismatch raises at FFT apply time.\nTF interpolation clamps at cal.frequency[1] and cal.frequency[end]; DC (0 Hz) returns the value at cal.frequency[1].\nBoundary behaviour is circular (inherent to FFT convolution). For signals of 0.1 s or longer, wrap-around is negligible for smooth TF curves.\n\nFails when:\n\nlength(out) ≠ length(signal).\nfs ≤ 0.\n\nExample:\n\n# One-off use — plans computed automatically:\nout = similar(a.sig)\napply_calibration!(out, a.sig, cal; fs = Float64(a.fs))\n\n# Chunked use — pre-compute plans once for the chunk length, then re-use:\nbuf      = zeros(chunk_samples)\nfwd      = FFTW.plan_rfft(buf)\nplans    = (fwd, inv(fwd))\napply_calibration!(out, chunk.sig, cal; fs = Float64(chunk.fs), plans = plans)\n\nDo not use when: Only PSD-based metrics are needed — use the PSD-matrix form apply_calibration!(psd_linear, freqs, cal) instead (no FFT overhead, simpler).\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration!-Tuple{Matrix{Float64}, Vector{Float64}, TFCalibration}","page":"API Reference","text":"apply_calibration!(psd_linear, freqs, cal::TFCalibration)\n\nPurpose:     Apply frequency-dependent TF calibration to a linear PSD matrix              in-place, converting from full-scale²/Hz to µPa²/Hz. Each frequency              bin (row) is divided by the TF's linear power sensitivity at that              frequency, interpolated using linear-power-scale interpolation              (DD-11). This is the Raven Workbench convention.\n\nArguments:\n\npsd_linear::Matrix{Float64}: PSD in full-scale²/Hz, size (nfreqs, nframes). Modified in-place. Each row is one frequency bin; each column is one frame.\nfreqs::Vector{Float64}: Bin-centre frequencies in Hz. Length must equal size(psd_linear, 1). Typically SpectrogramResult.freqs.\ncal::TFCalibration: Calibration TF. tf_dB values in dB re full-scale per µPa (canonical form, DD-12). Interpolated at each bin frequency using linear-power-scale interpolation (DD-11); constant extrapolation at edges.\n\nReturns:     psd_linear, modified in-place. Units after return: µPa²/Hz.\n\nConstraints:\n\nsize(psd_linear, 1) must equal length(freqs).\nOperates on linear (not dB) PSD values. Passing dB PSD values will produce incorrect results without error.\nExtrapolation beyond cal.frequency bounds uses the nearest endpoint value.\n\nFails when:  size(psd_linear, 1) ≠ length(freqs).\n\nExample:\n\napply_calibration!(psd_matrix, spec.freqs, rockhopper_tf)\n\nDo not use when: The PSD is already in dB. Work on linear PSD and convert to dB after calibration via to_dB(psd_result).\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration!"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration-Tuple{AbstractVector{Float64}, Calibration}","page":"API Reference","text":"apply_calibration(signal, cal; kwargs...) -> Vector{Float64}\n\nPurpose:     Out-of-place version of apply_calibration! for time-domain              signals. Allocates a new output buffer and returns it.\n\nArguments:\n\nsignal::AbstractVector{Float64}: Input in normalised ADC units.\ncal::Calibration: Calibration object.\nkwargs...: Forwarded to apply_calibration!. For TFCalibration, fs is required; plans is optional (see apply_calibration! for details). Ignored for NoCalibration (no kwargs apply).\n\nReturns:     Vector{Float64} same length as signal.\n\nConstraints: For TFCalibration, fs must be provided.\n\nFails when:  Same conditions as the matching apply_calibration! method.\n\nExample:\n\nsig_cal = apply_calibration(signal, cal; fs = Float64(a.fs))\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration"},{"category":"method","location":"api/#EcoAcoustics.apply_calibration-Tuple{Audiodata}","page":"API Reference","text":"apply_calibration(a::Audiodata; plans=nothing) -> Audiodata\n\nPurpose:     Apply the calibration stored in a.calibration to the recording's              signal and return a new Audiodata with is_calibrated = true.              The in-place method apply_calibration! is used internally; this              wrapper handles allocation and struct reconstruction.\n\nArguments:\n\na::Audiodata: The recording to calibrate. Must have is_calibrated = false.\nplans::Union{Nothing, Tuple} = nothing: Pre-computed FFTW plan tuple (forward_plan, inverse_plan) for the TFCalibration time-domain path. Ignored for ScalarCalibration and NoCalibration. See apply_calibration! for details on creating and re-using plans across chunks.\n\nReturns:     A new Audiodata with:              - sig containing the calibrated signal.              - is_calibrated = true.              - All other fields (fs, starttime, calibration, metadata)                copied unchanged from a.\n\n         Special case: if `a.calibration isa NoCalibration`, returns `a`\n         unchanged with `is_calibrated` still `false` — no transform is\n         possible without calibration data.\n\nConstraints:\n\na.is_calibrated must be false. Double-application throws.\na.fs is passed automatically to the TFCalibration method; no caller action needed.\n\nFails when:  a.is_calibrated is already true.\n\nExample:\n\na_cal = apply_calibration(a)\n@assert a_cal.is_calibrated\n@assert !a.is_calibrated   # original unchanged\n\nDo not use when: a.calibration isa NoCalibration and calibrated output is required — no transformation is possible. Check a.calibration first.\n\n\n\n\n\n","title":"EcoAcoustics.apply_calibration"},{"category":"method","location":"api/#EcoAcoustics.average_psd-Tuple{PSDResult}","page":"API Reference","text":"average_psd(result::PSDResult) -> Vector{Float64}\n\nPurpose:     Average a multi-frame PSD matrix over time, returning a single              spectral estimate. The mean is computed in linear power space —              averaging in dB is not equivalent and is not done here. Convert              to dB after averaging via to_dB.\n\nArguments:\n\nresult::PSDResult: A computed PSD result, typically with multiple frames.\n\nReturns:     Vector{Float64} of length size(result.psd_linear, 1). Element              k is the mean of result.psd_linear[k, :] across all frames.              Units match result — check psd_units(result).\n\nConstraints:\n\nAveraging in linear power is the Merchant 2015 convention for LTSA and TOL band integration. Do not average in dB.\nFor a single-frame PSDResult, the output equals result.psd_linear[:, 1].\n\nFails when:  Never.\n\nExample:\n\nresult = compute_psd(audio; window_seconds = 1.0)\navg    = average_psd(result)         # Vector{Float64}, length = n_freqs\ndb     = to_dB(avg)                  # dB re µPa²/Hz (if calibrated)\n\n\n\n\n\n","title":"EcoAcoustics.average_psd"},{"category":"method","location":"api/#EcoAcoustics.band_metrics-Tuple{Audiodata}","page":"API Reference","text":"band_metrics(audio::Audiodata; bands, window_seconds, overlap_fraction=0.5,\n             window=:hann, nfft=nothing, fft_plan=nothing, cal=nothing,\n             environment=:water) -> Dict{Symbol, Any}\n\nPurpose:     Compute one tidy row of candidate-band metrics for a single              recording (typically one duty-cycle file). Calls              compute_psd then compute_spl and flattens the              per-band BandSPL results into named columns. The              percentiles are taken over the file's PSD frames.\n\nArguments:\n\naudio::Audiodata: The recording. Must resolve to a calibration (recorder profile, attached calibration, or cal=), or compute_spl will reject it.\nbands::Dict{Symbol,Tuple{Float64,Float64}}: Required (DD-27). Each entry maps a label to a (low_Hz, high_Hz) band.\nwindow_seconds::Real: FFT window duration in seconds (the per-frame timescale over which percentiles are taken). Required.\noverlap_fraction, window, nfft, fft_plan: Forwarded to compute_psd.\ncal::Union{Calibration,Nothing}=nothing: Override calibration, forwarded to compute_psd. Use for the SM3M per-era workaround (e.g. cal = ScalarCalibration(-165f0) for post-Apr-2018 files).\nenvironment::Symbol=:water: :water (reference 1 µPa) or :air (20 µPa).\n\nReturns:     Dict{Symbol,Any} — one row. Always contains :start_time              (DateTime), :duration_s (Float64), :n_frames (Int, the              number of PSD frames the percentiles are computed over), plus, for              every band k and metric m in              (:mean,:median,:L1,:L5,:L10,:L25,:L75,:L90,:L95,:L99,:max), a key              Symbol(k, :_, m, :_dB) holding a dB value.\n\nConstraints:\n\nPercentiles are over PSD frames, so n_frames (≈ file_duration ÷ hop) sets their statistical resolution. A very short file yields few frames and noisy percentiles.\nEA percentile direction: L1/L5 are the quiet floor, L95/L99 the loud tail (see docs/src/explanations/spl.md).\n\nFails when:  bands is empty (AssertionError); the audio cannot be calibrated              (AssertionError from compute_spl, DD-21); any compute_psd /              compute_spl failure (short signal, band above Nyquist, …).\n\nExample:\n\naudio = read_audio(\"cycle_0001.wav\"; recorder = \"sm3m\")\nbands = Dict(:tol_63 => (56.2, 70.8), :b100_200 => (100.0, 200.0))\nrow   = band_metrics(audio; bands = bands, window_seconds = 1.0)\nrow[:b100_200_mean_dB]   # energetic-mean 100–200 Hz level for this cycle\n\nDo not use when: You need a coarse per-column series over a long continuous              recording — use compute_spl on an LTSAResult instead.\n\n\n\n\n\n","title":"EcoAcoustics.band_metrics"},{"category":"method","location":"api/#EcoAcoustics.band_table-Tuple{Any}","page":"API Reference","text":"band_table(items; reader=identity, bands, window_seconds,\n           overlap_fraction=0.5, window=:hann, nfft=nothing, cal=nothing,\n           environment=:water, on_error=:skip) -> DataFrame\n\nPurpose:     Build a one-row-per-cycle table of candidate-band metrics from a              collection of recordings. Maps band_metrics over items              and assembles the rows into a typed DataFrame — the hand-off              product for the R-side soundscape analysis.\n\nArguments:\n\nitems: Any iterable of things to turn into rows — Audiodata objects, or file paths (with a matching reader), or anything reader maps to Audiodata.\nreader = identity: Function item -> Audiodata. Default identity treats each item as an Audiodata. For files, pass e.g. reader = f -> read_audio(f; recorder = \"sm3m\"). Reading happens inside the per-item try, so unreadable/corrupt files are skipped under on_error=:skip.\nbands, window_seconds, overlap_fraction, window, nfft, cal, environment: Forwarded to band_metrics for every item.\nfft_plan = nothing: Optional pre-built FFTW plan shared across all items (and threads). When nothing and parallel=:threads, one plan is built from the first item's sampling rate and reused; when nothing and parallel=:none, each item builds its own. Build with make_spectrogram_plan.\nparallel::Symbol = :none: :none processes items serially; :threads uses Threads.@threads over items (start Julia with -t N). Output is identical either way — rows are assembled by item index, so the threaded result is deterministic and matches the serial result row-for-row.\non_error::Symbol = :skip: :skip logs and drops any item whose read or metric computation throws; :fail re-throws (wrapped in a task exception under :threads).\nprogress::Bool = false: show a progress bar.\n\nReturns:     DataFrame, one row per successfully processed item, in item              order. Columns: start_time, duration_s, n_frames, then              <bandkey>_<metric>_dB for every band (sorted-key order) × metric              (mean, median, L1, L5, L10, L25, L75, L90, L95, L99, max).              Columns are typed (DateTime, Float64, Int, Float64) even              when the table is empty. Pass items already sorted by time (e.g.              sort(readdir(...))) to get a time-ordered table.\n\nConstraints:\n\nColumn set is derived from bands alone, so every row has the same schema regardless of which items succeeded.\nUnder parallel=:threads, the shared FFT plan assumes a uniform sampling rate across items (true for a single-recorder deployment); an item with a different fs fails the spectrogram size assertion and is skipped.\nitems is materialised with collect for index-based assembly, so pass a vector of file paths, not a generator that reads audio eagerly.\n\nFails when:  on_error is not :skip or :fail (ArgumentError); under              on_error=:fail, the first per-item error propagates.\n\nExample:\n\nfiles = readdir(\"deployment/\"; join = true)          # 3 months of 5-min files\nbands = Dict(:tol_63   => (56.2, 70.8),\n             :b100_200 => (100.0, 200.0),\n             :b1_4k    => (1000.0, 4000.0))\ndf = band_table(files;\n                reader = f -> read_audio(f; recorder = \"sm3m\"),\n                bands = bands, window_seconds = 1.0)\n# df: one row per duty cycle; write to CSV for R with CSV.write(\"bands.csv\", df)\n\nDo not use when: You need gap-aware coverage over a continuous timeline — that              is the source-backed archive-scale workflow, not this per-file map.\n\n\n\n\n\n","title":"EcoAcoustics.band_table"},{"category":"method","location":"api/#EcoAcoustics.build_index-Tuple{AbstractString}","page":"API Reference","text":"build_index(directory; recorder, output_path=nothing) -> Arrow.Table\n\nPurpose:     Walk a directory tree, read every WAV and FLAC file, and build a              timestamp index as an Arrow.Table with one row per file. The              index is the persistent record of what files exist, when they were              recorded, and how long each one is. Once built and saved, it is              loaded by IndexedFileSource for all subsequent time-range queries              — no re-reading of audio files is required. Only .wav and .flac              files are indexed; other formats (AIF, AIFF, OGG) are silently              skipped. Convert them to FLAC or WAV with sox before indexing.\n\nArguments:\n\ndirectory::AbstractString: Root directory to scan. Walks recursively; all .wav and .flac files at any depth are included. Case-insensitive extension matching (.WAV is included).\nrecorder::AbstractString: Recorder family name, e.g. \"sm3m\", \"rockhopper\". Every file in the directory is assumed to come from this recorder type. This drives filename parsing (to extract timestamps) and calibration lookup. Mixed-recorder directories are not supported in v1 — index each recorder's subdirectory separately.\noutput_path::Union{AbstractString,Nothing} = nothing: If provided, the index is written to this path as an Arrow IPC file. Load it later with load_index. If nothing, the index is returned in memory only.\n\nReturns:     Arrow.Table with one row per successfully indexed file, sorted              ascending by start_time. Columns match the EcoAcoustics index              schema exactly:              file_path (String), start_time (DateTime), end_time              (DateTime), fs (Float32), nsamples (Int64), recorder              (String), recorder_id (Union{String,Missing}), site_id              (Union{String,Missing}), hydrophone_id (Union{String,Missing}),              time_uncertainty_ms (Union{Float64,Missing}), notes              (Union{String,Missing}).\n\nConstraints: Only WAV and FLAC files are indexed. Files with other extensions              are silently skipped — this is intentional, not a bug. Convert              AIF/AIFF or other formats with sox before indexing. All files              must belong to the same recorder type. File paths in              the returned table are relative to directory — IndexedFileSource              resolves them against a root at read time. end_time is computed              from the actual decoded sample count, not the header-claimed value,              so truncated files record their true duration. recorder_id and              site_id are populated only when the recorder's filename format              encodes them. hydrophone_id, time_uncertainty_ms, and notes              are always missing in v1 (reserved for future use). Calibration              warnings are suppressed during indexing — calibration is irrelevant              at index-build time.\n\nFails when:\n\ndirectory does not exist or is not a directory.\nIndividual unreadable files are skipped with a @warn and counted; the builder does not abort.\n\nExample:\n\ntbl = build_index(\"/data/deploy1\"; recorder=\"sm3m\",\n                  output_path=\"/data/deploy1/index.arrow\")\ntbl.start_time          # access the start_time column directly\nload_index(\"/data/deploy1/index.arrow\")  # reload from disk later\n\nDo not use when: Files span more than one recorder type — build separate                  indexes per recorder subdirectory and combine in v2.\n\n\n\n\n\n","title":"EcoAcoustics.build_index"},{"category":"method","location":"api/#EcoAcoustics.chunks-Tuple{AbstractAudioSource}","page":"API Reference","text":"chunks(source; chunk_seconds, stride_seconds=chunk_seconds, gap_handling=:skip)\n\nPurpose:     Return a lazy iterator of Audiodata windows covering the time              span of source. Windows are generated on demand — no audio is              read until the iterator is advanced. This is the low-level              iteration primitive; process_chunks is the preferred entry point              for batch metric computation.\n\nArguments:\n\nsource::AbstractAudioSource — the audio source to iterate over.\nchunk_seconds::Real — duration of each window in seconds. Must be positive.\nstride_seconds::Real — step between successive window start times in seconds. Default equals chunk_seconds (non-overlapping). Values smaller than chunk_seconds produce overlapping windows.\ngap_handling::Symbol — controls behaviour at coverage gaps:\n:skip (default) — windows where coverage_fraction == 0.0 (entirely in a gap with no audio) are skipped. Windows with partial coverage are still emitted; their coverage_fraction annotates the fill fraction. Use this for duty-cycled recordings where off-period windows should be invisible to the analysis.\n:zero_fill — all windows in the source time span are emitted, with gap regions zero-filled in the returned Audiodata.\n:error — all windows are emitted; read_audio_range throws ArgumentError if any window has partial or zero coverage.\n\nReturns:     A ChunksIterator, eltype Audiodata. Usable in for loops and              with collect. length is not defined — the iterator has              Base.SizeUnknown() because the chunk count depends on gap              filtering and cannot be known without iterating.\n\nConstraints: chunk_seconds and stride_seconds are rounded to millisecond              precision (the resolution of DateTime). Sub-millisecond values              are coerced silently.\n\nFails when:\n\ngap_handling is not :skip, :zero_fill, or :error — throws ArgumentError.\nchunk_seconds or stride_seconds is not positive — throws ArgumentError.\n\nExample:\n\nsrc = SingleFileSource(\"T1-C__0__20170912_181500.wav\"; recorder=\"sm3m\")\nfor chunk in chunks(src; chunk_seconds = 60.0)\n    println(chunk.starttime, \"  nsamples=\", nsamples(chunk))\nend\n\nDo not use when: You need parallel execution and a DataFrame of results — use              process_chunks instead. Use read_audio_range directly for              one-off arbitrary time windows.\n\n\n\n\n\n","title":"EcoAcoustics.chunks"},{"category":"method","location":"api/#EcoAcoustics.compute_decidecade-Tuple{PSDResult}","page":"API Reference","text":"compute_decidecade(psd::PSDResult; kwargs...) -> SPLResult\ncompute_decidecade(audio::Audiodata; kwargs...) -> SPLResult\n\nPurpose:     Alias for compute_tol. \"Decidecade\" (ISO 18405:2017) is              the post-2018 underwater acoustics term for the same band scheme that              ANSI S1.11 calls \"third-octave\". Output is identical to compute_tol              with the same arguments.\n\nArguments:   Same as compute_tol. Returns:     Same as compute_tol. Constraints: Same as compute_tol. Fails when:  Same as compute_tol.\n\nExample:\n\ncompute_decidecade(psd) == compute_tol(psd)   # true (same band scheme)\n\n\n\n\n\n","title":"EcoAcoustics.compute_decidecade"},{"category":"method","location":"api/#EcoAcoustics.compute_ltsa-Tuple{Audiodata}","page":"API Reference","text":"compute_ltsa(audio::Audiodata; average_span_seconds,\n             fft_window_seconds=1.0, fft_overlap=0.5, window=:hann,\n             nfft=nothing, fft_plan=nothing, cal=nothing) -> LTSAResult\n\nPurpose:     Compute a Long-Term Spectral Average from an Audiodata recording.              The signal is divided into consecutive non-overlapping columns of              average_span_seconds each; within each column a PSD is computed              and its FFT frames are averaged in linear power (energetic mean).              Each averaged PSD is one column of the returned frequency × time              matrix. A trailing partial column (fewer than average_span_seconds              worth of samples) is dropped.\n\nArguments:\n\naudio::Audiodata: The recording to analyse. audio.sig is Vector{Float64}.\naverage_span_seconds::Real: Column width in seconds — the averaging span (e.g. 60.0 for 1-minute columns, 3600.0 for hourly). Must be > 0 and large enough to hold at least one FFT window (average_span_seconds × fs ≥ fft_window_seconds × fs).\nfft_window_seconds::Real = 1.0: Inner STFT window duration in seconds, forwarded to spectrogram as window_seconds.\nfft_overlap::Real = 0.5: Inner STFT frame overlap fraction in [0, 1), forwarded to spectrogram as overlap_fraction.\nwindow::Symbol = :hann: Inner STFT window shape (:hann, :hamming, :blackman, :rectangular).\nnfft::Union{Int,Nothing} = nothing: FFT length. nothing uses the window length; a larger even value zero-pads.\nfft_plan = nothing: Pre-built FFTW plan reused across every column (all columns share one FFT size). Build with make_spectrogram_plan; it must match the nfft the inner spectrogram chooses, or an AssertionError is thrown.\ncal::Union{Calibration,Nothing} = nothing: Override calibration. When nothing (default), calibration is auto-resolved once from audio via the same cascade as compute_psd (pre-calibrated signal → recorder profile → warn and fall back to full-scale). When supplied, cal is applied directly and the auto-resolution (and its possible warning) is skipped.\n\nReturns:     LTSAResult. matrix is (n_freqs, n_columns) in µPa²/Hz              (calibrated) or full-scale²/Hz (uncalibrated); check              ltsa_units or result.is_calibrated.              n_columns = floor(nsamples(audio) / (average_span_seconds × fs)).\n\nConstraints:\n\nColumns are non-overlapping and consecutive: column j covers samples (j-1)·S+1 .. j·S, where S = round(average_span_seconds × fs). The inner FFT frames of one column never cross into an adjacent column.\nColumn averaging is the energetic (linear-power) mean only, matching average_psd and the Merchant 2015 convention. There is no dB-mean or median form (DD-28).\nIf the audio length is not an exact multiple of the column span, the final partial column is dropped — its samples do not appear in any column.\nCalibration is resolved once, before the column loop; a missing-calibration warning (if any) is emitted a single time, not per column.\n\nFails when:\n\naverage_span_seconds <= 0 — AssertionError.\nfft_window_seconds <= 0 — AssertionError.\naverage_span_seconds × fs < fft_window_seconds × fs (a column cannot hold one FFT window) — AssertionError.\nnsamples(audio) < round(average_span_seconds × fs) (audio shorter than one full column) — AssertionError; there is no column to emit.\nAny condition that makes the inner spectrogram or compute_psd fail (e.g. odd nfft, mismatched fft_plan).\n\nExample:\n\naudio = read_audio(\"deployment_hour.flac\"; recorder = \"sm3m\")\nlt    = compute_ltsa(audio; average_span_seconds = 60.0)   # 1-minute columns\nsize(lt.matrix)        # (n_freqs, n_minutes)\nlt_dB = to_dB(lt)      # dB re 1 µPa²/Hz (SM3M scalar calibration auto-resolved)\n\nDo not use when:\n\nThe recording is shorter than one average_span_seconds column — there is nothing to average; call compute_psd directly instead.\nA single deployment-wide average spectrum is wanted rather than a time series — use compute_psd + average_psd over the whole signal.\n\n\n\n\n\n","title":"EcoAcoustics.compute_ltsa"},{"category":"method","location":"api/#EcoAcoustics.compute_millidecade-Tuple{PSDResult}","page":"API Reference","text":"compute_millidecade(psd::PSDResult; low_Hz=10.0, high_Hz=fs/2, environment=:water) -> SPLResult\ncompute_millidecade(audio::Audiodata; low_Hz=10.0, high_Hz=fs/2,\n                    environment=:water, window_seconds=1.0, overlap_fraction=0.5,\n                    window=:hann, nfft=nothing, fft_plan=nothing) -> SPLResult\n\nPurpose:     Compute band-integrated SPL in millidecade bands. Generates the              band set via millidecade_bands, then delegates to              compute_spl. The PSDResult method requires a pre-computed              PSD; the Audiodata method computes the PSD internally.\n\n         Millidecade bands are very fine resolution: near 1 kHz, each band\n         spans roughly 2.3 Hz, producing ~87 bands in the 900–1100 Hz range\n         versus 1 third-octave band. This resolution is the standard for\n         NOAA NRS and MANTA long-term soundscape monitoring. For broadband\n         surveys or comparisons with PAMGuide / Triton output, use\n         [`compute_tol`](@ref) instead.\n\nArguments:\n\npsd::PSDResult or audio::Audiodata: Input data.\nlow_Hz::Real = 10.0: Lower frequency bound. Must be > 0 (required by millidecade_bands for log10). Bands with center < low_Hz are excluded.\nhigh_Hz::Real = fs/2: Upper frequency bound (Nyquist). Bands with center\nhigh_Hz are excluded.\nenvironment::Symbol = :water: Reference pressure. :water → 1 µPa; :air → 20 µPa.\nwindow_seconds, overlap_fraction, window, nfft: Forwarded to compute_psd (Audiodata method only).\nfft_plan: Pre-built FFTW plan from make_spectrogram_plan. Forwarded through compute_psd → spectrogram (Audiodata method only). Not available on the PSDResult method — the FFT has already been computed.\n\nReturns:     SPLResult with one BandSPL per millidecade              band whose center falls in [low_Hz, high_Hz]. Band label keys              match millidecade_bands output (:mdec_N where N is              the integer index such that f_c = 10^(N/1000)). The number of              bands scales with the frequency range: ~230 bands per decade, so              a [10 Hz, 24 kHz] range yields ~3400 bands.\n\nConstraints:\n\nSame calibration requirement as compute_spl: psd_units(psd) must be :µPa²_per_Hz (DD-21).\nlow_Hz must be > 0; millidecade_bands throws ArgumentError otherwise.\nFor MANTA-compatible output, use the default :mdec_N label convention — do not rename band keys.\n\nFails when:\n\nSame conditions as compute_spl.\nlow_Hz ≤ 0 → ArgumentError from millidecade_bands.\n\nExample:\n\npsd    = compute_psd(audio; window_seconds = 1.0)\nresult = compute_millidecade(psd)\n# Keys: :mdec_N for each millidecade band in [10 Hz, Nyquist]\nresult.bands[:mdec_3000].mean_dB   # energetic mean 1-kHz millidecade SPL\n\n# Or directly from Audiodata:\nresult = compute_millidecade(audio; window_seconds = 1.0)\n\nDo not use when:\n\nThird-octave resolution suffices — compute_millidecade produces ~14× more bands per decade than compute_tol and proportionally more output to store and process.\nComparing against tools that use ANSI preferred-center third-octave bands — millidecade band edges do not align with ANSI S1.11 preferred centers.\n\nReferences: Miksis-Olds, J.L., et al. (2021). Ocean sound analysis software for making ambient noise trends accessible (MANTA). Frontiers in Marine Science, 8. Hatch, L.T., et al. (2016). Quantifying loss of acoustic communication space for right whales in and around a U.S. national marine sanctuary. Conservation Biology, 26(6), 983–994.\n\n\n\n\n\n","title":"EcoAcoustics.compute_millidecade"},{"category":"method","location":"api/#EcoAcoustics.compute_octave-Tuple{PSDResult}","page":"API Reference","text":"compute_octave(psd::PSDResult; low_Hz=10.0, high_Hz=fs/2, environment=:water) -> SPLResult\ncompute_octave(audio::Audiodata; low_Hz=10.0, high_Hz=fs/2,\n               environment=:water, window_seconds=1.0, overlap_fraction=0.5,\n               window=:hann, nfft=nothing, fft_plan=nothing) -> SPLResult\n\nPurpose:     Compute band-integrated SPL in ANSI S1.6 octave bands. Generates              the band set via octave_bands, then delegates to              compute_spl. The PSDResult method requires a              pre-computed PSD; the Audiodata method computes the PSD internally.\n\nArguments:\n\npsd::PSDResult or audio::Audiodata: Input data.\nlow_Hz::Real = 10.0: Lower frequency bound. Bands with ANSI S1.6 preferred center < low_Hz are excluded. Default 10 Hz matches the typical lower limit of hydrophone response.\nhigh_Hz::Real = fs/2: Upper frequency bound (Nyquist). Bands with ANSI S1.6 preferred center > high_Hz are excluded.\nenvironment::Symbol = :water: Reference pressure. :water → 1 µPa; :air → 20 µPa.\nwindow_seconds, overlap_fraction, window, nfft: Forwarded to compute_psd (Audiodata method only).\nfft_plan: Pre-built FFTW plan from make_spectrogram_plan. Forwarded through compute_psd → spectrogram (Audiodata method only). Not available on the PSDResult method — the FFT has already been computed.\n\nReturns:     SPLResult with one BandSPL per ANSI S1.6              octave band whose preferred center falls in [low_Hz, high_Hz].              Band label keys match octave_bands output (:oct_16,              :oct_31_5, :oct_1000, etc.).\n\nConstraints:\n\nSame calibration requirement as compute_spl: psd_units(psd) must be :µPa²_per_Hz (DD-21).\nhigh_Hz must not exceed Nyquist — passes through the band-edge validation in compute_spl, which throws ArgumentError if any band edge exceeds Nyquist.\n\nFails when:  Same conditions as compute_spl.\n\nExample:\n\npsd    = compute_psd(audio; window_seconds = 1.0)\nresult = compute_octave(psd)\n# Keys: :oct_16, :oct_31_5, :oct_63, ..., up to Nyquist\nresult.bands[:oct_1000].mean_dB   # energetic mean 1-kHz octave-band SPL\n\n# Or directly from Audiodata:\nresult = compute_octave(audio; window_seconds = 1.0)\n\nDo not use when:\n\nThird-octave or finer resolution is needed — use compute_tol or compute_millidecade.\nCustom band boundaries are required — use compute_spl directly.\n\nReferences: ANSI S1.6-1984 (R2006). Preferred Frequencies, Frequency Levels, and Band Numbers for Acoustical Measurements. Acoustical Society of America.\n\n\n\n\n\n","title":"EcoAcoustics.compute_octave"},{"category":"method","location":"api/#EcoAcoustics.compute_psd-Tuple{AbstractAudioSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"compute_psd(src, start, stop; gap_handling=:zero_fill,\n            window_seconds, ...) -> PSDResult\n\nPurpose:     Convenience wrapper: read a time window from an              AbstractAudioSource and compute PSD. Equivalent to calling              read_audio_range(src, start, stop) followed by the              Audiodata overload of compute_psd. Calibration is resolved              automatically from the resulting Audiodata (see              compute_psd(audio::Audiodata; ...)).\n\nArguments:\n\nsrc::AbstractAudioSource: Source to read from.\nstart::DateTime: Start of the time window (inclusive).\nstop::DateTime: End of the time window (exclusive).\ngap_handling::Symbol = :zero_fill: Gap handling passed to read_audio_range. :zero_fill inserts silence for gaps; :error raises on any gap.\nwindow_seconds::Real, overlap_fraction, window, nfft, fft_plan: Forwarded to spectrogram (see compute_psd(audio::Audiodata; ...)).\n\nReturns:     PSDResult for the requested window.\n\nConstraints:\n\nstop > start (enforced by read_audio_range).\nThe window must contain at least one full analysis frame after gap handling.\nFor large sources, prefer process_chunks with a compute_psd callback rather than loading the full time range at once.\n\nFails when:  stop <= start, or the resulting Audiodata is too short for one frame.\n\nExample:\n\nsrc    = IndexedFileSource(load_index(\"archive.arrow\"), \"archive/\")\nt0, t1 = time_range(src)\nresult = compute_psd(src, t0, t0 + Minute(5); window_seconds = 1.0)\n\n\n\n\n\n","title":"EcoAcoustics.compute_psd"},{"category":"method","location":"api/#EcoAcoustics.compute_psd-Tuple{AbstractAudioSource}","page":"API Reference","text":"compute_psd(src::AbstractAudioSource; window_seconds, ...) -> PSDResult\n\nPurpose:     Compute PSD over the full time extent of src. Thin delegation              to compute_psd(src, start, stop; ...) — see that method for all              parameter semantics, calibration resolution, and constraints.\n\nArguments:   Same keyword arguments as the range form. start and stop are              taken from time_range(src).\n\nExample:\n\nsrc    = SingleFileSource(\"recording.flac\"; recorder = \"rockhopper\")\nresult = compute_psd(src; window_seconds = 1.0)\n\n\n\n\n\n","title":"EcoAcoustics.compute_psd"},{"category":"method","location":"api/#EcoAcoustics.compute_psd-Tuple{Audiodata}","page":"API Reference","text":"compute_psd(audio::Audiodata; window_seconds, overlap_fraction=0.5,\n            window=:hann, nfft=nothing, fft_plan=nothing) -> PSDResult\n\nPurpose:     Convenience wrapper: compute PSD directly from an Audiodata              object. Calls spectrogram internally and resolves              calibration automatically via _psd_calibration:\n\n         1. If `audio.is_calibrated` is `true`, no PSD-layer calibration\n            is applied (`NoCalibration`). The PSD is in physical units\n            because the signal was already calibrated in the time domain.\n            `PSDResult.is_calibrated` is set to `true` to reflect this.\n         2. If `audio.calibration` is not `NoCalibration`, it is applied\n            at the PSD layer (e.g. `ScalarCalibration` for SM3M).\n         3. Otherwise, `get_profile(Symbol(audio.metadata.recorder))` is\n            tried. If the profile has a `:tf` field that is a\n            `TFCalibration`, it is used (handles Rockhopper automatically).\n         4. If no calibration is found, a warning is emitted and the PSD\n            is returned in full-scale²/Hz.\n\nArguments:\n\naudio::Audiodata: The recording to analyse.\nwindow_seconds::Real: Analysis window duration. Forwarded to spectrogram.\noverlap_fraction::Real = 0.5: Frame overlap fraction. Forwarded to spectrogram.\nwindow::Symbol = :hann: Window function. Forwarded to spectrogram.\nnfft::Union{Int,Nothing} = nothing: FFT length. Forwarded to spectrogram.\nfft_plan = nothing: Pre-computed FFTW plan. When supplied, forwarded to spectrogram to avoid per-call plan construction overhead. Build with make_spectrogram_plan. Must match the nfft that spectrogram would choose; a size mismatch throws AssertionError (DD-04).\ncal::Union{Calibration,Nothing} = nothing: Override calibration. When supplied, bypasses _psd_calibration and applies cal directly. When nothing (default), calibration is auto-resolved from audio.metadata.\n\nReturns:     PSDResult. Units depend on the resolved calibration —              check psd_units(result) or result.is_calibrated.\n\nConstraints:\n\naudio.sig must be non-empty and long enough for at least one frame.\nAll other constraints from spectrogram apply.\nWhen audio.is_calibrated = true, PSDResult.is_calibrated is set to true even though cal is NoCalibration at the PSD layer. This ensures psd_units returns :µPa²_per_Hz for pre-calibrated signals.\n\nFails when:  Same conditions as spectrogram.\n\nExample:\n\naudio = read_audio(\"recording.flac\"; recorder=\"rockhopper\")\nresult = compute_psd(audio; window_seconds = 1.0)   # auto-calibrates via Rockhopper profile\nresult_explicit = compute_psd(audio; window_seconds = 1.0, cal = NoCalibration())\n\n\n\n\n\n","title":"EcoAcoustics.compute_psd"},{"category":"method","location":"api/#EcoAcoustics.compute_spl-Tuple{Audiodata}","page":"API Reference","text":"compute_spl(audio::Audiodata; bands, environment=:water,\n            window_seconds=1.0, overlap_fraction=0.5,\n            window=:hann, nfft=nothing) -> SPLResult\n\nPurpose:     Convenience wrapper: compute band-integrated SPL directly from              an Audiodata object. Calls compute_psd internally,              then delegates to the compute_spl(psd::PSDResult; ...) primitive.              Calibration is resolved automatically via the three-step cascade              in compute_psd (DD-14):              1. audio.is_calibrated == true → no further calibration at PSD layer              2. audio.calibration isa !NoCalibration → apply it at PSD layer              3. get_profile(Symbol(recorder)) → use recorder TF if available                 (handles Rockhopper automatically)              4. No calibration found → @warn; compute_spl then asserts and fails\n\n         This means an uncalibrated recording with no resolvable profile will\n         fail at the `compute_spl` assertion (DD-21), not silently return\n         meaningless SPL values.\n\nArguments:\n\naudio::Audiodata: Recording to analyse.\nbands: Forwarded to compute_spl(psd; ...). Required — see that method for semantics (DD-27).\nwindow_seconds::Real = 1.0: Analysis window duration. Forwarded to compute_psd → spectrogram.\noverlap_fraction::Real = 0.5: Frame overlap fraction. Forwarded to compute_psd.\nwindow::Symbol = :hann: Window function. One of :hann, :hamming, :blackman, :rectangular. Forwarded to compute_psd.\nnfft::Union{Int,Nothing} = nothing: FFT length. nothing uses window_length (no zero-padding). Forwarded to compute_psd.\nfft_plan = nothing: Pre-built FFTW plan from make_spectrogram_plan. Forwarded through compute_psd → spectrogram. Pass a plan when processing many chunks at the same sample rate and window size to avoid repeated FFTW wisdom lookups. The plan's FFT length must match the resolved nfft; a mismatch throws AssertionError (DD-04).\n\nReturns:     SPLResult. All fields identical to calling              compute_spl(compute_psd(audio; ...), bands=bands, environment=environment).\n\nConstraints:\n\nThe calibration cascade must resolve to a non-NoCalibration result, or audio.is_calibrated must be true. If neither holds, compute_psd emits a warning and returns an uncalibrated PSDResult, after which compute_spl asserts and throws AssertionError (DD-21).\nAll constraints from spectrogram and compute_psd apply.\n\nFails when:  Same conditions as compute_spl(psd::PSDResult; ...) plus any              failure modes of compute_psd.\n\nExample:\n\naudio = read_audio(\"recording.flac\"; recorder = \"rockhopper\")\nresult = compute_spl(audio;\n                     window_seconds = 1.0,\n                     bands = Dict(:full => (10.0, Float64(audio.fs) / 2)))\nresult.bands[:full].mean_dB   # energetic mean SPL, dB re 1 µPa\n\nDo not use when:\n\nYou already have a PSDResult — pass it directly to compute_spl(psd; ...) to avoid recomputing the spectrogram.\nCustom spectral parameters are needed alongside specific bands — build the PSDResult explicitly, inspect it, then call the primitive.\n\n\n\n\n\n","title":"EcoAcoustics.compute_spl"},{"category":"method","location":"api/#EcoAcoustics.compute_spl-Tuple{LTSAResult}","page":"API Reference","text":"compute_spl(ltsa::LTSAResult; bands, environment=:water) -> SPLResult\n\nPurpose:     Integrate frequency bands out of an LTSA, producing one band-level              time series per band. Each LTSA time column is treated as a single              spectral estimate: for every column the power is summed across the              band's bins, giving a per-column band SPL series plus the energetic              mean and percentiles over columns. This is the band time series used              for temporal / diel / seasonal soundscape plots. Reuses the same              _integrate_bands core as compute_spl(::PSDResult) — the only              difference is that a \"column\" here is an LTSA time column rather than              an FFT frame.\n\nArguments:\n\nltsa::LTSAResult: A calibrated LTSA (µPa²/Hz). Uncalibrated input fails.\nbands::Dict{Symbol,Tuple{Float64,Float64}}: Required (DD-27). Each entry maps a label to a (low_Hz, high_Hz) band; a bin is included when its centre lies in [low_Hz, high_Hz]. No default.\nenvironment::Symbol = :water: :water (reference 1 µPa) or :air (20 µPa).\n\nReturns:     SPLResult. Each BandSPL.spl_dB is the per-column series              (length = number of LTSA columns); SPLResult.time is              ltsa.column_times (seconds from the audio start). Units              :dB_re_1µPa for water.\n\nConstraints:\n\nltsa must be calibrated (ltsa_units(ltsa) === :µPa²_per_Hz).\nPercentiles are taken over LTSA columns, so their statistical meaning depends on the column span (ltsa.average_span_seconds). A handful of coarse columns gives a coarse distribution.\n\nFails when:  Uncalibrated LTSA (AssertionError, DD-21); a band with              low_Hz ≥ high_Hz or high_Hz > Nyquist (ArgumentError); no bins              fall in a band (ArgumentError).\n\nExample:\n\nlt  = compute_ltsa(audio; average_span_seconds = 60.0)\nspl = compute_spl(lt; bands = Dict(:b100_200 => (100.0, 200.0)))\nspl.bands[:b100_200].spl_dB    # per-minute band level series (dB re 1 µPa)\nspl.bands[:b100_200].mean_dB   # energetic mean over all columns\n\nDo not use when: You want a fine within-file distribution for percentiles — a              file's PSD frames (compute_spl(compute_psd(audio); bands)) give              hundreds of samples per file, whereas an LTSA offers only as many              samples as it has columns.\n\n\n\n\n\n","title":"EcoAcoustics.compute_spl"},{"category":"method","location":"api/#EcoAcoustics.compute_spl-Tuple{PSDResult}","page":"API Reference","text":"compute_spl(psd::PSDResult; bands, environment=:water) -> SPLResult\n\nPurpose:     Compute band-integrated sound pressure level from a calibrated              PSDResult. For each requested frequency band, integrates              the PSD over the band's bins, converts to dB SPL, and computes              per-frame and aggregate statistics.\n\n         Integration formula for each band [f_lo, f_hi]:\n           band_power[j] = Σ_k psd[k,j] × df        (µPa²)\n         where the sum runs over all bins k with f_lo ≤ freqs[k] ≤ f_hi\n         and df = freqs[2] − freqs[1] (uniform bin width).\n\n           SPL[j] = 10 × log10(band_power[j] / pref²)\n         where pref = 1.0 µPa (water) or 20.0 µPa (air).\n\nArguments:\n\npsd::PSDResult: Calibrated PSD. Must satisfy psd_units(psd) === :µPa²_per_Hz. Use compute_psd with an appropriate calibration, or call compute_spl(audio::Audiodata; ...) which resolves calibration automatically.\nbands::Dict{Symbol, Tuple{Float64,Float64}}: Frequency bands to integrate. Each entry maps a label Symbol to a (low_Hz, high_Hz) tuple in Hz. There is no default — bands must always be supplied explicitly (DD-27). Overlapping bands are permitted; each is integrated independently. For standard band sets use the convenience wrappers: compute_tol, compute_octave, compute_millidecade.\nenvironment::Symbol = :water: Acoustic medium. Determines the reference pressure:\n:water → pref = 1 µPa, output units :dB_re_1µPa\n:air   → pref = 20 µPa, output units :dB_re_20µPa\n\nReturns:     SPLResult with one BandSPL per label.              Each BandSPL.spl_dB has the same length as psd.time.\n\nConstraints:\n\npsd_units(psd) must be :µPa²_per_Hz. Uncalibrated PSDs are in full-scale²/Hz — integrating them produces numbers with no acoustic meaning. The assertion fires with a message pointing to the fix (DD-21).\nPSD frequency bins must be uniformly spaced (guaranteed for PSD from spectrogram; asserted here to document the assumption).\nBand edges are validated before any integration:\nhigh_Hz > Nyquist → ArgumentError listing all offending labels\nlow_Hz ≥ high_Hz  → ArgumentError listing all offending labels\nlow_Hz < 10 Hz    → consolidated @warn listing offending labels (below typical hydrophone response; may be intentional, so only warned)\n\nFails when:\n\npsd_units(psd) ≠ :µPa²_per_Hz         → AssertionError (DD-21)\nAny band has high_Hz > psd.fs / 2      → ArgumentError\nAny band has low_Hz ≥ high_Hz          → ArgumentError\nAny band has no PSD bins in range        → ArgumentError naming the band\n\nExample:\n\npsd    = compute_psd(audio; window_seconds = 1.0)\nresult = compute_spl(psd; bands = Dict(\n    :full  => (10.0, Float64(psd.fs) / 2),\n    :tonal => (18000.0, 22000.0)))\nresult.bands[:full].mean_dB    # energetic mean SPL, dB re 1 µPa\nresult.bands[:tonal].L99_dB   # 99th-percentile tonal SPL\n\nDo not use when:\n\npsd is uncalibrated — the assertion will fire. Use the Audiodata wrapper or supply calibration explicitly to compute_psd.\nStandard band sets (octave, third-octave, millidecade) are needed — use compute_octave, compute_tol, or compute_millidecade, which populate bands automatically.\nSystem-weighted broadband SPL (ADEON DPS Figure 1, left path) is required. This function implements the frequency-flat path only: the full calibrated PSD is integrated over the band. System-weighted SPL applies a single sensitivity value at a representative frequency (typically 250 Hz) directly to the time-domain signal and is not available in v1. For instruments with flat frequency response the two paths agree; for instruments with frequency-dependent response (e.g. Rockhopper TF calibration), the frequency-flat path is the physically correct choice. See DD-26.\n\nReferences: Merchant et al. (2015) Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265.\n\n\n\n\n\n","title":"EcoAcoustics.compute_spl"},{"category":"method","location":"api/#EcoAcoustics.compute_tol-Tuple{PSDResult}","page":"API Reference","text":"compute_tol(psd::PSDResult; low_Hz=10.0, high_Hz=fs/2, environment=:water) -> SPLResult\ncompute_tol(audio::Audiodata; low_Hz=10.0, high_Hz=fs/2,\n            environment=:water, window_seconds=1.0, overlap_fraction=0.5,\n            window=:hann, nfft=nothing, fft_plan=nothing) -> SPLResult\n\nPurpose:     Compute band-integrated SPL in ANSI S1.11 third-octave (decidecade)              bands. Generates the band set via tol_bands, then delegates              to compute_spl. The PSDResult method requires a              pre-computed PSD; the Audiodata method computes the PSD internally.              compute_decidecade is an alias for this function.\n\nArguments:\n\npsd::PSDResult or audio::Audiodata: Input data.\nlow_Hz::Real = 10.0: Lower frequency bound. Bands with ANSI preferred center < low_Hz are excluded. Default 10 Hz matches the typical lower limit of hydrophone response.\nhigh_Hz::Real = fs/2: Upper frequency bound (Nyquist of the recording). Bands with ANSI preferred center > high_Hz are excluded. Defaults to Nyquist to clip at the recording's frequency limit automatically.\nenvironment::Symbol = :water: Reference pressure. :water → 1 µPa (:dB_re_1µPa); :air → 20 µPa (:dB_re_20µPa).\nwindow_seconds, overlap_fraction, window, nfft: Forwarded to compute_psd (Audiodata method only; not available on PSDResult method).\nfft_plan: Pre-built FFTW plan from make_spectrogram_plan. Forwarded through compute_psd → spectrogram (Audiodata method only). Pass when processing many chunks at the same window size to avoid repeated FFTW wisdom lookups. The plan length must match the resolved nfft (DD-04). Not available on the PSDResult method — the FFT has already been computed.\n\nReturns:     SPLResult with one BandSPL per ANSI S1.11              band whose preferred center falls in [low_Hz, high_Hz]. Band              label keys match tol_bands output (:tol_N, :tol_12_5,              etc.). Returns a result with an empty bands Dict if no ANSI S1.11              preferred centers fall in [low_Hz, high_Hz].\n\nConstraints:\n\nSame calibration requirement as compute_spl: psd_units(psd) must be :µPa²_per_Hz. Uncalibrated input throws AssertionError (DD-21).\nBand edges validated by compute_spl: high_Hz > Nyquist throws ArgumentError; low_Hz < 10 Hz triggers @warn.\n\nFails when:  Same conditions as compute_spl.\n\nExample:\n\npsd    = compute_psd(audio; window_seconds = 1.0)\nresult = compute_tol(psd)\n# Keys: :tol_10, :tol_12_5, :tol_16, ..., :tol_N (up to Nyquist)\nresult.bands[:tol_1000].mean_dB   # energetic mean 1-kHz third-octave SPL\n\n# Or directly from Audiodata:\nresult = compute_tol(audio; window_seconds = 1.0)\n\nDo not use when:\n\nOctave-band resolution suffices — use compute_octave.\nSub-Hz resolution is needed — use compute_millidecade.\nCustom band boundaries are required — use compute_spl with an explicit bands Dict.\n\nReferences: ANSI S1.11-2004 (R2009). Specification for Octave-Band and Fractional-Octave-Band Analog and Digital Filters. Acoustical Society of America. ISO 18405:2017. Underwater Acoustics — Terminology. International Organization for Standardization.\n\n\n\n\n\n","title":"EcoAcoustics.compute_tol"},{"category":"method","location":"api/#EcoAcoustics.coverage_fraction-Tuple{AbstractAudioSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"coverage_fraction(source, start, stop) -> Float64\n\nPurpose:     Return the fraction of the time window [start, stop] that              contains real audio data (as opposed to silence or gaps).\n\nArguments:\n\nsource::AbstractAudioSource: The audio source to query.\nstart::DateTime: Start of the window.\nstop::DateTime: End of the window.\n\nReturns:     Float64 in [0.0, 1.0]. 1.0 means the window is fully              covered by audio. 0.0 means no audio exists in the window.\n\nConstraints: The default implementation always returns 1.0 — it does not              validate start < stop or inspect the source at all. Subtypes              that have gap information — such as IndexedFileSource — should              override this method and enforce start < stop.\n\nExample:\n\nfrac = coverage_fraction(source, DateTime(2023,10,12), DateTime(2023,10,13))\n\n\n\n\n\n","title":"EcoAcoustics.coverage_fraction"},{"category":"method","location":"api/#EcoAcoustics.coverage_fraction-Tuple{IndexedFileSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"coverage_fraction(source::IndexedFileSource, start, stop) -> Float64\n\nPurpose:     Return the fraction of the window [start, stop] that is covered              by audio files in the index. Gaps between files count as zero              coverage.\n\nArguments:\n\nsource::IndexedFileSource: The source to query.\nstart::DateTime, stop::DateTime: The time window of interest.\n\nReturns:     Float64 in [0.0, 1.0]. 1.0 means all of [start, stop] is              covered by audio. 0.0 means no files overlap the window.\n\nConstraints: Assumes files in the index do not overlap each other — a properly              built index always satisfies this. If files do overlap, coverage              may be over-counted. Returns 0.0 for degenerate windows              (stop <= start).\n\nFails when:  Never.\n\nExample:\n\nfrac = coverage_fraction(src, DateTime(2023,1,1), DateTime(2023,1,2))\n\n\n\n\n\n","title":"EcoAcoustics.coverage_fraction"},{"category":"method","location":"api/#EcoAcoustics.coverage_fraction-Tuple{SingleFileSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"coverage_fraction(source::SingleFileSource, start, stop) -> Float64\n\nPurpose:     Return the fraction of [start, stop] covered by this file's              audio. Used by read_audio_range and the future chunks iterator              to annotate each chunk with its data quality.\n\nArguments:\n\nsource::SingleFileSource: The source to query.\nstart::DateTime, stop::DateTime: The time window of interest.\n\nReturns:     Float64 in [0.0, 1.0]. Computed as the millisecond length of              the intersection of [start, stop] with the file's time range,              divided by the millisecond length of [start, stop].\n\nConstraints: start must be before stop. Returns 0.0 for degenerate              windows (start >= stop).\n\nFails when:  Never throws; degenerate windows return 0.0.\n\nExample:\n\nfrac = coverage_fraction(src, DateTime(2017,9,12,18,15), DateTime(2017,9,12,18,16))\n\n\n\n\n\n","title":"EcoAcoustics.coverage_fraction"},{"category":"method","location":"api/#EcoAcoustics.decidecade_bands-Tuple{Real, Real}","page":"API Reference","text":"decidecade_bands(low_Hz, high_Hz) -> Dict{Symbol, Tuple{Float64, Float64}}\n\nPurpose:     Alias for tol_bands. Returns ANSI S1.11 third-octave              (decidecade) bands with :tol_N label keys. The \"decidecade\"              (ISO 18405:2017) and \"third-octave\" (ANSI S1.11) terms refer to the              same band scheme; the former is preferred in the post-2018 underwater              acoustics literature. Output is identical to tol_bands.\n\nArguments:   Same as tol_bands. Returns:     Same as tol_bands. Constraints: Same as tol_bands — base-10 decidecade edges matching              ISO 18405 / ADEON DPS / PAMGuide (DD-30). Fails when:  Never.\n\nExample:\n\ndecidecade_bands(10.0, 1000.0) == tol_bands(10.0, 1000.0)   # true\n\n\n\n\n\n","title":"EcoAcoustics.decidecade_bands"},{"category":"method","location":"api/#EcoAcoustics.duration-Tuple{Audiodata}","page":"API Reference","text":"duration(a::Audiodata) -> Millisecond\n\nPurpose:     Compute the duration of the recording.\n\nArguments:\n\na::Audiodata: The recording.\n\nReturns:     Millisecond. Use .value to extract the integer count if              needed.\n\nConstraints: Returns Millisecond(0) for an empty recording.\n\nFails when:  Never.\n\nExample:\n\nduration(a)          # e.g. Millisecond(60000) for a 60-second recording\nduration(a).value    # 60000 (integer milliseconds)\n\n\n\n\n\n","title":"EcoAcoustics.duration"},{"category":"method","location":"api/#EcoAcoustics.endtime-Tuple{Audiodata}","page":"API Reference","text":"endtime(a::Audiodata) -> DateTime\n\nPurpose:     Compute the recording end time from starttime, sample count,              and sampling rate.\n\nArguments:\n\na::Audiodata: The recording.\n\nReturns:     DateTime. Computed using nanosecond arithmetic to minimise              rounding error, then stored as a DateTime (millisecond              resolution). Sub-millisecond remainders are truncated.\n\nConstraints: Returns starttime unchanged for an empty recording              (nsamples = 0). See docs/src/sources.md for why              read_audio_range avoids round-tripping through endtime              when slicing at the file boundary.\n\nFails when:  Never.\n\nExample:\n\nendtime(a)   # e.g. DateTime(\"2023-10-12T00:07:54.000\")\n\n\n\n\n\n","title":"EcoAcoustics.endtime"},{"category":"method","location":"api/#EcoAcoustics.get_profile-Tuple{Symbol}","page":"API Reference","text":"get_profile(id::Symbol) -> AbstractRecorderProfile\n\nPurpose:     Return the singleton calibration profile for the recorder identified              by id. The profile carries the shipped TFCalibration and any              recorder-level constants. Called automatically by compute_psd when              no explicit cal argument is supplied.\n\nArguments:\n\nid::Symbol: Recorder family identifier (e.g. :rockhopper). Resolved via Val{id} dispatch — each supported recorder registers a get_profile(::Val{:<name>}) method in its own source file.\n\nReturns:     The concrete AbstractRecorderProfile subtype for that recorder.              Always the same singleton object; do not mutate.\n\nConstraints: Calibration is pinned to the package version (DD-13). To use a              different calibration, construct a TFCalibration via              load_tf_calcurves and pass it explicitly as cal= to compute_psd.\n\nFails when:  id is not recognised. Raises ArgumentError with the unknown              symbol and a pointer to how to add a new recorder.\n\nExample:\n\ntf = get_profile(:rockhopper).tf    # TFCalibration for Rockhopper\n\n\n\n\n\n","title":"EcoAcoustics.get_profile"},{"category":"method","location":"api/#EcoAcoustics.load_index-Tuple{AbstractString}","page":"API Reference","text":"load_index(path) -> Arrow.Table\n\nPurpose:     Load a previously built index from an Arrow IPC file and return              it as an Arrow.Table. Validates that all expected schema columns              are present before returning.\n\nArguments:\n\npath::AbstractString: Path to an Arrow file written by build_index.\n\nReturns:     Arrow.Table with the same column layout as build_index. Access              columns as tbl.start_time, tbl.file_path, etc. The table is              memory-mapped when possible, so loading a large index does not              require reading the whole file into RAM.\n\nConstraints: The file must have been written by build_index or otherwise              contain all eleven expected columns. Extra columns are ignored.              Column types are as written; no type coercion is performed.\n\nFails when:\n\npath does not exist or cannot be read.\nAny of the eleven required columns is absent.\n\nExample:\n\ntbl = load_index(\"/data/deploy1/index.arrow\")\ntbl.start_time\n\n\n\n\n\n","title":"EcoAcoustics.load_index"},{"category":"method","location":"api/#EcoAcoustics.load_tf_calcurves-Tuple{AbstractString}","page":"API Reference","text":"load_tf_calcurves(path; vmax_peak_V) -> TFCalibration\n\nPurpose:     Load a Cornell-format Rockhopper calibration CSV and return a              TFCalibration in the package canonical form (dB re full-scale per              µPa, DD-12). The primary caller is this file's module-init block;              the function is exported so advanced users can load a custom or              updated calibration file.\n\nArguments:\n\npath::AbstractString: Path to a CSV file with a named-column header row. Required columns: Frequency_Hz and AnalogSensitivity_dB_re_1VperRefPress. Optional columns (SensorSensitivity_dB_re_1V_perRefPress, PreampGain_dB, RecorderGain_dB) are present in the Cornell file for provenance but are not used arithmetically by this loader. Additional unknown columns are silently ignored. Column order does not matter.\nvmax_peak_V::Float64: ADC full-scale peak voltage in volts. 5.0 for the Rockhopper. Used to convert AnalogSensitivity to the canonical form: tf_dB = AnalogSensitivity_dB − 20·log10(vmax_peak_V).\n\nReturns:     TFCalibration with:              - frequency: strictly ascending Hz values from the Frequency_Hz column.              - tf_dB: canonical sensitivity in dB re full-scale per µPa.              - source: the absolute path supplied.              - format: :rockhopper_calcurves_csv.              - conversion_notes: records the exact dB shift applied.\n\nConstraints:\n\nFrequency_Hz must be strictly ascending (no duplicates).\nvmax_peak_V must be > 0.\nThe resulting TFCalibration is validated by the inner constructor (≥ 2 points, strictly ascending, matching lengths).\n\nFails when:\n\npath is not found or unreadable.\nA required column is missing (error names the missing column and lists required ones).\nFrequency_Hz is not strictly ascending.\nAny row has the wrong number of fields.\n\nLegacy file formats not supported — read before using a custom file:\n\nCornell TF column (column 13 of RH_calCurves). The Excel sheet also contains a column called TF that folds in a PeakToRMS factor of 20·log10(2√2) ≈ 9.03 dB. This factor is appropriate for converting a peak-calibrated sine to RMS but is not appropriate for PSD, which operates on instantaneous squared samples. A CSV built from the TF column rather than the AnalogSensitivity column (column 5) will produce PSD values that are 9.03 dB too high. This loader cannot detect the error from column names alone if the CSV was constructed incorrectly; verify you have extracted column 5.\nBare 2-column Raven Expedition CSV (no header row, columns: frequency, dB re full-scale per µPa). Those values are already in canonical form; passing such a file to this loader would subtract 20·log10(vmaxpeakV) ≈ 13.98 dB a second time, producing a calibration that is 13.98 dB too negative and PSD values that are 13.98 dB too low. Use _load_tf_csv (private, headerless-CSV reader) and construct TFCalibration manually if you have a bare 2-column file.\n\nExample:\n\n# Package-internal use (module init):\ntf = load_tf_calcurves(\n    joinpath(@__DIR__, \"calibration_data\", \"rockhopper_tf_calibration.csv\");\n    vmax_peak_V = 5.0)\n\n# Advanced user with a custom calibration file:\nmy_tf = load_tf_calcurves(\"/path/to/my_RH_calCurves_extract.csv\"; vmax_peak_V = 5.0)\npsd   = compute_psd(audio; cal = my_tf)\n\nDo not use when:\n\nYou have a bare 2-column CSV (no header). See the legacy warning above.\nYou have a CSV derived from the Cornell TF column (column 13). See above.\n\n\n\n\n\n","title":"EcoAcoustics.load_tf_calcurves"},{"category":"method","location":"api/#EcoAcoustics.lookup_calibration-Tuple{AbstractString, AbstractString, Any}","page":"API Reference","text":"lookup_calibration(path, recorder, meta; strict=false)\n\nPurpose:     Return a Calibration object for a recording based on its              recorder family.\n\nArguments:\n\npath::AbstractString: File path. Currently unused; reserved for per-file TF calibration lookup in v2.\nrecorder::AbstractString: Recorder family name (e.g. \"sm3m\").\nmeta: Named tuple from parse_filename. Currently unused; reserved for per-serial calibration overrides in v2.\nstrict::Bool = false: Controls behaviour when no calibration profile is found. strict=false (default): emit @warn and return NoCalibration() (exploration mode — forgiving). strict=true: throw an ArgumentError naming the unrecognised recorder (production mode — fail fast). See DD-17.\n\nReturns:     ScalarCalibration for recorders with a scalar profile;              TFCalibration for recorders registered with a tf_path;              NoCalibration when no profile is found (with @warn at              strict=false, or a thrown ArgumentError at strict=true).\n\nConstraints: Only scalar calibration is returned in v1. TF calibration              (frequency-dependent) is constructed separately when needed.\n\nFails when:  strict=true and no calibration profile is registered for              the given recorder (throws ArgumentError).\n\nExample:\n\ncal = lookup_calibration(\"file.wav\", \"sm3m\", meta)\n\n\n\n\n\n","title":"EcoAcoustics.lookup_calibration"},{"category":"method","location":"api/#EcoAcoustics.ltsa_units-Tuple{LTSAResult}","page":"API Reference","text":"ltsa_units(result::LTSAResult) -> Symbol\n\nPurpose:     Return the physical unit of result.matrix as a Symbol, mirroring              psd_units. Dispatches on the is_calibrated flag, not on              the calibration type.\n\nReturns:     :µPa²_per_Hz when result.is_calibrated, else              :fullscale²_per_Hz.\n\nFails when:  Never.\n\nExample:\n\nlt = compute_ltsa(audio; average_span_seconds = 60.0)\nltsa_units(lt)   # :µPa²_per_Hz for a calibrated recording\n\n\n\n\n\n","title":"EcoAcoustics.ltsa_units"},{"category":"function","location":"api/#EcoAcoustics.make_spectrogram_plan","page":"API Reference","text":"make_spectrogram_plan(fs, window_seconds, nfft=nothing) -> plan\n\nPurpose:     Build an FFTW plan for the FFT size that spectrogram will use with              the same fs, window_seconds, and nfft arguments. Pre-building              the plan once and passing it to every spectrogram call via the              fft_plan kwarg avoids FFTW's per-call plan-selection overhead,              which matters when processing thousands of chunks.\n\nArguments:\n\nfs::Real: Sampling rate in Hz. Must match the fs passed to spectrogram.\nwindow_seconds::Real: Window duration in seconds. Must match the window_seconds passed to spectrogram.\nnfft::Union{Int,Nothing} = nothing: FFT length. When nothing (default), equals window_length = round(Int, window_seconds * fs). Must match the nfft passed to spectrogram.\n\nReturns:     An FFTW plan sized for arrays of length nfft. Pass directly as              the fft_plan kwarg to spectrogram.\n\nConstraints:\n\nfs, window_seconds, and nfft must be identical to the arguments used in the corresponding spectrogram call. A mismatch will be caught by the size assertion in spectrogram.\nThe formula window_length = round(Int, window_seconds * fs) and nfft_actual = isnothing(nfft) ? window_length : nfft must mirror those in spectrogram exactly. If either formula changes in spectrogram, update this function to match.\n\nFails when:  No explicit validation — spectrogram will assert on a size              mismatch when the plan is used.\n\nExample:\n\n# Pre-build a plan for 48 kHz data with a 0.1 s window, then reuse it across\n# many chunks fed through process_chunks:\nplan = make_spectrogram_plan(48000.0, 0.1)\n\nresult1 = spectrogram(chunk1.sig; fs = 48000.0, window_seconds = 0.1, fft_plan = plan)\nresult2 = spectrogram(chunk2.sig; fs = 48000.0, window_seconds = 0.1, fft_plan = plan)\n\n\n\n\n\n","title":"EcoAcoustics.make_spectrogram_plan"},{"category":"method","location":"api/#EcoAcoustics.millidecade_bands-Tuple{Real, Real}","page":"API Reference","text":"millidecade_bands(low_Hz, high_Hz) -> Dict{Symbol, Tuple{Float64, Float64}}\n\nPurpose:     Return a Dict of millidecade band integration edges for every band              whose center frequency falls in [low_Hz, high_Hz]. A millidecade              is 1/1000 of a decade on a log-frequency axis; each band spans a              frequency ratio of 10^(1/1000) ≈ 1.0023 (about 0.23%). This is the              high-resolution band scheme used by NOAA's National Recording System              (NRS) and MANTA for long-term soundscape monitoring.\n\n         Resolution note: millidecade bands are very fine. Near 1 kHz, each\n         band spans roughly 2.3 Hz. The range [900, 1100] Hz contains ~87\n         bands, compared with 1 third-octave band. This resolution makes\n         millidecade bands suitable for high-resolution PSD aggregation across\n         long archives while retaining stable, integer-indexed band identities\n         — the property that MANTA exploits.\n\nArguments:\n\nlow_Hz::Real:  Lower bound in Hz. Must be > 0. Bands with center < low_Hz are excluded.\nhigh_Hz::Real: Upper bound in Hz. Bands with center > high_Hz are excluded. Pass fs/2 to clip at Nyquist.\n\nReturns:     Dict{Symbol, Tuple{Float64, Float64}}. Keys are :mdec_N where              N is the integer band index such that f_c = 10^(N/1000).              :mdec_3000 has center 10^3 = 1000 Hz; :mdec_4000 has center              10^4 = 10000 Hz. This MANTA convention is unambiguous — adjacent              bands never have the same center Hz when rounded to an integer.              Band edges are (10^((N−0.5)/1000), 10^((N+0.5)/1000)).\n\nConstraints:\n\nlow_Hz must be > 0 (required for log10). Throws ArgumentError otherwise.\nhigh_Hz must be > 0. Throws ArgumentError otherwise.\nReturns empty Dict if no band centers fall in [low_Hz, high_Hz].\nCaller is responsible for Nyquist clipping; pass fs/2 as high_Hz.\n\nFails when:\n\nlow_Hz ≤ 0  → ArgumentError\nhigh_Hz ≤ 0 → ArgumentError\n\nExample:\n\nbands = millidecade_bands(900.0, 1100.0)   # 87 bands\nbands[:mdec_3000]    # center = 10^3 = 1000 Hz; ≈ (999.9, 1000.1) Hz\nlength(bands)        # 87\n\nDo not use when:\n\nThird-octave resolution suffices — millidecade_bands produces ~14× more bands per decade than tol_bands and proportionally more results to store.\nComparing against tools that use ANSI preferred-center third-octave bands — millidecade band edges do not align with ANSI S1.11 preferred centers.\n\nReferences: Hatch, L.T., et al. (2016). Quantifying loss of acoustic communication space for right whales in and around a U.S. national marine sanctuary. Conservation Biology, 26(6), 983–994. Early use of millidecade bands.\n\nMiksis-Olds, J.L., et al. (2021). Ocean sound analysis software for making ambient noise trends accessible (MANTA). Frontiers in Marine Science, 8. MANTA millidecade band convention and index scheme.\n\n\n\n\n\n","title":"EcoAcoustics.millidecade_bands"},{"category":"method","location":"api/#EcoAcoustics.nsamples-Tuple{Audiodata}","page":"API Reference","text":"nsamples(a::Audiodata) -> Int\n\nPurpose:     Return the number of audio samples in the recording.\n\nArguments:\n\na::Audiodata: The recording.\n\nReturns:     Non-negative Int. Returns 0 for an empty recording — the              sentinel value returned by read_audio_range when a requested              window falls entirely in a gap.\n\nFails when:  Never.\n\nExample:\n\nnsamples(a)   # e.g. 2_880_000 for 60 s at 48 000 Hz\n\n\n\n\n\n","title":"EcoAcoustics.nsamples"},{"category":"method","location":"api/#EcoAcoustics.octave_bands-Tuple{Real, Real}","page":"API Reference","text":"octave_bands(low_Hz, high_Hz) -> Dict{Symbol, Tuple{Float64, Float64}}\n\nPurpose:     Return a Dict of ANSI S1.6 octave-band integration edges for every              band whose preferred center frequency falls in [low_Hz, high_Hz].              Suitable for passing directly to compute_spl or              compute_octave.\n\nArguments:\n\nlow_Hz::Real:  Lower bound in Hz. Bands with center < low_Hz are excluded.\nhigh_Hz::Real: Upper bound in Hz. Bands with center > high_Hz are excluded. Pass fs/2 (Nyquist) to clip at the recording's frequency limit.\n\nReturns:     Dict{Symbol, Tuple{Float64, Float64}}. Each key is a Symbol of the              form :oct_N where N is the ANSI preferred center frequency as a              bare integer (:oct_1000) or with a decimal underscore for              non-integer preferred values (:oct_31_5). Each value is              (low_Hz, high_Hz) computed as              (f_c / 2^(1/2), f_c × 2^(1/2)) — the ANSI S1.6 band edges.              Returns an empty Dict if no bands fall in range.\n\nConstraints:\n\nCenter frequencies are the ANSI S1.6 preferred values, not exact formula outputs. This matches PAMGuide, PAMGuard, MANTA, and Merchant 2015.\nBand edges are derived from the preferred center, not from adjacent centers: edges may slightly overlap or gap between adjacent bands, as is standard.\nThe caller is responsible for Nyquist clipping. Pass high_Hz = fs/2 to exclude bands above the recording's frequency limit. No Nyquist check is applied inside this function.\n\nFails when:  Never. Returns empty Dict for ranges with no matching bands.\n\nExample:\n\nbands = octave_bands(10.0, 10000.0)   # 10 bands: 16 Hz through 8000 Hz\n# keys: :oct_16, :oct_31_5, :oct_63, :oct_125, ..., :oct_8000\nbands[:oct_1000]   # (707.1..., 1414.2...)  = (1000/√2, 1000×√2)\n\nDo not use when:\n\nThird-octave resolution is required — use tol_bands or decidecade_bands.\nContinuous frequency coverage at high resolution is required — use millidecade_bands.\n\nReferences: ANSI S1.6-1984 (R2006). Preferred Frequencies, Frequency Levels, and Band Numbers for Acoustical Measurements. Acoustical Society of America.\n\n\n\n\n\n","title":"EcoAcoustics.octave_bands"},{"category":"method","location":"api/#EcoAcoustics.parse_filename-Tuple{AbstractString}","page":"API Reference","text":"parse_filename(path; recorder=\"unknown\", strict=false)\n\nParse basic metadata from an audio filename.\n\nThis extracts candidate metadata from the filename only.   It does not read audio data or perform calibration lookup.   The result is intended to be merged with recorder profiles, XML metadata, and/or user-provided overrides inside read_audio.\n\nArguments\n\npath::AbstractString:   Path to the audio file. Only the basename is inspected.\nrecorder::AbstractString=\"unknown\":   Recorder family (e.g. \"rockhopper\", \"ls1x\", \"sm3m\", \"snap\").   Used to select recorder-specific parsing rules via RecorderProfile.   If a profile defines a custom parser function, that function   handles the entire filename parse.\nstrict::Bool=false:   Controls how parsing failures are reported. This function never   throws because a field is missing. It always returns the same   NamedTuple shape. With strict=false (default), failures emit   warnings; with strict=true, warnings are more prominent but   validation still occurs later in read_audio after merging all   metadata sources.\n\nReturns\n\nA NamedTuple with fields:\n\ntimestamp::Union{DateTime,Nothing}:   Recording start time inferred from the filename (UTC if an explicit   \"Z\" suffix is present; otherwise timezone is unknown).     nothing if no timestamp could be parsed.\ntimezone::Union{String,Nothing}:   \"UTC\" for Rockhopper-style \"Z\" timestamps; otherwise nothing.\nlat::Union{Float64,Missing} / lon::Union{Float64,Missing}:   Recorder latitude/longitude if encoded in the filename.     missing otherwise.\nsite_id::Union{String,Nothing}:   Recorder or deployment site identifier, if present.\nrecorder_id::Union{String,Nothing}:   Device-specific recorder identifier, if present.\n\nNotes\n\nRecorder-specific parsing behavior is defined by RecorderProfile. If a profile provides a custom parser function (e.g. SNAP’s YYMMDD → 20YYMMDD rule), parse_filename delegates to that function.\nUnknown recorders do not fall back to generic parsing. Instead, all fields default to nothing/missing and a warning is issued.\nFilename patterns must be defined explicitly via a RecorderProfile. Users may either:   • add a new recorder profile, or   • provide missing metadata manually when calling read_audio (e.g. read_audio(path; starttime=..., lat=..., lon=...)).\nThis design prevents silent mis-parsing and keeps all filename rules explicit.\nThis function isolates filename parsing only. Required metadata (timestamp, timezone, location) is validated later in read_audio after merging filename metadata, recorder defaults, XML metadata, and user overrides.\n\n\n\n\n\n","title":"EcoAcoustics.parse_filename"},{"category":"method","location":"api/#EcoAcoustics.process_chunks-Tuple{AbstractAudioSource, Any}","page":"API Reference","text":"process_chunks(source, f; chunk_seconds, stride_seconds=chunk_seconds,\n               parallel=:threads, device=:cpu, gap_handling=:skip,\n               on_error=:fail, progress=true)\n\nPurpose:     Apply a function to every audio chunk drawn from source and              collect the results as a DataFrame. The primary entry point for              archive-scale soundscape metric computation. Analysis pipelines              written against SingleFileSource run unchanged on an              IndexedFileSource spanning a multi-terabyte archive.\n\nArguments:\n\nsource::AbstractAudioSource — the audio source to iterate.\nf::Function — applied to each Audiodata chunk. Must return a NamedTuple. Each field becomes a column in the output DataFrame. Do not return fields named start_time or coverage_fraction; those names are reserved and will be overwritten if present.\nchunk_seconds::Real — window duration in seconds (positive).\nstride_seconds::Real — step between window start times. Default equals chunk_seconds (non-overlapping).\nparallel::Symbol:\n:threads (default) — dispatches over Julia threads via Threads.@threads. Thread-safe because each read_audio_range call opens and closes its own file independently. f must be thread-safe; avoid shared mutable state in closures.\n:none — sequential processing. Useful for debugging and for f functions with internal mutable state.\n:distributed — throws ArgumentError (planned for v2).\ndevice::Symbol — :cpu (default, only implemented option in v1). :gpu and :auto throw ArgumentError (planned for v2).\ngap_handling::Symbol — :skip (default), :zero_fill, or :error. Same semantics as chunks.\non_error::Symbol:\n:fail (default) — rethrows any exception immediately. Recommended during development so bugs surface at once.\n:skip — drops the failed chunk; that row is absent from the output. Use for long production runs where isolated corrupt files should not abort the job.\n:record — adds a row with a string error column describing the exception. Metric columns are missing for that row.\nprogress::Bool — true (default) displays a ProgressMeter progress bar on stdout. Thread-safe: ProgressMeter.next! uses internal locking.\n\nReturns:     A DataFrame with columns start_time::DateTime and              coverage_fraction::Float64, plus whatever columns f returns.              Rows follow window order (pre-indexed slots preserve order even              under concurrent execution). Returns an empty DataFrame if no              windows are found or all chunks failed with on_error=:skip.\n\nConstraints: f must be thread-safe when parallel=:threads. chunk_seconds              and stride_seconds are rounded to millisecond precision.\n\nFails when:\n\nAny argument validation fails (same conditions as chunks).\non_error, parallel, or device is not one of the listed symbols.\nparallel=:distributed or device ∈ (:gpu, :auto) — throws ArgumentError; these are planned for v2 but not implemented in v1.\nAny chunk raises an exception and on_error=:fail.\n\nExample:\n\nsrc = IndexedFileSource(tbl, \"/data/deployment/\")\ndf  = process_chunks(src,\n                     chunk -> (rms = sqrt(sum(abs2, chunk.sig) / length(chunk.sig)),);\n                     chunk_seconds = 60.0,\n                     on_error      = :skip)\n# df columns: start_time, coverage_fraction, rms\n\nDo not use when: You need custom control flow between chunks (early exit,              stateful accumulators across chunks) — use chunks directly.\n\n\n\n\n\n","title":"EcoAcoustics.process_chunks"},{"category":"method","location":"api/#EcoAcoustics.psd_units-Tuple{PSDResult}","page":"API Reference","text":"psd_units(result::PSDResult) -> Symbol\n\nPurpose:     Return a Symbol describing the physical units of a PSDResult's              psd_linear field.\n\nArguments:\n\nresult::PSDResult: A computed PSD result.\n\nReturns:\n\n:µPa²_per_Hz when result.is_calibrated is true. This covers both the common case (calibration applied at the PSD layer via apply_calibration!) and the time-domain pre-calibration case (signal already in µPa before spectrogram, PSD wrapper set is_calibrated = true).\n:fullscale²_per_Hz when result.is_calibrated is false; power is relative to ADC full-scale squared per Hz, not a physical unit.\n\nConstraints: No-fail. Dispatches solely on the Bool field is_calibrated.\n\nExample:\n\nr = compute_psd(audio; window_seconds = 1.0)   # auto-calibrates\npsd_units(r)   # :µPa²_per_Hz\n\n\n\n\n\n","title":"EcoAcoustics.psd_units"},{"category":"method","location":"api/#EcoAcoustics.read_audio-Tuple{AbstractString}","page":"API Reference","text":"read_audio(path; starttime, recorder, lat, lon, site_id, strict)\n\nPurpose:     Read a mono audio file and return an Audiodata struct with signal,              sample rate, timestamps, and recorder metadata.\n\nArguments:\n\npath::AbstractString:   Path to the audio file. Supported formats: .wav, .flac.\nstarttime::Union{DateTime,Nothing} = nothing:   Override for recording start time. When provided, takes precedence over any   timestamp parsed from the filename — including DateTime(0) if you explicitly   want year zero. When nothing (default), the filename is parsed first; if that   also fails, a warning is always emitted and DateTime(0) is returned. When   strict=true, an ArgumentError is thrown instead of warning.\nrecorder::AbstractString = \"unknown\":   Recorder family name (e.g. \"rockhopper\", \"sm3m\", \"ls1x\", \"snap\").   Used to select filename parsing rules and calibration profile.\nlat::Union{Float64,Missing} = missing:   Recorder latitude in decimal degrees (positive = North). Overrides any value   parsed from the filename. Leave as missing when location is unknown.\nlon::Union{Float64,Missing} = missing:   Recorder longitude in decimal degrees (positive = East). Overrides any value   parsed from the filename. Leave as missing when location is unknown.\nsite_id::Union{String,Nothing} = nothing:   Deployment site identifier (e.g. \"T1-C\"). Override for filename-parsed value.\nstrict::Bool = false:   Controls behaviour when metadata is missing or unrecognised. Propagates to   lookup_calibration and parse_filename. strict=false (default,   exploration mode): missing fields emit @warn and fall back to sentinel   values — DateTime(0) for a missing timestamp, NoCalibration() for an   unrecognised recorder. strict=true (production mode): any missing field   that would otherwise warn instead throws an ArgumentError. Use   strict=true in batch pipelines where silent fallbacks would corrupt   results; use strict=false for interactive exploration. See DD-17.\n\nReturns:     Audiodata with signal in raw linear amplitude (uncalibrated), sample              rate as Float32, timestamps as DateTime (UTC where known), and              calibration stored but not applied.\n\nConstraints: Only mono audio is supported. Multi-channel files will throw an error.              Keyword arguments act as overrides: filename-parsed values are used when              a keyword argument is not supplied (i.e. still at its default).\n\nFails when:  File cannot be read or is corrupt; audio is not mono; signal is              empty after decoding; file extension is not .wav or .flac              (throws ArgumentError — convert AIF with sox input.aif output.flac).\n\nExample:\n\na = read_audio(\"T1-C__0__20170912_181500.wav\"; recorder=\"sm3m\")\na = read_audio(\"data.wav\"; starttime=DateTime(2021,3,19,16,34,0), recorder=\"ls1x\")\n\nDo not use when: Audio has multiple channels (stereo/hydrophone arrays) — mono is                  enforced.\n\n\n\n\n\n","title":"EcoAcoustics.read_audio"},{"category":"method","location":"api/#EcoAcoustics.read_audio_range-Tuple{AbstractAudioSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"read_audio_range(source, start, stop; gap_handling=:zero_fill) -> Audiodata\n\nPurpose:     Return an Audiodata covering exactly the time window              [start, stop], reading from source.\n\nArguments:\n\nsource::AbstractAudioSource: The audio source to read from.\nstart::DateTime: Start of the requested window (inclusive).\nstop::DateTime: End of the requested window (exclusive).\ngap_handling::Symbol = :zero_fill: How to handle regions where no audio is available. :zero_fill pads with zeros; :error raises if coverage is less than 1.0.\n\nReturns:     Audiodata whose signal covers [start, stop]. When the              requested window falls entirely outside the source, returns an              empty Audiodata (zero-length signal). Check              coverage_fraction(source, start, stop) to determine how much              of the returned signal is real audio vs. zero-fill.\n\nConstraints: start < stop. The returned Audiodata always has              is_calibrated = false; calibration is applied separately.\n\nFails when:\n\nCalled on a subtype that has not implemented this method.\ngap_handling = :error and coverage is less than 1.0.\n\nExample:\n\nusing Dates\nchunk = read_audio_range(source, DateTime(2023,10,12,0,0,0),\n                                 DateTime(2023,10,12,0,1,0))\n\nDo not use when: You need calibrated audio — call apply_calibration! on                  the returned Audiodata before metric computation.\n\n\n\n\n\n","title":"EcoAcoustics.read_audio_range"},{"category":"method","location":"api/#EcoAcoustics.read_audio_range-Tuple{IndexedFileSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"read_audio_range(source::IndexedFileSource, start, stop; gap_handling=:zero_fill)\n\nPurpose:     Return an Audiodata covering exactly [start, stop], reading              from whichever files in the index overlap the requested window.              Files are read from disk only as needed; files outside the window              are never touched.\n\nArguments:\n\nsource::IndexedFileSource: The source to read from.\nstart::DateTime: Start of the requested window (inclusive).\nstop::DateTime: End of the requested window (exclusive).\ngap_handling::Symbol = :zero_fill: How to handle gaps between files:\n:zero_fill — gap regions are filled with zeros in the output signal. coverage_fraction on the returned Audiodata reflects the fill.\n:error — raise if coverage is less than 1.0.\n\nReturns:     Audiodata whose signal covers exactly [start, stop], with              starttime = start. When the window falls entirely in a gap,              returns an empty Audiodata (zero-length signal). Metadata              (recorder, site_id, calibration, etc.) comes from the first              file that contributes samples to the output — if the window spans              files from different deployments, the first file's metadata is used              for the whole result. This simplification is noted in Constraints.\n\nConstraints: start < stop is required. Only :zero_fill and :error are              valid for gap_handling — :skip raises with an explanation (use              chunks() for gap-skipping iteration). All overlapping files must              have the same sample rate. The returned Audiodata always has              is_calibrated = false. Metadata is from the first contributing              file — if the window spans files with different recorders or              site_ids, the first file's metadata applies to the whole result.\n\nFails when:\n\nstart >= stop.\ngap_handling is not :zero_fill or :error.\nOverlapping files have different sample rates.\ngap_handling = :error and coverage is less than 1.0.\n\nExample:\n\nusing Dates\nchunk = read_audio_range(src,\n                         DateTime(2023,1,1,0,0,0),\n                         DateTime(2023,1,1,0,1,0))\n\nDo not use when: You need calibrated audio — call apply_calibration! on the                  returned Audiodata before metric computation.\n\n\n\n\n\n","title":"EcoAcoustics.read_audio_range"},{"category":"method","location":"api/#EcoAcoustics.read_audio_range-Tuple{SingleFileSource, Dates.DateTime, Dates.DateTime}","page":"API Reference","text":"read_audio_range(source::SingleFileSource, start, stop; gap_handling=:zero_fill)\n\nPurpose:     Return an Audiodata whose signal covers exactly [start, stop],              sliced from the cached file signal and zero-padded where the              requested window extends beyond the file.\n\nArguments:\n\nsource::SingleFileSource: The source to read from.\nstart::DateTime: Start of the requested window (inclusive).\nstop::DateTime: End of the requested window (exclusive).\ngap_handling::Symbol = :zero_fill: Behaviour when the window extends beyond the file:\n:zero_fill — pad out-of-file regions with zeros.\n:error — throw if coverage_fraction < 1.0.\n\nReturns:     Audiodata with starttime = start. Signal length is              leading_zeros + real_samples + trailing_zeros, where each              piece is computed independently from its own time span (see              Constraints). Returns an empty Audiodata (zero-length signal)              when the window has no overlap with the file at all.\n\nConstraints: start < stop is required.\n\n         **DateTime precision and sample counts:** `DateTime` has\n         millisecond resolution. The file's true duration in samples\n         rarely divides to an exact millisecond count, so converting\n         the total window duration to samples and back introduces a\n         rounding error of up to 1 sample. To avoid this, boundaries\n         that coincide with the file edge use the exact sample count\n         directly rather than deriving it from time arithmetic. Only\n         gap regions (zero-fill outside the file) go through\n         millisecond rounding, where a ±1 sample error is\n         inconsequential.\n\n         The returned `Audiodata` always has `is_calibrated = false`.\n         Calibration and metadata are copied from the source file.\n\nFails when:\n\nstart >= stop.\ngap_handling = :error and coverage_fraction < 1.0.\ngap_handling is not :zero_fill or :error.\n\nExample:\n\nusing Dates\nchunk = read_audio_range(src,\n                         DateTime(2017,9,12,18,15,0),\n                         DateTime(2017,9,12,18,16,0))\n\nDo not use when: You need calibrated audio — call apply_calibration! on                  the returned Audiodata before metric computation.\n\n\n\n\n\n","title":"EcoAcoustics.read_audio_range"},{"category":"method","location":"api/#EcoAcoustics.spectrogram-Tuple{AbstractVector{Float64}}","page":"API Reference","text":"spectrogram(signal; fs, window_seconds, overlap_fraction=0.5,\n            window=:hann, nfft=nothing, fft_plan=nothing)\n            -> SpectrogramResult\n\nPurpose:     Compute the complex Short-Time Fourier Transform (STFT) of a mono              Float64 signal. Returns a SpectrogramResult containing the              complex single-sided spectrum for every analysis frame, the time and              frequency axes, and the window metadata required for downstream PSD              computation. This is the lowest-level time-frequency primitive in              EcoAcoustics.jl; PSD, SPL, LTSA, and soundscape indices all consume              its output.\n\nArguments:\n\nsignal::AbstractVector{Float64}: Input signal in arbitrary linear units (calibrated µPa or raw ADC — this function is unit-agnostic). Must be Float64.\nfs::Real: Sampling rate in Hz. Strictly positive.\nwindow_seconds::Real: Analysis window duration in seconds. window_length = round(Int, window_seconds * fs) samples. Must yield window_length ≥ 2.\noverlap_fraction::Real = 0.5: Fraction of the window that overlaps between consecutive frames. Must be in [0, 1). 0.0 = no overlap (frames are adjacent, no sample is used twice); 0.5 = 50% overlap (PAMGuide default). 1.0 is excluded — it would produce infinite frames.\nwindow::Symbol = :hann: Tapering window applied to each frame before the FFT. One of :hann, :hamming, :blackman, :rectangular. Applied via _make_window.\nnfft::Union{Int,Nothing} = nothing: FFT length. Must be ≥ window_length. When nothing (default), equals window_length (no zero-padding). Set larger for finer frequency resolution without changing the analysis window duration — e.g. nfft = 2 * window_length halves the bin spacing.\nfft_plan: Pre-computed FFTW plan for arrays of length nfft. When nothing (default), a plan is built internally. For batch use via process_chunks, pre-build once with make_spectrogram_plan and pass here to avoid FFTW's plan-selection overhead on every chunk call.\n\nReturns:     SpectrogramResult. stft is complex, single-sided, size              (nfft ÷ 2 + 1) × num_frames. No magnitude correction or              calibration has been applied.\n\nConstraints:\n\nsignal must be Float64. All arithmetic in EcoAcoustics.jl uses Float64 for numerical precision; Float32 input is rejected with an informative error.\nPartial trailing frames are dropped: a frame is only computed if the full window_length samples are available. This is the Merchant 2015 convention. num_frames = div(length(signal) - window_length, hop) + 1.\nWhen nfft > window_length, the zero-pad region is appended after the windowed frame inside the STFT loop. Padding is transparent to the caller.\nCalibration is not applied here. For physically meaningful dB values, pass a calibrated signal (after apply_calibration!) and apply calibration at the PSD layer.\n\nFails when:\n\nsignal is empty.\nlength(signal) < window_length.\nwindow_seconds * fs < 2 (window too short for a meaningful FFT).\nnfft < window_length.\noverlap_fraction is not in [0, 1).\nfft_plan was built for a different length than nfft (checked upfront via the plan's internal .sz field).\nwindow is not one of the four recognised symbols (thrown by _make_window).\n\nExample:\n\n# 1-second signal at 48 kHz, 0.1 s window, 50% overlap, Hann window\nsignal = randn(Float64, 48000)\nresult = spectrogram(signal; fs = 48000.0, window_seconds = 0.1)\n\n# result.stft size: (2401, 19) — 2401 frequency bins, 19 frames\n# result.freqs[end]    == 24000.0   (Nyquist = fs/2)\n# result.time[1]       ≈ 0.05       (center of first frame: 4800/2 / 48000)\n# result.window_energy ≈ 1800.0     (Hann window energy ≈ 3N/8 for N=4800)\n\nDo not use when:\n\nThe signal has not been calibrated and physically meaningful SPL is required. Apply apply_calibration! first.\nThe signal is multi-channel. Multi-channel audio is not supported in v1; pass one channel at a time.\n\nReferences: Merchant et al. (2015) Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. Window energy convention and frame-center definition.\n\n\n\n\n\n","title":"EcoAcoustics.spectrogram"},{"category":"method","location":"api/#EcoAcoustics.time_range-Tuple{AbstractAudioSource}","page":"API Reference","text":"time_range(source::AbstractAudioSource) -> (DateTime, DateTime)\n\nPurpose:     Return the start and end time of the audio source as a 2-tuple.\n\nArguments:\n\nsource::AbstractAudioSource: Any concrete audio source.\n\nReturns:     (starttime::DateTime, endtime::DateTime) covering all audio              available from this source.\n\nConstraints: starttime ≤ endtime. An empty source may return equal times.\n\nFails when:  Called on a subtype that has not implemented this method.\n\nExample:\n\nt_start, t_stop = time_range(source)\n\n\n\n\n\n","title":"EcoAcoustics.time_range"},{"category":"method","location":"api/#EcoAcoustics.time_range-Tuple{IndexedFileSource}","page":"API Reference","text":"time_range(source::IndexedFileSource) -> (DateTime, DateTime)\n\nPurpose:     Return the earliest start time and latest end time across all              files in the index.\n\nArguments:\n\nsource::IndexedFileSource: The source to query.\n\nReturns:     (first_start, last_end) as a 2-tuple of DateTime. This spans              the entire deployment, including any gaps between files.\n\nConstraints: The index must contain at least one row (enforced at construction).\n\nFails when:  Never — both values are scanned from the index at call time.\n\nExample:\n\nt_start, t_stop = time_range(src)\n\n\n\n\n\n","title":"EcoAcoustics.time_range"},{"category":"method","location":"api/#EcoAcoustics.time_range-Tuple{SingleFileSource}","page":"API Reference","text":"time_range(source::SingleFileSource) -> (DateTime, DateTime)\n\nPurpose:     Return the start and end time of the audio file.\n\nArguments:\n\nsource::SingleFileSource: The source to query.\n\nReturns:     (starttime, endtime) as a 2-tuple of DateTime. endtime is              derived from starttime, sample count, and sampling rate —              it is not read from the file header.\n\nConstraints: endtime is computed via nanosecond arithmetic but stored in a              DateTime, which has millisecond resolution. The sub-millisecond              remainder is truncated. See read_audio_range for how this is              handled correctly when slicing.\n\nFails when:  Never — the values are cached at construction time.\n\nExample:\n\nt_start, t_stop = time_range(src)\n\n\n\n\n\n","title":"EcoAcoustics.time_range"},{"category":"method","location":"api/#EcoAcoustics.to_dB-Tuple{LTSAResult}","page":"API Reference","text":"to_dB(result::LTSAResult) -> Matrix{Float64}\n\nPurpose:     Convert the linear LTSA matrix to decibels: 10 × log10(matrix).              Returns a new matrix the same shape as result.matrix; the result              is not modified.\n\n         Units after conversion:\n         - `ltsa_units(result) == :µPa²_per_Hz` → dB re 1 µPa²/Hz\n         - `ltsa_units(result) == :fullscale²_per_Hz` → dBFS/Hz\n\nReturns:     Matrix{Float64}, shape (n_freqs, n_columns), all values in dB.\n\nConstraints: Matrix values must be strictly positive; log10(0) = -Inf and              log10(negative) = NaN are left as-is (IEEE 754), consistent with              to_dB for PSDResult.\n\nFails when:  Never.\n\nExample:\n\nlt    = compute_ltsa(audio; average_span_seconds = 60.0)\nlt_dB = to_dB(lt)          # Matrix{Float64}, dB re 1 µPa²/Hz if calibrated\n\n\n\n\n\n","title":"EcoAcoustics.to_dB"},{"category":"method","location":"api/#EcoAcoustics.to_dB-Tuple{PSDResult}","page":"API Reference","text":"to_dB(result::PSDResult) -> Matrix{Float64}\nto_dB(v::AbstractVector{Float64}) -> Vector{Float64}\n\nPurpose:     Convert linear PSD values to decibels: 10 × log10(x).              Works on a full PSDResult (returns the same shape as              result.psd_linear) or on a Vector{Float64} such as the              output of average_psd.\n\n         Units after conversion:\n         - `psd_units(result) == :µPa²_per_Hz` → output in dB re 1 µPa²/Hz\n         - `psd_units(result) == :fullscale²_per_Hz` → output in dBFS/Hz\n\nArguments:\n\nresult::PSDResult: Source PSD to convert.\nv::AbstractVector{Float64}: Averaged PSD vector (from average_psd).\n\nReturns:     Matrix{Float64} (same size as psd_linear) or Vector{Float64}              (same length as v). All values in decibels.\n\nConstraints:\n\nInput values must be strictly positive (> 0). log10(0) returns -Inf; negative input returns NaN. Both are left as-is — no clamping or warning.\nDo not call on dB values; to_dB(to_dB(x)) ≠ x.\n\nFails when:  Never (no assertions; behaviour on non-positive input is defined              by IEEE 754 log10 semantics).\n\nExample:\n\nresult  = compute_psd(audio; window_seconds = 1.0)\ndb_mat  = to_dB(result)               # Matrix{Float64}\ndb_mean = to_dB(average_psd(result))  # Vector{Float64}\n\n\n\n\n\n","title":"EcoAcoustics.to_dB"},{"category":"method","location":"api/#EcoAcoustics.tol_bands-Tuple{Real, Real}","page":"API Reference","text":"tol_bands(low_Hz, high_Hz) -> Dict{Symbol, Tuple{Float64, Float64}}\n\nPurpose:     Return a Dict of ANSI S1.11 third-octave (decidecade) band              integration edges for every band whose preferred center frequency              falls in [low_Hz, high_Hz]. Third-octave bands are the standard              choice for soundscape reporting, biological call-level measurements,              and most PAMGuide-family analyses.\n\nArguments:\n\nlow_Hz::Real:  Lower bound in Hz. Bands with center < low_Hz are excluded.\nhigh_Hz::Real: Upper bound in Hz. Bands with center > high_Hz are excluded. Pass fs/2 to clip at Nyquist.\n\nReturns:     Dict{Symbol, Tuple{Float64, Float64}}. Keys are :tol_N Symbols              using the ANSI preferred (nominal) center frequency for readability              and axis compatibility. Non-integer preferred values use an              underscore decimal separator: :tol_12_5, :tol_31_5. Band edges              are the base-10 decidecade edges (f_c / 10^(1/20), f_c ×              10^(1/20)), where f_c = 10^(n/10) is the exact decidecade center              for band index n (so :tol_63 has center 63.096 Hz, not 63.0).\n\nConstraints:\n\nLabels use the ANSI S1.11-2004 preferred (nominal) centers; edges use the exact base-10 decidecade centers f_c = 10^(n/10) (ISO 18405 / ADEON / MANTA definition). Band levels are computed by frequency-domain spectral integration of the PSD — the same method family ADEON and MANTA use (verified against Martin et al. 2021 and the ADEON DPS; see DD-30), and validated against PAMGuide's PSD integrated into the same bands to ~0.001 dB. Two differences to be aware of (DD-30): (1) PAMGuide's PG_TOL uses a time-domain filter bank, which diverges from spectral integration by up to ~1.7 dB at low frequency; (2) ADEON splits straddling edge bins fractionally, whereas EA uses hard bin edges (DD-25) — a smaller low-frequency difference from exact ADEON output.\ntol_bands and decidecade_bands are identical. The term \"decidecade\" (ISO 18405:2017) superseded \"third-octave\" in the underwater acoustics literature around 2018; older literature (including Merchant 2015) uses \"third-octave\". Both refer to 1/10-decade frequency bands.\nCaller is responsible for Nyquist clipping.\n\nFails when:  Never. Returns empty Dict for ranges with no matching bands.\n\nExample:\n\nbands = tol_bands(10.0, 1000.0)\n# 17 bands: :tol_10, :tol_12_5, :tol_16, ..., :tol_1000\nbands[:tol_1000]   # (891.25..., 1122.02...)  = (1000/10^(1/20), 1000×10^(1/20))\n\nDo not use when:\n\nOctave-band resolution is sufficient — use octave_bands.\nHigh-resolution (sub-1-Hz bandwidth) SPL is needed — use millidecade_bands.\n\nReferences: ANSI S1.11-2004 (R2009). Specification for Octave-Band and Fractional-Octave-Band Analog and Digital Filters. Acoustical Society of America. ISO 18405:2017. Underwater Acoustics — Terminology. International Organization for Standardization.\n\n\n\n\n\n","title":"EcoAcoustics.tol_bands"},{"category":"section","location":"explanations/psd/#Power-Spectral-Density","page":"PSD","text":"The power spectral density (PSD) describes how acoustic power is distributed across frequency. It is the primary output of the EcoAcoustics.jl soundscape pipeline and the foundation for SPL, TOL, LTSA, and soundscape indices. Every metric above PSD in the pipeline is a transformation of a PSDResult.\n\n","title":"Power Spectral Density"},{"category":"section","location":"explanations/psd/#Position-in-the-pipeline","page":"PSD","text":"spectrogram → complex STFT matrix  [SpectrogramResult]\n      │\n      ▼\ncompute_psd → power per Hz bin  [PSDResult, µPa²/Hz after calibration]\n      │\n      ├──→ SPL / TOL       (task 10)  →  broadband or third-octave level  [dB re 1 µPa]\n      │\n      └──→ LTSA            (task 11)  →  long-term spectral average  [dB re 1 µPa²/Hz]\n                │\n                └──→ Soundscape indices  (task 12)  →  ACI, entropy\n\nspectrogram is unit-agnostic — it produces complex amplitudes in full-scale units and applies no calibration. All physical unit conversion happens at the PSD layer (DD-08).\n\n","title":"Position in the pipeline"},{"category":"section","location":"explanations/psd/#Normalization:-Merchant-et-al.-(2015)-Equation-1","page":"PSD","text":"EcoAcoustics.jl uses the normalization defined in Merchant et al. (2015) Measuring Acoustic Habitats, Methods in Ecology and Evolution, 6, 257–265. This is also the convention used by PAMGuide, MANTA, and Triton.\n\nFor each frequency bin k and time frame j, the PSD before calibration is:\n\npsd[k, j] = correction_k × |STFT[k, j]|² / (fs × W)\n\nwhere:\n\n|STFT[k, j]|² is the squared magnitude of the complex STFT at bin k, frame j (abs2 in Julia).\nfs is the sample rate in Hz.\nW = Σwᵢ² is the window energy — the sum of squared window coefficients (SpectrogramResult.window_energy). For a Hann window of length N, W ≈ 3N/8.\ncorrection_k is the single-sided correction factor (see below).\n\nThe result is in units of full-scale² per Hz before calibration, and µPa² per Hz after calibration.\n\n","title":"Normalization: Merchant et al. (2015) Equation 1"},{"category":"section","location":"explanations/psd/#Single-sided-correction-(DD-10)","page":"PSD","text":"spectrogram uses Julia's rfft, which produces only the non-negative frequency bins of the DFT. For a real-valued signal of length N with even nfft:\n\nBin 0 (DC, frequency 0 Hz) and bin N/2 (Nyquist) are real-valued in the two-sided DFT — they have no negative-frequency counterpart. Their power is complete as-is.\nEvery interior bin k = 1, …, N/2 − 1 represents two DFT bins of equal magnitude (the positive and negative frequency pair). The rfft discards the negative-frequency half, so the power must be doubled to account for the missing contribution.\n\nIn code:\n\npsd_linear[2:end-1, :] .*= 2.0   # interior bins ×2\n# DC (row 1) and Nyquist (last row) are not doubled\n\nThis requires nfft to be even. spectrogram enforces this with an @assert iseven(nfft) (DD-07).\n\n","title":"Single-sided correction (DD-10)"},{"category":"section","location":"explanations/psd/#The-even-nfft-constraint-(DD-07)","page":"PSD","text":"The single-sided correction treats psd_linear[end, :] as the Nyquist bin. This is only correct when nfft is even: for even nfft, rfft of length N produces N/2+1 bins (DC + N/2−1 interior + Nyquist). For odd nfft, the last bin is NOT Nyquist and would be incorrectly excluded from the ×2 correction.\n\nspectrogram rejects odd nfft at the source:\n\n@assert iseven(nfft_actual) \"spectrogram: nfft must be even (DD-07)...\"\n\nIf your window length is odd, pass an explicit even nfft (e.g. nfft = window_length + 1).\n\n","title":"The even-nfft constraint (DD-07)"},{"category":"section","location":"explanations/psd/#Calibration-at-the-PSD-layer","page":"PSD","text":"The spectrogram is calibration-agnostic. Calibration is applied at the PSD layer because that is where we know the physical quantity (power per Hz). This preserves the complex STFT for future click/whistle detectors, which need phase information but not physical calibration.\n\ncompute_psd dispatches on the calibration type:\n\nCalibration type Effect on psd_linear\nNoCalibration No change; output in full-scale²/Hz\nScalarCalibration(S) Uniform multiply by 10^(-S/10) → µPa²/Hz\nTFCalibration Per-bin divide by interpolated tf_lin[k] → µPa²/Hz\n\npsd_units(result) returns :µPa²_per_Hz when result.is_calibrated is true, and :fullscale²_per_Hz otherwise (DD-15).\n\n","title":"Calibration at the PSD layer"},{"category":"section","location":"explanations/psd/#Averaging-and-the-dB-step","page":"PSD","text":"Always average in linear power, not in dB. The Merchant 2015 LTSA convention averages spectral estimates in linear power (µPa²/Hz), then converts to dB for display or band integration.\n\nresult = compute_psd(audio; window_seconds = 1.0)\navg    = average_psd(result)       # Vector{Float64}, µPa²/Hz\ndb     = to_dB(avg)                # dB re 1 µPa²/Hz\n\nAveraging in dB is mathematically equivalent only when the PSD has no temporal variation. In practice, bioacoustic data always has variation, and dB averaging underestimates power when the dynamic range is high — see DD-16.\n\nto_dB is a thin wrapper: 10 .* log10.(x). It works on a PSDResult (returns a Matrix{Float64}) or on a Vector{Float64} from average_psd.\n\n","title":"Averaging and the dB step"},{"category":"section","location":"explanations/psd/#Pre-calibrated-signals","page":"PSD","text":"A signal can be calibrated in the time domain before spectrogram computation:\n\naudio_cal = apply_calibration(audio)    # audio_cal.is_calibrated == true\nresult    = compute_psd(audio_cal; window_seconds = 1.0)\n\nIn this case, compute_psd detects audio_cal.is_calibrated == true and applies NoCalibration() at the PSD layer (DD-14). result.is_calibrated will be true — the wrapper propagates audio.is_calibrated into the PSDResult even when PSD-layer calibration is NoCalibration. This means psd_units correctly returns :µPa²_per_Hz for pre-calibrated signals.\n\n","title":"Pre-calibrated signals"},{"category":"section","location":"explanations/psd/#Chunked-processing-at-archive-scale","page":"PSD","text":"For archive-scale recordings, use process_chunks with a compute_psd callback rather than loading the full time range into memory:\n\nsrc    = IndexedFileSource(load_index(\"archive.arrow\"), \"archive/\")\nt0, t1 = time_range(src)\n\nresults = process_chunks(src, chunk -> compute_psd(chunk; window_seconds = 60.0);\n                         chunk_seconds = 3600,    # 1-hour PSD per chunk\n                         gap_handling  = :skip)\n\n# results is a DataFrame with one row per chunk.\n# Each row's PSDResult is in results.result; convert to dB for analysis.\n\nEach chunk is a 1-hour Audiodata; compute_psd computes the PSD for that chunk independently. The calibration is resolved per-chunk from the chunk's metadata. This pattern is constant in memory regardless of archive size.\n\nFor a continuous time-averaged PSD over the full archive, average the per-chunk results in linear power:\n\nall_avg = [average_psd(r) for r in results.result]\ngrand_avg = mean(stack(all_avg, dims=2); dims=2) |> vec\ndb_grand_avg = to_dB(grand_avg)\n\n","title":"Chunked processing at archive scale"},{"category":"section","location":"explanations/psd/#References","page":"PSD","text":"Merchant, N.D., et al. (2015). Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. DOI:10.1111/2041-210X.12330\n\nDesign decisions: DD-07 through DD-16 in docs/design_decisions.md.","title":"References"},{"category":"section","location":"explanations/spl_bands/#SPL-Band-Schemes","page":"SPL bands","text":"EcoAcoustics.jl provides four standard frequency band schemes for SPL computation. Each scheme is a Dict{Symbol, Tuple{Float64, Float64}} mapping a band label to (low_Hz, high_Hz) integration edges. The same Dict format is accepted by compute_spl for custom band sets, so all four are interchangeable at the API level.\n\n","title":"SPL Band Schemes"},{"category":"section","location":"explanations/spl_bands/#Overview:-which-scheme-to-use","page":"SPL bands","text":"Scheme Function Bands in [10 Hz, 24 kHz] Use case\nOctave octave_bands ~10 bands Coarse overview; regulatory reporting; comparison with older literature\nThird-octave tol_bands ~30 bands Standard soundscape reporting; PAMGuide / Triton output; Merchant 2015\nDecidecade decidecade_bands identical to above Same as third-octave; preferred term in post-2018 underwater acoustics\nMillidecade millidecade_bands ~3400 bands High-resolution PSD aggregation; MANTA / NOAA NRS output\n\nWhen in doubt, use third-octave / decidecade. It is the most common output format in published soundscape literature and the default unit for comparing recordings across sites and instruments.\n\n","title":"Overview: which scheme to use"},{"category":"section","location":"explanations/spl_bands/#ANSI-preferred-center-frequencies-(DD-20)","page":"SPL bands","text":"Do not replace the tabulated preferred values with exact formula values. This is the most important constraint in this file.\n\nBoth octave and third-octave band sets use the ANSI tabulated preferred center frequencies — not the values computed from the exact band-number formula. The formulas are:\n\noctave centers:        1000 × 2^(n − 10)     for n = 0, 1, 2, …\nthird-octave centers:  1000 × 2^((n − 30)/3) for n = 0, 1, 2, …\n\nThe ANSI preferred values round these exact results to cleaner numbers. For example:\n\nExact formula: 1000 × 2^(-5) = 31.25 Hz → preferred: 31.5 Hz\nExact formula: 1000 × 2^(-4) = 62.5 Hz → preferred: 63 Hz\nExact formula: 1000 × 2^(-5/3) ≈ 3.155 Hz → preferred: 3.15 Hz\n\nPAMGuide, PAMGuard, MANTA, and Merchant (2015) all use the preferred values. If EcoAcoustics.jl used exact formula values instead, frequency-axis labels would not match reference tools, and cross-tool comparisons would require a manual mapping step.\n\nThe preferred values are stored in the module-level constants OCTAVE_PREFERRED_HZ and TOL_PREFERRED_HZ in src/soundscape/bands.jl.\n\n","title":"ANSI preferred center frequencies (DD-20)"},{"category":"section","location":"explanations/spl_bands/#Band-edges","page":"SPL bands","text":"Integration edges are derived from the band center, not from adjacent centers. Octave bands use the base-2 convention; third-octave / decidecade bands use the base-10 convention with exact decidecade centers f_c = 10^(n/10) (DD-30):\n\noctave band:       f_lo = f_c / 2^(1/2),    f_hi = f_c × 2^(1/2)    (±√2, base-2)\ndecidecade band:   f_lo = f_c / 10^(1/20),  f_hi = f_c × 10^(1/20)  (≈ ±1.1220, base-10)\n\nFor decidecade bands the label uses the ANSI preferred (nominal) center for readability (:tol_63), while the edges use the exact base-10 center (63.096 Hz) — the ISO 18405 / ADEON / MANTA decidecade definition. Band levels are computed by frequency-domain spectral integration of the PSD. This is the same method family ADEON and MANTA use — verified against the primary sources (Martin et al. 2021 hybrid millidecade; ADEON Data Processing Specification), which compute decidecades by summing / integrating the PSD in the frequency domain, not with a filter bank (DD-30). EA matches PAMGuide's PSD integrated into the same bands to ~0.001 dB.\n\nTwo differences are worth knowing (both in DD-30):\n\nPAMGuide's dedicated PG_TOL third-octave output uses the older filter-bank method (time-domain IIR octave filters), which differs from spectral integration by up to ~1.7 dB at low frequencies (63–315 Hz) where the filter skirts span a large fraction of the band.\nADEON splits edge bins that straddle two bands fractionally by percent overlap; EA uses hard bin edges (a bin is fully in or out — DD-25). So EA matches the ADEON approach but is not bit-identical at band edges; the difference is small and concentrated at low frequency.\n\nNeither is an error — they are method / edge-handling choices.\n\nBecause edges are computed from centers rather than from adjacent centers, adjacent bands may slightly overlap or gap. This is standard practice and is present in PAMGuide and MANTA output. It is not a bug.\n\n","title":"Band edges"},{"category":"section","location":"explanations/spl_bands/#Decidecade-vs.-third-octave-(DD-20)","page":"SPL bands","text":"decidecade_bands is a pure alias for tol_bands. The two terms refer to the same band scheme:\n\nThird-octave (ANSI S1.11): traditional term used in air acoustics and in Merchant 2015 and all PAMGuide-family literature.\nDecidecade (ISO 18405:2017): a 1/10-decade frequency band. This term entered the underwater acoustics literature around 2018 and is now preferred in that community. EA implements the base-10 decidecade (DD-30), so tol_bands and decidecade_bands are the same base-10 band — the near-match log10(2^(1/3)) ≈ 0.1003 is not exact and the ~0.08% edge difference is material at low frequencies (see DD-30).\n\nBoth functions produce the same output. Choose whichever term matches your literature. compute_decidecade delegates directly to compute_tol with no overhead.\n\n","title":"Decidecade vs. third-octave (DD-20)"},{"category":"section","location":"explanations/spl_bands/#Symbol-naming-for-non-integer-centers","page":"SPL bands","text":"Band label Symbols cannot contain dots. Non-integer preferred centers use an underscore decimal separator:\n\nPreferred center Symbol\n12.5 Hz :tol_12_5\n31.5 Hz :oct_31_5\n3.15 Hz :tol_3_15\n6.3 Hz :tol_6_3\n1000 Hz :tol_1000\n\nThe _band_label helper in bands.jl uses Julia's shortest-roundtrip string() representation (e.g., 12.5 prints as \"12.5\", not \"12.50\"), then replaces \".\" with \"_\". The key concern is round-trippability: the string must reconstruct the original float without ambiguity.\n\n","title":"Symbol naming for non-integer centers"},{"category":"section","location":"explanations/spl_bands/#Millidecade-bands:-resolution-and-index-convention","page":"SPL bands","text":"A millidecade is 1/1000 of a decade on a log-frequency axis. Each band spans a frequency ratio of 10^(1/1000) ≈ 1.00230 — about 0.23%.","title":"Millidecade bands: resolution and index convention"},{"category":"section","location":"explanations/spl_bands/#Resolution-is-very-fine","page":"SPL bands","text":"Near 1 kHz, each millidecade band spans approximately 2.3 Hz. The 900–1100 Hz range contains 87 bands compared with one third-octave band covering the same range. Over a [10 Hz, 24 kHz] range — a typical 48 kHz recording — there are roughly 3,400 millidecade bands. This is the design intent: MANTA and the NOAA NRS use millidecade bands specifically because the fine resolution allows high-resolution PSD archives to be stored in a compact, stable, integer-indexed format.\n\nBefore using compute_millidecade, consider:\n\nStorage: 3,400 BandSPL entries per time step, each with a 5-element vector of aggregate statistics, adds up quickly for long archives.\nFrequency resolution requirement: a millidecade band near 10 Hz spans only ~0.023 Hz. A PSD with df = 1 Hz (1-second window at 1 kHz) is far too coarse — many bands will contain zero bins and compute_spl will throw. For millidecade bands at 10 Hz you need df < 0.02 Hz, which requires a window of at least 50 seconds. In practice, millidecade analyses use long windows (typically 60 s) or restrict the low-frequency cutoff to where the frequency resolution is adequate.","title":"Resolution is very fine"},{"category":"section","location":"explanations/spl_bands/#MANTA-index-convention","page":"SPL bands","text":"Millidecade bands use an integer index n such that the center frequency is:\n\nf_c(n) = 10^(n / 1000)\n\nBand labels are :mdec_N where N is this integer:\n\n:mdec_3000 → f_c = 10^3 = 1000 Hz\n:mdec_4000 → f_c = 10^4 = 10000 Hz\n:mdec_2000 → f_c = 10^2 = 100 Hz\n\nThis MANTA convention is unambiguous: no two adjacent bands round to the same center Hz when expressed as an integer. Using center Hz in the symbol (:mdec_1000) would alias once Hz-rounded centers of adjacent bands coincide.\n\nBand edges:\n\nf_lo(n) = 10^((n − 0.5) / 1000)\nf_hi(n) = 10^((n + 0.5) / 1000)\n\n","title":"MANTA index convention"},{"category":"section","location":"explanations/spl_bands/#Nyquist-clipping","page":"SPL bands","text":"Band generators clip bands by center frequency, not by band edge. Passing high_Hz = fs/2 (Nyquist) to a generator may include a band centered exactly at Nyquist, whose upper edge will exceed Nyquist. compute_spl will reject that band with ArgumentError.\n\nWhen using the convenience wrappers (compute_tol, compute_octave, etc.), choose high_Hz such that the uppermost included center's upper edge stays within Nyquist. For third-octave (decidecade) bands, the safe upper limit is Nyquist / 10^(1/20) ≈ 0.891 × Nyquist. For a 48 kHz recording (Nyquist = 24 kHz), this is ~21.4 kHz — so the 20 kHz band is the topmost safe third-octave band.\n\nThe wrappers default to high_Hz = Float64(psd.fs) / 2.0, which is correct when Nyquist does not coincide with an ANSI preferred center. For the common case (fs = 48, 44.1, 96, 192 kHz), no ANSI preferred center sits exactly at Nyquist and the default works without adjustment.\n\n","title":"Nyquist clipping"},{"category":"section","location":"explanations/spl_bands/#References","page":"SPL bands","text":"ANSI S1.6-1984 (R2006). Preferred Frequencies, Frequency Levels, and Band Numbers for Acoustical Measurements. Acoustical Society of America.\n\nANSI S1.11-2004 (R2009). Specification for Octave-Band and Fractional-Octave-Band Analog and Digital Filters. Acoustical Society of America.\n\nISO 18405:2017. Underwater Acoustics — Terminology. International Organization for Standardization.\n\nMiksis-Olds, J.L., et al. (2021). Ocean sound analysis software for making ambient noise trends accessible (MANTA). Frontiers in Marine Science, 8. DOI:10.3389/fmars.2021.703650\n\nDesign decisions: DD-20 in docs/design_decisions.md.","title":"References"},{"category":"section","location":"explanations/calibration/#Calibration","page":"Calibration","text":"This page explains the calibration system in EcoAcoustics.jl: what system sensitivity means physically, how the three calibration types work, when to use each method, and what the is_calibrated flag guarantees.\n\n","title":"Calibration"},{"category":"section","location":"explanations/calibration/#Why-calibration-matters","page":"Calibration","text":"A hydrophone recording is a time series of normalised numbers in [−1, 1], where 1.0 represents the ADC full-scale voltage. These numbers have no physical units until you apply the system's end-to-end sensitivity.\n\nSystem sensitivity (in dB re 1 V/µPa) is the conversion factor from voltage to underwater sound pressure. It accounts for:\n\nThe hydrophone element's sensitivity (how many volts per pascal of pressure)\nAny preamplifier or board gain\nThe ADC full-scale voltage\n\nFor the SM3M at default gain, the total is −153 dB re 1 V/µPa. That means a normalised sample of 1.0 corresponds to approximately 4.47 × 10⁷ µPa of pressure (a very loud sound — this is the upper limit of what the recorder can capture without clipping). The underwater acoustic reference level is 1 µPa (not 1 Pa as in air), which is why the numbers are large.\n\nThe sign is conventionally negative. The system sensitivity formula is:\n\nsystem_sensitivity_dB = hydrophone_sensitivity + preamp_gain + board_gain\n                        + 20 · log₁₀(1 / Vadc_0pk)\n\nBecause hydrophone sensitivity is typically around −165 dB and gain adds back less than that, the total remains negative.\n\nSPL from a calibrated signal:\n\ndB_SPL = 20 · log₁₀(rms(signal)) − system_sensitivity_dB\n\nBecause system_sensitivity_dB is negative, subtracting it adds a large positive number — converting from dBFS (relative to ADC full scale) to dB re 1 µPa.\n\n","title":"Why calibration matters"},{"category":"section","location":"explanations/calibration/#The-three-calibration-types","page":"Calibration","text":"","title":"The three calibration types"},{"category":"section","location":"explanations/calibration/#NoCalibration","page":"Calibration","text":"No calibration data is available for this recorder. apply_calibration! copies the signal unchanged, and is_calibrated stays false. Metric outputs will be in dBFS rather than dB re 1 µPa, and metric functions will warn you.\n\nUse NoCalibration when you are exploring data or doing relative comparisons where absolute physical units don't matter.","title":"NoCalibration"},{"category":"section","location":"explanations/calibration/#ScalarCalibration","page":"Calibration","text":"One number — system_sensitivity_dB — applies uniformly across all frequencies. This is appropriate for recorders whose frequency response is flat over the band of interest (e.g. SM3M, LS1X).\n\ncal = ScalarCalibration(-153.0f0)   # SM3M at default gain\n\nApplying it multiplies every sample by 10^(-system_sensitivity_dB / 20):\n\nSensitivity Linear multiplier\n−153 dB ≈ 4.47 × 10⁷\n−165 dB ≈ 1.78 × 10⁸\n\nThe GPU-accelerated path for ScalarCalibration uses a KernelAbstractions kernel that runs on whatever device the array lives on — pass a CuArray to use the GPU, a plain Array to use the CPU.","title":"ScalarCalibration"},{"category":"section","location":"explanations/calibration/#TFCalibration","page":"Calibration","text":"A frequency-dependent sensitivity curve, stored as (freqs, tf_db) arrays. Used when the recorder's sensitivity varies across frequency — for example, the Rockhopper hydrophone from Cornell, which is supplied with a per-unit TF curve.\n\ncal = lookup_calibration(\"file.flac\", \"rockhopper\", meta)\n# returns TFCalibration with 2797 frequency points, 1–98000 Hz\n\nThe TF data is loaded from rockhopper_TF.csv at the time lookup_calibration is called and embedded directly in the TFCalibration object. No file path survives into the runtime object.\n\n","title":"TFCalibration"},{"category":"section","location":"explanations/calibration/#Time-domain-vs-frequency-domain-application","page":"Calibration","text":"TFCalibration has two application methods because it is used in two different pipeline contexts.","title":"Time-domain vs frequency-domain application"},{"category":"section","location":"explanations/calibration/#Time-domain:-apply_calibration!","page":"Calibration","text":"Use this when you need the calibrated waveform — for example, before listening to a clip, computing a spectrogram that will be displayed, or extracting click waveforms for further analysis.\n\nout = similar(a.sig)\napply_calibration!(out, a.sig, cal; fs = a.fs)\n\nInternally, this constructs an FIR filter from the TF data and convolves it with the signal:\n\nInterpolate the TF onto an FFT frequency grid (DC to Nyquist, fir_length ÷ 2 + 1 bins).\nConvert dB → linear magnitude.\nInverse FFT to get the impulse response.\nCircularly shift the impulse response to the center of the array so the window peak aligns with the main lobe.\nApply a Hann window to reduce sidelobe energy.\nConvolve with the signal using filtfilt (default, zero-phase) or filt (causal).\n\nFIR length controls frequency resolution and transient duration. The default of 512 samples gives ≈ 188 Hz per bin at 96 kHz — sufficient for smooth hydrophone TF curves. Increase to 1024 if the TF has narrow spectral features.\n\nPhase modes:\n\nphase Method Group delay Transient\n:zero (default) filtfilt None ≈ fir_length − 1 samples at both ends\n:causal filt (fir_length − 1) ÷ 2 samples ≈ fir_length − 1 samples at start only\n\nAt fs = 196,000 Hz (Rockhopper) with fir_length = 512, the causal group delay is ≈ 1.3 ms. At fs = 48,000 Hz (SM3M), it is ≈ 5.3 ms. This matters for click timing in v2 — use :zero (filtfilt) for any analysis that needs sample-accurate timestamps.\n\nNote on filtfilt design: filtfilt applies the filter twice (forward + backward), giving an effective magnitude response of |H(f)|². To achieve the correct calibration |H(f)|² = TFlinear, the FIR is designed from `sqrt.(TFlinear)for the:zeropath. The:causal` path uses TF_linear directly. Both paths produce the same calibrated output; they differ only in phase and edge behaviour.\n\n","title":"Time-domain: apply_calibration!"},{"category":"section","location":"explanations/calibration/#The-is_calibrated-flag","page":"Calibration","text":"Audiodata.is_calibrated is false at construction and set to true only by apply_calibration. It is a hard guarantee:\n\nMetric functions that require physical units (SPL, PSD, TOL, LTSA) check this flag and warn when it is false, labelling output as dBFS.\nApplying calibration twice throws ArgumentError. There is no \"re-calibrate\" path — create a new Audiodata from the raw file if you need to start over.\nNoCalibration does not set the flag to true. A recording with no calibration profile cannot be marked as calibrated.\n\n","title":"The is_calibrated flag"},{"category":"section","location":"explanations/calibration/#Typical-workflow","page":"Calibration","text":"# 1. Read the file (calibration looked up but not applied)\na = read_audio(\"T1-C__0__20170912_181500.wav\")\n@assert !a.is_calibrated\n@assert a.calibration isa ScalarCalibration\n\n# 2. Apply calibration — returns a new Audiodata\na_cal = apply_calibration(a)\n@assert a_cal.is_calibrated\n\n# 3. Now compute metrics on the calibrated recording\n# (metric functions will not warn about missing calibration)\n\nFor archive-scale processing via process_chunks, apply calibration inside the chunk function:\n\ncompute_spl(source) do chunk\n    chunk_cal = apply_calibration(chunk)\n    (; spl_db = rms_to_spl(chunk_cal.sig))\nend","title":"Typical workflow"},{"category":"section","location":"metadata/#Filename-parsing,-recorder-profiles,-and-calibration","page":"Filename parsing and metadata","text":"EcoAcoustics.jl keeps three concerns separate:\n\nReading audio samples — read_audio handles file I/O.\nUnderstanding filenames — parse_filename + RecorderProfile handle recorder-specific naming conventions.\nCalibration — CalibrationProfile + lookup_calibration supply the physical sensitivity constants. Calibration is stored in Audiodata but never applied at I/O time.\n\n","title":"Filename parsing, recorder profiles, and calibration"},{"category":"section","location":"metadata/#Recorder-profiles:-one-file-per-recorder","page":"Filename parsing and metadata","text":"Each supported recorder family has its own file under src/recorders/:\n\nsrc/recorders/\n├── recorders.jl      # RecorderProfile and CalibrationProfile structs,\n│                     # RECORDER_PROFILES and CALIBRATION_PROFILES dicts,\n│                     # lookup_calibration function\n├── rockhopper.jl     # Rockhopper (Embedded Ocean Systems)\n├── sm3m.jl           # SM3M (Wildlife Acoustics)\n├── ls1x.jl           # LS1X (Loggerhead Instruments)\n└── snap.jl           # SNAP (NOAA/OSU, retired)\n\nAdding a new recorder means adding one file and one include line in src/EcoAcoustics.jl. Nothing else changes.\n\n","title":"Recorder profiles: one file per recorder"},{"category":"section","location":"metadata/#Using-built-in-recorder-profiles","page":"Filename parsing and metadata","text":"Pass the recorder name to read_audio:\n\nusing EcoAcoustics\n\nad = read_audio(\"139635MD01_197K_A6M_RH428_20231012_000654Z.flac\";\n                recorder = \"rockhopper\")\n\nInternally, read_audio calls parse_filename which looks up RECORDER_PROFILES[\"rockhopper\"] and parses the filename tokens:\n\nmeta = parse_filename(\"139635MD01_197K_A6M_RH428_20231012_000654Z.flac\";\n                      recorder = \"rockhopper\")\n# (timestamp=DateTime(2023,10,12,0,6,54), timezone=\"UTC\",\n#  lat=missing, lon=missing, site_id=\"A6M\", recorder_id=\"RH428\")\n\nThe metadata is then merged with calibration and any user overrides to produce an Audiodata struct.\n\n","title":"Using built-in recorder profiles"},{"category":"section","location":"metadata/#Providing-metadata-manually","page":"Filename parsing and metadata","text":"When filenames have no structure, supply metadata directly:\n\nusing Dates, EcoAcoustics\n\nad = read_audio(\"mystery_001.wav\";\n                recorder  = \"unknown\",\n                starttime = DateTime(2021, 5, 1, 12, 0, 0),\n                lat       = 38.5,\n                lon       = -74.5,\n                site_id   = \"MAB01\")\n\nparse_filename returns all-nothing/missing for unknown recorders and emits a warning. Your provided metadata populates the final Audiodata.\n\n","title":"Providing metadata manually"},{"category":"section","location":"metadata/#The-Audiodata-struct","page":"Filename parsing and metadata","text":"read_audio returns an Audiodata containing the signal and two sub-structures:\n\nstruct Audiodata\n    sig::Vector{Float64}          # raw uncalibrated samples\n    fs::Float32                   # sampling rate [Hz]\n    starttime::DateTime           # recorder clock time (may be local, not UTC)\n    is_calibrated::Bool           # false until apply_calibration! is called\n    calibration::Calibration      # stored but not applied at I/O\n    metadata::RecordingMetadata   # provenance fields\nend\n\nendtime is a derived helper — not a stored field — so it is always consistent with the actual signal length:\n\nendtime(ad)   # starttime + signal_length / fs (nanosecond precision)\nnsamples(ad)  # length(ad.sig)\nduration(ad)  # endtime(ad) - ad.starttime\n\nProvenance fields live in RecordingMetadata:\n\nstruct RecordingMetadata\n    timezone::Union{String,Nothing}    # IANA name; nothing if unknown\n    lat::Union{Float64,Missing}\n    lon::Union{Float64,Missing}\n    site_id::Union{String,Nothing}\n    recorder::String\n    recorder_id::Union{String,Nothing}\nend\n\nAccess them via ad.metadata.site_id, ad.metadata.recorder, etc.\n\nnote: Timezone and UTC\nstarttime reflects the recorder's clock. When timezone is not \"UTC\", the timestamp is local time and has not been converted to UTC by this package. Convert to UTC using the IANA timezone name before computing absolute time differences across deployments.\n\n","title":"The Audiodata struct"},{"category":"section","location":"metadata/#Calibration-and-is_calibrated","page":"Filename parsing and metadata","text":"Audiodata.calibration stores the calibration object for the recording. It is never applied at I/O time. The is_calibrated flag starts as false and is set to true only by apply_calibration! (v1, in progress). Metric functions (SPL, PSD, LTSA) check the flag and warn when computing on uncalibrated data, labelling output as dBFS.\n\nThree calibration types exist:\n\nType Description\nNoCalibration() No calibration available\nScalarCalibration(sens_db) Single total sensitivity in dB re 1 V/µPa\nTFCalibration(freqs, tf_db) Frequency-dependent transfer function\n\nScalar calibration is returned by lookup_calibration when a CalibrationProfile is registered for the recorder. The total sensitivity is:\n\nsens_db = hydrophone_sensitivity + preamp_gain + board_gain\n          + 20·log10(1 / Vadc_0pk)\n\nTransfer-function (TF) calibration applies to recorders like the Rockhopper, where sensitivity is frequency-dependent. The TF is stored as a two-column file (frequency in Hz, sensitivity in dB) supplied by the manufacturer. At calibration lookup time the file is read once and the arrays are embedded directly in the TFCalibration struct, which then travels with Audiodata. No path reference survives into the runtime object. TF calibration is implemented in v2.\n\n","title":"Calibration and is_calibrated"},{"category":"section","location":"metadata/#Adding-a-new-recorder","page":"Filename parsing and metadata","text":"Checklist:\n\nCreate src/recorders/yourrecorder.jl following the examples below.\nAdd include(\"recorders/yourrecorder.jl\") to src/EcoAcoustics.jl after the other recorder includes.\nAdd a @testset block to test/recorder_tests/test_filename_formats.jl with at least one representative filename.\nIf calibration values are known, add a test to test/recorder_tests/test_calibration.jl.","title":"Adding a new recorder"},{"category":"section","location":"metadata/#Tokenized-filenames","page":"Filename parsing and metadata","text":"Most recorders encode metadata as underscore-separated tokens. Suppose filenames follow:\n\nST01_20250101_120000_ID42.wav\n\nOnly specify the fields that apply; everything else defaults to nothing.\n\n# src/recorders/soundtrap.jl\n# SoundTrap (Ocean Instruments) — example\n\nRECORDER_PROFILES[\"soundtrap\"] = RecorderProfile(\n    \"SoundTrap\";\n    split_char        = '_',\n    date_token        = 2,               # \"20250101\"\n    time_token        = 3,               # \"120000\"\n    datetime_format   = \"yyyymmdd_HHMMSS\",\n    site_token        = 1,               # \"ST01\"\n    recorder_id_token = 4,               # \"ID42\"\n)\n\nCALIBRATION_PROFILES[\"soundtrap\"] = CalibrationProfile(\n    \"SoundTrap 300 HF default\",\n    -177.0,   # hydrophone sensitivity [dB re 1 V/µPa]\n    +16.0,    # preamp gain [dB]\n      0.0,    # board gain [dB]\n      1.0,    # ADC full-scale peak voltage [V]\n)\n\nTest:\n\n@testset \"SoundTrap filename parsing\" begin\n    meta = EcoAcoustics.parse_filename(\"ST01_20250101_120000_ID42.wav\";\n                                       recorder = \"soundtrap\")\n    @test meta.timestamp == DateTime(2025, 1, 1, 12, 0, 0)\n    @test meta.site_id   == \"ST01\"\n    @test meta.recorder_id == \"ID42\"\nend","title":"Tokenized filenames"},{"category":"section","location":"metadata/#Custom-parser-functions-(e.g.,-SNAP)","page":"Filename parsing and metadata","text":"When token positions alone are insufficient, supply a parser function that receives the filename basename (without extension) and returns the standard metadata named tuple. See src/recorders/snap.jl for a worked example — SNAP uses a 2-digit year (YYMMDD) that requires explicit expansion before parsing.\n\nThe custom parser overrides all generic token logic. Only the function output matters; the token-index fields on the profile are ignored when a parser is present.","title":"Custom parser functions (e.g., SNAP)"},{"category":"section","location":"metadata/#Recorders-with-XML-sidecars-(e.g.,-DMON)","page":"Filename parsing and metadata","text":"Some recorders (e.g., DMON, PAMS) store metadata in a per-file XML sidecar alongside each WAV. The has_xml flag on RecorderProfile marks this:\n\nRECORDER_PROFILES[\"dmon\"] = RecorderProfile(\n    ...,\n    has_xml = true,\n    parser  = _parse_dmon,   # reads both the wav path and the XML sidecar\n)\n\nThe custom parser function receives the file path, locates the sidecar (e.g., same basename with .xml extension), parses it, and returns the standard metadata named tuple. All XML-parsing logic is isolated to src/recorders/dmon.jl — nothing else in the package changes. DMON support is planned for a future release.\n\n","title":"Recorders with XML sidecars (e.g., DMON)"},{"category":"section","location":"metadata/#No-generic-guessing","page":"Filename parsing and metadata","text":"Calling parse_filename with an unrecognised recorder name:\n\nparse_filename(\"foo_20200101_120000.wav\"; recorder=\"unknownbox\")\n\nreturns all nothing/missing fields and emits a warning. There is no generic heuristic fallback. Silent mis-parsing in scientific workflows is worse than an explicit warning.\n\nYou must either define a RecorderProfile or supply metadata manually via read_audio.\n\n","title":"No generic guessing"},{"category":"section","location":"metadata/#How-read_audio-assembles-the-result","page":"Filename parsing and metadata","text":"parse_filename(path; recorder) — extract what the filename knows.\nlookup_calibration(path, recorder, meta) — retrieve calibration.\nMerge in order of precedence (highest wins):\nUser-supplied kwargs (starttime, lat, lon, site_id)\nFilename-parsed metadata\nRecorder defaults\nConstruct Audiodata with is_calibrated = false.\n\nThis merge order means you can always override a bad filename parse without modifying the profile.","title":"How read_audio assembles the result"},{"category":"section","location":"calibration/rockhopper/#Rockhopper-Calibration","page":"Rockhopper","text":"The Cornell Rockhopper is a small underwater acoustic recorder designed for long-term passive monitoring. It stores audio as 16-bit FLAC at sample rates up to 200 kHz. EcoAcoustics.jl ships a fleet-level frequency-dependent calibration for the Rockhopper and applies it automatically.\n\n","title":"Rockhopper Calibration"},{"category":"section","location":"calibration/rockhopper/#What-is-the-Rockhopper-calibration?","page":"Rockhopper","text":"The Rockhopper has a hydrophone, a preamplifier, and an ADC. The combined system response varies with frequency: sensitivity is not constant across the recording bandwidth. To convert ADC units (full-scale counts) to physical pressure units (µPa), you need the transfer function — the system sensitivity at every frequency in dB.\n\nEcoAcoustics.jl uses the AnalogSensitivity_dB_re_1VperRefPress column from the Cornell RH_calCurves spreadsheet, converted to the package canonical form (dB re full-scale per µPa). The calibration is loaded once at startup and reused across all Rockhopper PSD computations.\n\n","title":"What is the Rockhopper calibration?"},{"category":"section","location":"calibration/rockhopper/#The-calibration-pipeline","page":"Rockhopper","text":"ADC full-scale       →    TF correction    →    µPa²/Hz\n(dimensionless)           (DD-12, DD-13)        (physical)\n\nIn detail:\n\nThe raw signal is stored in full-scale units ([-1, +1]) in Audiodata.sig.\nThe spectrogram is computed from the raw signal — no calibration yet (DD-08).\ncompute_psd(audio) calls _psd_calibration(audio), which auto-resolves the Rockhopper TF via get_profile(:rockhopper).tf (DD-14).\napply_calibration!(psd_linear, freqs, tf) divides each PSD frequency bin by the interpolated TF value in linear power: psd_calibrated[k] = psd_raw[k] / tf_lin[k] where tf_lin[k] = 10^(tf_dB[k] / 10) and tf_dB[k] is the interpolated sensitivity at frequency freqs[k].\n\n","title":"The calibration pipeline"},{"category":"section","location":"calibration/rockhopper/#Units-and-sign-convention","page":"Rockhopper","text":"The canonical TF form in EcoAcoustics.jl is:\n\ntf_dB = AnalogSensitivity_dB − 20·log10(Vmax_peak_V)\n\nwhere Vmax_peak_V = 5.0 V for the Rockhopper. This shift (~13.98 dB) converts from \"dB re 1 V per µPa at the ADC input\" to \"dB re ADC full-scale per µPa.\" After the TF correction, PSD values are in µPa²/Hz and psd_units returns :µPa²_per_Hz.\n\nThe sign convention is: tf_dB is conventionally negative (e.g. −160 dB at low frequencies), meaning the hydrophone + preamplifier + ADC system attenuates the physical pressure signal before digitisation. Dividing by a small number (10^(tf_dB/10) ≪ 1) scales the PSD back up to physical units.\n\n","title":"Units and sign convention"},{"category":"section","location":"calibration/rockhopper/#Automatic-calibration-resolution","page":"Rockhopper","text":"For any Audiodata with recorder = \"rockhopper\", compute_psd resolves the calibration automatically:\n\naudio  = read_audio(\"RH428_20230101_120000.flac\"; recorder = \"rockhopper\")\nresult = compute_psd(audio; window_seconds = 1.0)\n# result.is_calibrated == true\n# psd_units(result) == :µPa²_per_Hz\n\nThis works because _psd_calibration (DD-14) checks get_profile(:rockhopper).tf, which returns the shipped TFCalibration.\n\nIf you have a per-unit calibration file (not the fleet-level default), load it explicitly and pass it to compute_psd:\n\nmy_tf  = load_tf_calcurves(\"/path/to/RH428_calCurves.csv\"; vmax_peak_V = 5.0)\nspec   = spectrogram(audio.sig; fs = Float64(audio.fs), window_seconds = 1.0)\nresult = compute_psd(spec, my_tf)\n\n","title":"Automatic calibration resolution"},{"category":"section","location":"calibration/rockhopper/#The-shipped-calibration-file","page":"Rockhopper","text":"The calibration data is stored at:\n\nsrc/recorders/calibration_data/rockhopper_tf_calibration.csv\n\nIt was derived from the RH_calCurves sheet of Rockhopper_TF.xlsx (Cornell University). The CSV contains 2798 frequency points from 0 Hz to approximately 99 884 Hz with headers:\n\nFrequency_Hz,SensorSensitivity_dB_re_1V_perRefPress,PreampGain_dB,\nRecorderGain_dB,AnalogSensitivity_dB_re_1VperRefPress\n\nOnly Frequency_Hz and AnalogSensitivity_dB_re_1VperRefPress are used by load_tf_calcurves. The other columns are retained in the CSV for provenance.\n\n","title":"The shipped calibration file"},{"category":"section","location":"calibration/rockhopper/#What-NOT-to-use","page":"Rockhopper","text":"The RH_calCurves spreadsheet also has a column called TF (column 13). This column folds in a PeakToRMS conversion factor of 20·log10(2√2) ≈ 9.03 dB. That factor is appropriate for converting a peak-calibrated sine to RMS in the time domain, but it is not appropriate for PSD, which operates on instantaneous squared samples. Using the TF column instead of AnalogSensitivity will produce PSD values that are 9.03 dB too high. The load_tf_calcurves function expects the AnalogSensitivity column; it cannot detect which column you extracted from the column name alone.\n\n","title":"What NOT to use"},{"category":"section","location":"calibration/rockhopper/#Cross-validation","page":"Rockhopper","text":"PSD output is cross-validated against MANTA (a PAMGuide-compatible tool). The reference data and expected tolerance are described in test/validation/rockhopper/README.md. The test file is test/test_psd_manta_validation.jl; it skips gracefully when reference data is absent.","title":"Cross-validation"},{"category":"section","location":"explanations/band_metrics/#Band-metrics-and-the-per-cycle-table","page":"Band metrics","text":"Soundscape analysis rarely uses the full spectrum directly. Instead it reduces each recording to a handful of frequency-band levels — a 63 Hz shipping indicator, a broadband 100–200 Hz summary, and so on — and tracks how those levels behave over time. EcoAcoustics.jl provides this in two composable steps:\n\nBand integration — sum power across a band's bins to get a band SPL series (compute_spl on a PSDResult or an LTSAResult).\nPer-cycle table — flatten those band levels into one tidy row per recording and assemble a DataFrame for statistical analysis in R (band_metrics, band_table).\n\nBoth steps are pure composition over compute_psd and compute_spl — no new signal processing (DD-29).\n\n","title":"Band metrics and the per-cycle table"},{"category":"section","location":"explanations/band_metrics/#Band-integration:-compute_spl-on-a-PSD-or-an-LTSA","page":"Band metrics","text":"compute_spl integrates each requested band out of a calibrated frequency × time matrix. Both callers run the identical band-integration core (_integrate_bands); they differ only in what a \"column\" of the series means:\n\nInput A column is… Series length Use for\nPSDResult one FFT frame (~1 s) hundreds per file within-file distributions, percentiles\nLTSAResult one LTSA time column (e.g. 60 s) one per column long-recording band time series\n\n# Band time series across a long recording (one value per LTSA column):\nlt  = compute_ltsa(audio; average_span_seconds = 60.0)\nspl = compute_spl(lt; bands = Dict(:b100_200 => (100.0, 200.0)))\nspl.bands[:b100_200].spl_dB     # per-minute 100–200 Hz level\n\nEach band becomes a BandSPL holding the per-column series (spl_dB), the energetic mean (mean_dB), and nine percentiles. The bands are supplied as a Dict{Symbol,Tuple{Float64,Float64}} — there is no default band set (DD-27); you always state the frequency ranges explicitly.","title":"Band integration: compute_spl on a PSD or an LTSA"},{"category":"section","location":"explanations/band_metrics/#Percentile-direction-(read-this-before-interpreting-Ln)","page":"Band metrics","text":"L_n is the exceedance level — the level exceeded n% of the columns — following the ISO 18405 standard (ADEON, OSPAR/JOMOPANS; DD-31). Equivalently L_n is the (100−n)th percentile:\n\nL1, L5 → the loud tail (high levels; brief vessel passages).\nL95, L99 → the quiet background (low levels; L95 is the standard ambient indicator).\n\nNote this is the inverse of raw-percentile SPD-plot labels, where a 5% line is the quiet 5th percentile (= EA's L95). See the SPL page for the full discussion and the suggested methods wording. When you want a loud-event indicator, use L1, L5, or max — not L95.\n\n","title":"Percentile direction (read this before interpreting Ln)"},{"category":"section","location":"explanations/band_metrics/#The-per-cycle-table","page":"Band metrics","text":"For archive-scale exploratory work — e.g. a three-month, duty-cycled deployment where each cycle is one self-contained 5-minute file — the natural unit is one row per cycle, with a set of band metrics as columns. That table is the hand-off to the R-side statistical modelling.","title":"The per-cycle table"},{"category":"section","location":"explanations/band_metrics/#band_metrics-—-one-row","page":"Band metrics","text":"audio = read_audio(\"cycle_0001.wav\"; recorder = \"sm3m\")\nbands = Dict(:tol_63 => (56.1, 70.7), :b100_200 => (100.0, 200.0))\nrow   = band_metrics(audio; bands = bands, window_seconds = 1.0)\n\nband_metrics computes the file's PSD, integrates the bands, and flattens the result into a Dict{Symbol,Any}:\n\nMeta columns: start_time (DateTime), duration_s, n_frames — the number of PSD frames the percentiles are taken over (a quality-control signal: a short or partial file shows up as few frames).\nBand metric columns: for every band k and metric m, a key Symbol(k, :_, m, :_dB) — e.g. :tol_63_mean_dB. The metrics are mean, median, L1, L5, L10, L25, L75, L90, L95, L99, max.\n\nPercentiles are taken over the file's PSD frames (~1 s each), giving a rich within-cycle distribution — fine enough that a single vessel pass inside a cycle moves L5/max (the loud-tail exceedance levels).","title":"band_metrics — one row"},{"category":"section","location":"explanations/band_metrics/#band_table-—-the-DataFrame","page":"Band metrics","text":"files = readdir(\"deployment/\"; join = true)\ndf = band_table(files;\n                reader = f -> read_audio(f; recorder = \"sm3m\"),\n                bands  = bands,\n                window_seconds = 1.0)\n\nband_table maps band_metrics over items and assembles a typed DataFrame, one row per cycle:\n\nreader maps each item to an Audiodata. The read happens inside the per-item error guard, so corrupt/unreadable files are skipped (on_error = :skip, the default) rather than aborting the whole run. Pass on_error = :fail to stop on the first error.\nSchema is fixed by bands, so every row has the same columns regardless of which files succeeded. Columns are concretely typed (DateTime, Float64, Int) even when the table is empty.\n\nWrite it out for R with CSV.write(\"bands.csv\", df).","title":"band_table — the DataFrame"},{"category":"section","location":"explanations/band_metrics/#SM3M-per-era-calibration","page":"Band metrics","text":"Absolute band levels are only comparable once the correct per-era hydrophone sensitivity is applied. The SM3M profile currently encodes a single era (−153 dB, pre-Apr-2018); for post-Apr-2018 files pass the correct calibration explicitly via the cal keyword until per-era resolution is implemented:\n\nband_metrics(audio; bands = bands, window_seconds = 1.0,\n             cal = ScalarCalibration(-165.0f0))   # post-Apr-2018 era\n\nSee CANDIDATE_ACOUSTIC_BANDS.org and ROADMAP.org for the full data context, and examples/band_table_candidate_bands.jl for the complete Chapter 1 workflow.\n\n","title":"SM3M per-era calibration"},{"category":"section","location":"explanations/band_metrics/#Provisional-parameters","page":"Band metrics","text":"The choices in the Chapter 1 pipeline — the candidate band set, the 1 s FFT window, and taking percentiles over PSD frames — are the test-run configuration, not fixed conventions. They are expected to change for production runs once the exploratory analysis identifies which bands and timescales carry signal (DD-29).\n\n","title":"Provisional parameters"},{"category":"section","location":"explanations/band_metrics/#References","page":"Band metrics","text":"Design decisions: DD-27 (explicit bands), DD-29 (band-integration core and the per-cycle table) in docs/design_decisions.md. Composes the PSD, SPL, and LTSA layers.","title":"References"},{"category":"section","location":"explanations/ltsa/#Long-Term-Spectral-Average-(LTSA)","page":"LTSA","text":"A long-term spectral average (LTSA) shows how acoustic energy is distributed across frequency over long timescales — hours, days, or a full deployment. It is the standard first look at a passive-acoustic dataset: a single frequency × time image in which diel cycles, vessel passages, weather events, and seasonal shifts are all visible at a glance.\n\nIn EcoAcoustics.jl the LTSA is also the reusable pre-aggregation product for downstream soundscape work. It is computed once per recording as a matrix of linear power spectral density; arbitrary frequency bands and percentile statistics are then derived from that matrix without touching the audio again.\n\n","title":"Long-Term Spectral Average (LTSA)"},{"category":"section","location":"explanations/ltsa/#Position-in-the-pipeline","page":"LTSA","text":"spectrogram → complex STFT matrix           [SpectrogramResult]\n      │\n      ▼\ncompute_psd → power per Hz, per frame        [PSDResult, µPa²/Hz after calibration]\n      │\n      ▼\ncompute_ltsa → power per Hz, per time column [LTSAResult, µPa²/Hz]\n      │\n      ├──→ band time series   (integrate freq bands out of each column)\n      │\n      └──→ percentile statistics (down the time axis, per band)\n\ncompute_ltsa composes the layers below it — it adds no new spectral-estimation or normalization math (DD-28). Each column is exactly average_psd(compute_psd(spectrogram(slice))).\n\n","title":"Position in the pipeline"},{"category":"section","location":"explanations/ltsa/#What-an-LTSA-column-is","page":"LTSA","text":"The recording is divided into consecutive, non-overlapping time columns, each average_span_seconds long (typically 60 s, 300 s, or 3600 s). Within one column:\n\nspectrogram computes the short-time Fourier transform using the inner FFT window (fft_window_seconds, default 1 s) and overlap (fft_overlap, default 0.5). A 60 s column with a 1 s / 50 % window contains ~119 FFT frames.\ncompute_psd converts each frame to power per Hz (µPa²/Hz once calibrated).\naverage_psd averages those frames in linear power — the energetic mean.\n\nThe averaged spectrum is one column of LTSAResult.matrix. There is no dB-mean and no median mode: averaging in linear power is the physically correct operation and matches the PSD and SPL layers (DD-16, DD-22, DD-28).\n\n                 time columns  (average_span_seconds each)\n              ┌────┬────┬────┬────┬────┐\n   f  Nyquist │    │    │    │    │    │\n   r          │    │    │    │    │    │   each cell = energetic mean of the\n   e          │    │    │    │    │    │   FFT frames inside that column,\n   q          │    │    │    │    │    │   at that frequency bin\n   s        0 │    │    │    │    │    │\n              └────┴────┴────┴────┴────┘\n\n","title":"What an LTSA column is"},{"category":"section","location":"explanations/ltsa/#Two-timescales:-the-column-span-vs-the-FFT-window","page":"LTSA","text":"An LTSA has two independent time parameters, and they do different jobs:\n\nParameter Symbol Controls Typical value\naverage_span_seconds column width time resolution of the LTSA image 60 s – 3600 s\nfft_window_seconds inner FFT window frequency resolution of every column 1 s\n\nfft_window_seconds sets frequency resolution. Bin width is df = fs / nfft ≈ 1 / fft_window_seconds. A 1 s window gives 1 Hz bins; a 0.1 s window gives 10 Hz bins. Longer windows resolve tonals (vessel lines, turbine tonals) more finely but smear short transients.\naverage_span_seconds sets time resolution. It is how much the column averages over. Longer columns are smoother and cheaper to store/plot but hide short events; shorter columns show detail at the cost of a longer, noisier image.\n\nThe only hard coupling between them: a column must be at least as long as one FFT window (average_span_seconds × fs ≥ fft_window_seconds × fs), or a column could not contain a single frame. compute_ltsa asserts this.\n\n","title":"Two timescales: the column span vs the FFT window"},{"category":"section","location":"explanations/ltsa/#Non-overlapping-columns-and-the-dropped-partial-column-(DD-28)","page":"LTSA","text":"Column j covers samples (j-1)·S + 1 … j·S, where S = round(Int, average_span_seconds × fs). Columns never overlap, and the FFT frames of one column never cross into the next.\n\nThe number of columns is n_columns = div(nsamples, S) — integer floor division. If the recording length is not an exact multiple of the column span, the final < S samples are dropped, not zero-padded or emitted as a short column. This keeps every column a true average_span_seconds average, so statistics taken across columns (means, percentiles) are not biased by a short-and-therefore-different final column. See DD-28 for the full rationale.\n\nLTSAResult.column_times[j] is the start time of column j in seconds, relative to the start of the analysed audio, derived from the integer sample offset so it reflects the actual rounding of S.\n\n","title":"Non-overlapping columns and the dropped partial column (DD-28)"},{"category":"section","location":"explanations/ltsa/#Why-the-matrix-is-stored-in-linear-units","page":"LTSA","text":"LTSAResult.matrix holds linear PSD (µPa²/Hz when calibrated), not decibels. This is deliberate: the LTSA is the input to two further operations that both require linear power —\n\nBand time series. Integrating a frequency band (e.g. the 63 Hz one-third-octave, or a hand-picked 100–200 Hz band) is a sum of linear power across the band's bins, done per column. The candidate bands for the first soundscape analyses are listed in CANDIDATE_ACOUSTIC_BANDS.org; every one of them can be integrated out of the same LTSA matrix without re-reading audio.\nPercentiles over time. Exceedance levels (L10, L50, L90, …) and maxima are taken down the time axis of a band's linear series, then converted to dB.\n\nConvert to dB only for display, with to_dB(result) — it returns a new matrix and does not mutate the stored one.\n\n","title":"Why the matrix is stored in linear units"},{"category":"section","location":"explanations/ltsa/#Calibration","page":"LTSA","text":"Calibration is resolved once, before the column loop, using the same cascade as compute_psd (pre-calibrated signal → attached calibration → recorder profile → warn and fall back to full-scale; DD-14). Resolving once means a missing-calibration warning is emitted at most a single time rather than once per column.\n\naudio = read_audio(\"deployment_hour.wav\"; recorder = \"sm3m\")\nlt    = compute_ltsa(audio; average_span_seconds = 60.0)   # SM3M scalar cal auto-resolved\nltsa_units(lt)     # :µPa²_per_Hz\n\nLTSAResult.is_calibrated follows the same rule as the PSD layer: audio.is_calibrated || !(resolved_cal isa NoCalibration). ltsa_units dispatches on that flag (returning :µPa²_per_Hz or :fullscale²_per_Hz), mirroring psd_units (DD-15). To override auto-resolution, pass cal = ... explicitly.\n\n","title":"Calibration"},{"category":"section","location":"explanations/ltsa/#Worked-example","page":"LTSA","text":"using EcoAcoustics\n\naudio = read_audio(\"deployment_hour.wav\"; recorder = \"sm3m\")\n\n# 1-minute LTSA columns, 1 s / 50 % inner FFT window (1 Hz bins).\nlt = compute_ltsa(audio; average_span_seconds = 60.0,\n                  fft_window_seconds = 1.0, fft_overlap = 0.5)\n\nsize(lt.matrix)        # (n_freqs, n_minutes)\nlt.freqs               # frequency axis, Hz\nlt.column_times        # [0.0, 60.0, 120.0, ...] seconds\nltsa_units(lt)         # :µPa²_per_Hz\n\nlt_dB = to_dB(lt)      # Matrix{Float64}, dB re 1 µPa²/Hz — for plotting","title":"Worked example"},{"category":"section","location":"explanations/ltsa/#Reusing-one-FFT-plan-at-archive-scale","page":"LTSA","text":"Every column uses the same inner FFT size, so a single pre-built FFTW plan can be shared across all of them. For long recordings this avoids FFTW's per-call plan-selection overhead:\n\nplan = make_spectrogram_plan(audio.fs, 1.0)   # 1 s window at this fs\nlt   = compute_ltsa(audio; average_span_seconds = 60.0,\n                    fft_window_seconds = 1.0, fft_plan = plan)\n\nThe plan changes only how FFTW computes each transform, never the result — the output is bit-identical to the no-plan call (verified in test/test_ltsa.jl).\n\n","title":"Reusing one FFT plan at archive scale"},{"category":"section","location":"explanations/ltsa/#Parameter-selection-guidance","page":"LTSA","text":"Recording length Suggested average_span_seconds Notes\nMinutes to ~1 hour 1 – 10 s Fine detail; individual vessel passes resolved\nHours to ~1 day 60 s The common default; diel structure clear\nDays to a deployment 300 – 3600 s Smooth, storable; seasonal/weekly structure\n\nChoose fft_window_seconds from the frequency structure you need to resolve, not the recording length: 1 s (1 Hz bins) is a good default for soundscape work; drop to 0.1 s only when short transients matter more than tonal resolution.\n\n","title":"Parameter selection guidance"},{"category":"section","location":"explanations/ltsa/#When-not-to-use-compute_ltsa","page":"LTSA","text":"Recording shorter than one column span. There is nothing to average across time — call compute_psd directly. compute_ltsa asserts rather than returning an empty result.\nA single deployment-wide average spectrum is wanted (one spectrum, not a time series) — use average_psd(compute_psd(...)) over the whole signal.\nOverlapping or dB-averaged columns are required. Not supported by design (DD-28); both are non-breaking to add later if a concrete need appears.\n\n","title":"When not to use compute_ltsa"},{"category":"section","location":"explanations/ltsa/#References","page":"LTSA","text":"Merchant, N.D., et al. (2015). Measuring Acoustic Habitats. Methods in Ecology and Evolution, 6, 257–265. DOI:10.1111/2041-210X.12330\n\nDesign decision: DD-28 in docs/design_decisions.md. Composes the PSD layer (DD-08 through DD-16) and the spectrogram layer (DD-01 through DD-07).","title":"References"},{"category":"section","location":"sources/#Audio-sources-and-time-based-access","page":"Audio sources and time-based access","text":"EcoAcoustics.jl separates the question of where audio lives from the question of how it is processed. The bridge between the two is the AbstractAudioSource interface.\n\n","title":"Audio sources and time-based access"},{"category":"section","location":"sources/#The-source-abstraction","page":"Audio sources and time-based access","text":"An audio source is a Julia struct that knows how to retrieve audio for any requested time window. Callers ask for audio by time:\n\nchunk = read_audio_range(source, DateTime(2023,10,12,18,15),\n                                 DateTime(2023,10,12,18,16))\n\nand the source figures out which file(s) to read, how to assemble the signal, and how to fill any gaps. Nothing above this layer opens files directly.\n\nAbstractAudioSource is defined in src/sources/AbstractAudioSource.jl. Any concrete subtype must implement two methods:\n\nMethod Returns\ntime_range(source) (start::DateTime, stop::DateTime) — the full span of available audio\nread_audio_range(source, start, stop) Audiodata covering [start, stop] exactly\n\nA third method, coverage_fraction(source, start, stop), has a default implementation (always returns 1.0) that subtypes can override when they have gap information.","title":"The source abstraction"},{"category":"section","location":"sources/#Why-an-abstract-interface?","page":"Audio sources and time-based access","text":"The same chunking code, metric computations, and analysis pipelines work identically regardless of whether audio comes from one WAV file, an indexed directory of thousands of files, or (in a future version) a cloud Zarr archive. The source handles the I/O details; everything else sees only read_audio_range.\n\nAdding a new storage backend — for example, S3-hosted Zarr — means writing one new subtype in src/sources/. No code above that layer changes.\n\n","title":"Why an abstract interface?"},{"category":"section","location":"sources/#SingleFileSource","page":"Audio sources and time-based access","text":"SingleFileSource is the simplest concrete source: it wraps one WAV or FLAC file. At construction time, the entire file is read into memory and cached in an Audiodata struct. All subsequent read_audio_range calls slice the in-memory signal — no further disk I/O occurs.\n\nsrc = SingleFileSource(\"T1-C__0__20170912_181500.wav\"; recorder=\"sm3m\")\n\nt_start, t_stop = time_range(src)\nchunk = read_audio_range(src, t_start, t_stop + Minute(1))\n\nWhen to use it: For one-off analysis of a single file, unit tests, and interactive exploration. The file must fit in RAM.\n\nWhen not to use it: When processing an archive of many files — use IndexedFileSource instead.\n\n","title":"SingleFileSource"},{"category":"section","location":"sources/#Gap-handling","page":"Audio sources and time-based access","text":"read_audio_range accepts a gap_handling keyword that controls what happens when the requested time window extends beyond the available audio:\n\nValue Behaviour\n:zero_fill (default) Regions outside the file are filled with zeros. The returned signal always spans exactly [start, stop].\n:error An ArgumentError is thrown if any part of the window is uncovered.\n\nThe fraction of the returned signal that is real audio (as opposed to zero-fill) is available from coverage_fraction:\n\nfrac = coverage_fraction(src, start, stop)\n# 1.0 = fully covered; 0.0 = entirely outside the file\n\nMetric functions and the chunks iterator use coverage_fraction to annotate each result row, so downstream analyses can distinguish high- quality data from padded windows.","title":"Gap handling"},{"category":"section","location":"sources/#Return-value-for-no-overlap-windows","page":"Audio sources and time-based access","text":"If the requested window has no overlap with the file at all, read_audio_range returns an Audiodata with a zero-length signal (nsamples(chunk) == 0). Downstream code must handle this case. This avoids the need for nothing-checks everywhere — the same type is returned in all cases.\n\n","title":"Return value for no-overlap windows"},{"category":"section","location":"sources/#The-DateTime-precision-constraint","page":"Audio sources and time-based access","text":"DateTime in Julia has millisecond resolution. This becomes important at the boundary between time arithmetic and sample counts.\n\nConsider a file recorded at 96 000 Hz with 10.5 seconds of audio:\n\nnsamples = 1 008 000\nduration = 1 008 000 / 96 000 = 10.500 000 s  (exact)\n\nThe endtime helper computes this as nanoseconds first and then stores in a DateTime:\n\nendtime(a) = a.starttime + Nanosecond(round(Int, 1e9 * nsamples / fs))\n#          = starttime + Nanosecond(10_500_000_000)\n#          = starttime + 10 500 ms   ✓ (exact in this example)\n\nNow consider a file where the duration does not divide to an exact millisecond:\n\nnsamples = 1 000 001\nduration = 1 000 001 / 96 000 ≈ 10.416 677 083 s\n         = 10 416.677 083 ms\n\nAdding Nanosecond(10_416_677_083) to a DateTime truncates to milliseconds:\n\nstarttime + Nanosecond(10_416_677_083)\n# → starttime + 10 416 ms   (677 µs dropped)\n\nIf read_audio_range uses this truncated endtime to derive how many samples to copy, it gets:\n\nsamples_to_copy = round(10_416 ms / 1000 * 96_000) = 1 000 000 ≠ 1 000 001\n\nOff by one sample at the tail of every file that doesn't divide evenly.","title":"The DateTime precision constraint"},{"category":"section","location":"sources/#The-fix:-split-boundary-arithmetic","page":"Audio sources and time-based access","text":"SingleFileSource.read_audio_range avoids this by splitting sample-count computation at the file boundary:\n\nWhen the window ends at or after the file end (stop >= file_end), use the exact n_file sample count for the real-audio portion. No time arithmetic, no rounding.\nWhen the window starts at or before the file start (start <= file_start), set src_offset = 0. The file always starts at sample 0.\nOnly gap regions (zero-fill outside the file) are derived from time arithmetic, where a ±1 sample error in silence is inconsequential.\n\nThis is a deliberate design decision, documented in the source code.","title":"The fix: split-boundary arithmetic"},{"category":"section","location":"sources/#Click-detectors-require-sample-level-precision","page":"Audio sources and time-based access","text":"The DateTime truncation issue is why click detector output timestamps are stored as a (file_start::DateTime, sample_offset::Int64) pair, not as a bare DateTime. The file_start anchors the recording, and sample_offset gives the exact integer position within it. The wall-clock time of a click is then:\n\nevent_time(file_start, sample_offset, fs) =\n    file_start + Nanosecond(round(Int, 1e9 * sample_offset / fs))\n\nThis is lossless. Storing clicks as plain DateTime would lose up to 0.5 ms of precision at 96 kHz — about 48 samples. Click detector design is planned for v2.\n\n","title":"Click detectors require sample-level precision"},{"category":"section","location":"sources/#Metadata-propagation","page":"Audio sources and time-based access","text":"read_audio_range copies calibration and all RecordingMetadata fields from the source file to every returned Audiodata. The returned chunk always has is_calibrated = false, regardless of the source file's state. Calibration is applied later via apply_calibration!.\n\nThis means the chunk carries the same recorder identity, location, and calibration object as the original file, ready for metric computation.\n\n","title":"Metadata propagation"},{"category":"section","location":"sources/#Iterating-over-chunks","page":"Audio sources and time-based access","text":"chunks is the low-level iteration primitive. It returns a lazy ChunksIterator that yields one Audiodata per window on demand:\n\nsrc = SingleFileSource(\"deployment.wav\"; recorder=\"sm3m\")\nfor chunk in chunks(src; chunk_seconds = 60.0)\n    println(chunk.starttime, \"  nsamples=\", nsamples(chunk))\nend\n\nThe iterator has Base.IteratorSize = SizeUnknown — length is not defined because the number of emitted chunks depends on gap filtering and cannot be known without iterating. Use collect to materialise all chunks, or process them in a for loop.","title":"Iterating over chunks"},{"category":"section","location":"sources/#Gap-handling-in-chunks","page":"Audio sources and time-based access","text":"The gap_handling keyword has three modes:\n\nValue Behaviour\n:skip (default) Windows entirely in a gap (coverage_fraction == 0) are silently skipped. Windows with partial coverage are still emitted — their signal is zero-filled at the gap boundary. Matches how ecologists treat duty-cycled recordings: off-period windows are invisible; on-period windows near a boundary carry a coverage_fraction < 1 annotation.\n:zero_fill All windows in the source time span are emitted. Gap regions are zero-filled. Use when a uniform time axis is required (e.g., LTSA).\n:error All windows are emitted. read_audio_range throws ArgumentError if any window has partial or zero coverage.","title":"Gap handling in chunks"},{"category":"section","location":"sources/#Overlapping-windows","page":"Audio sources and time-based access","text":"Set stride_seconds < chunk_seconds for overlapping windows:\n\nfor chunk in chunks(src; chunk_seconds = 2.0, stride_seconds = 1.0)\n    # consecutive chunks share 1 second of signal\nend\n\nWindows that extend past the source end are still emitted with the out-of-file region zero-filled (:zero_fill / :skip modes) or raising (:error mode).\n\n","title":"Overlapping windows"},{"category":"section","location":"sources/#process_chunks-—-batch-metric-computation","page":"Audio sources and time-based access","text":"process_chunks is the primary entry point for archive-scale computation. It applies a user function to every chunk and returns a DataFrame:\n\ndf = process_chunks(src,\n                    chunk -> (rms = sqrt(sum(abs2, chunk.sig) / length(chunk.sig)),);\n                    chunk_seconds = 60.0,\n                    on_error      = :skip)\n# columns: start_time, coverage_fraction, rms\n\nThe user function f(chunk::Audiodata) must return a NamedTuple. Its fields become columns in the output. start_time and coverage_fraction are always prepended; do not return those field names from f.","title":"process_chunks — batch metric computation"},{"category":"section","location":"sources/#Parallelism","page":"Audio sources and time-based access","text":"parallel=:threads (default) dispatches over all available Julia threads via Threads.@threads. Windows are pre-materialised into an indexed array so each thread writes its result into a pre-allocated slot — row order in the output DataFrame matches window order regardless of thread scheduling.\n\nf must be thread-safe. Avoid shared mutable state in closures passed as f.\n\nparallel=:none runs sequentially, which is useful for debugging and for f functions with internal mutable state.","title":"Parallelism"},{"category":"section","location":"sources/#Error-handling","page":"Audio sources and time-based access","text":"on_error Behaviour\n:fail (default) Rethrows any exception immediately. Recommended during development.\n:skip Drops failed chunks. That window does not appear in the output. Use for long production runs over imperfect archives.\n:record Adds a row with an error::String column. Metric columns are missing for that row.","title":"Error handling"},{"category":"section","location":"sources/#Progress","page":"Audio sources and time-based access","text":"progress=true (default) displays a ProgressMeter bar on stdout. The meter is thread-safe.\n\n","title":"Progress"},{"category":"section","location":"sources/#Planned-source-types","page":"Audio sources and time-based access","text":"Type Status Description\nSingleFileSource v1, complete One WAV/FLAC file in memory\nIndexedFileSource v1, complete Arrow timestamp index over a directory\nZarrSource v2, deferred Cloud-native chunked storage\n\nAll three implement the same AbstractAudioSource interface. Analysis pipelines written for SingleFileSource run unchanged on an IndexedFileSource covering a multi-terabyte archive.","title":"Planned source types"},{"category":"section","location":"explanations/glossary/#Glossary","page":"Glossary","text":"Terms used in EcoAcoustics.jl documentation, drawn from Merchant et al. (2015), NOAA Passive Acoustic Monitoring Data Standards documentation, and standard DSP texts. Direct access to ISO 18405:2017 was not available during writing; any definition sourced from secondary literature is noted as such. Definitions marked [uncertain — secondary source only] should be verified against the ISO standard before use in regulatory or publication contexts.\n\nPlanned additions (v1.1 / v1.2). When click and whistle detectors enter the package, additional terms and citations will be added. Anticipated references include: Frasier et al. for click detection (KERNO-F); Roch & Silbido / Baumgartner et al. for whistle contour tracing (Silbido remora); Martin et al. (2021) and the MANTA group for millidecade spectra; Wiggins & Hildebrand (2007) for the Triton / HARP architecture underlying LTSA design. These are noted here so that users comparing EcoAcoustics.jl output against those tools know the methodological lineage.\n\n","title":"Glossary"},{"category":"section","location":"explanations/glossary/#STFT-—-Short-Time-Fourier-Transform","page":"Glossary","text":"The Short-Time Fourier Transform is computed by dividing a signal into overlapping frames and applying the Discrete Fourier Transform (DFT) to each frame independently. For frame i of a signal x, with window w of length N and hop size h:\n\nX_i[k] = Σ_{n=0}^{N−1}  w[n] · x[i·h + n] · exp(−j2πkn/N)\n\nThe result is a matrix of complex numbers: column i is the spectrum of frame i, row k is the time history of frequency bin k. In EcoAcoustics.jl, the STFT is computed by spectrogram and stored in SpectrogramResult.stft.\n\nISO 18405:2017 defines frequency-domain analysis of underwater sound in the context of PSD (§3.1.2); the STFT is the standard computational path to that quantity. [Reference via Merchant 2015 — ISO text not directly consulted.]\n\n","title":"STFT — Short-Time Fourier Transform"},{"category":"section","location":"explanations/glossary/#Single-sided-spectrum","page":"Glossary","text":"A real-valued signal's DFT is conjugate-symmetric: the bin at negative frequency −k carries exactly the same information as the bin at positive frequency +k. The single-sided spectrum retains only the bins from 0 Hz (DC) through fs/2 (Nyquist), discarding the redundant negative-frequency half.\n\nIn FFTW.jl, rfft(x) for a real vector of length N (even) returns a complex vector of length N/2 + 1:\n\nIndex 1: DC bin (0 Hz)\nIndices 2 to N/2: positive-frequency interior bins\nIndex N/2 + 1: Nyquist bin (fs/2)\n\nThe single-sided amplitude correction — multiplying interior bins by √2 (or interior power bins by 2) to restore the energy lost by dropping the negative half — is applied at the PSD layer, not in spectrogram. This separation keeps the STFT layer independent of normalisation conventions.\n\n","title":"Single-sided spectrum"},{"category":"section","location":"explanations/glossary/#Frame,-window,-hop,-overlap","page":"Glossary","text":"Frame: one short segment of the signal submitted to a single FFT. Length is window_length = round(Int, window_seconds × fs) samples. Frames are labelled 1 to num_frames.\n\nWindow (or analysis window, tapering window): a smooth bell-shaped function multiplied element-wise into each frame before the FFT. Its purpose is to reduce spectral leakage: without tapering, the abrupt cut at each frame boundary acts as a rectangular pulse and spreads energy across all frequency bins. Common choices and their trade-offs:\n\nWindow Sidelobe level Main lobe width Typical use\nHann −31 dB Moderate General acoustic analysis\nHamming −43 dB Moderate When slightly lower leakage needed\nBlackman −58 dB Wide Narrow tonal signals\nRectangular 0 dB Narrowest Calibration checks only\n\nHop (also step size, frame advance): the number of samples between the start of one frame and the start of the next. hop = round(Int, (1 − overlap_fraction) × window_length). A hop equal to half the window length (overlap = 50%) means consecutive frames share half their samples.\n\nOverlap: the fraction of the window shared between consecutive frames. overlap_fraction = 1 − hop / window_length. PAMGuide default is 50% overlap. Higher overlap gives finer time resolution (more frames) at proportionally higher computational cost. overlap_fraction = 1.0 is undefined (infinite frames); spectrogram rejects it with an AssertionError.\n\n","title":"Frame, window, hop, overlap"},{"category":"section","location":"explanations/glossary/#Window-energy","page":"Glossary","text":"window_energy = Σ wᵢ²   (equivalently: ‖w‖²)\n\nThe sum of squared window coefficients. Required for converting raw STFT magnitudes to power spectral density: the PSD at frequency bin k is proportional to |X[k]|² / (fs × window_energy).\n\nMerchant et al. (2015) Section 2.1 calls this the normalisation factor and uses the symbol W² for the sum. EcoAcoustics.jl stores it on SpectrogramResult so the PSD layer has it without recomputing the window.\n\nThe window is applied to each frame unmodified (not pre-normalised to unit energy). This is the PAMGuide / Merchant convention. Some other software normalises the window so that Σwᵢ² = 1 before applying it; that convention absorbs the correction into the window itself and produces different absolute power values. Always check which convention a tool uses when comparing results.\n\n","title":"Window energy"},{"category":"section","location":"explanations/glossary/#Power-spectral-density-(PSD,-*P*_ss)","page":"Glossary","text":"The power spectral density gives a signal's power per unit frequency. For calibrated underwater acoustic data the units are µPa²/Hz. For a single-sided STFT bin k from a calibrated signal:\n\nP_ss[k] = |X[k]|²  /  (fs × window_energy)       [µPa²/Hz]\n\nThe subscript ss denotes \"single-sided\" (all energy concentrated in the positive-frequency range, with interior bins doubled to account for the dropped negative-frequency mirror). After taking 10 · log₁₀, the result is in dB re 1 µPa²/Hz.\n\nIn EcoAcoustics.jl, PSD is computed at the metric layer (task 9) from a SpectrogramResult. Notation follows Merchant et al. (2015).\n\nISO 18405:2017 §3.1.2 — definition via Merchant 2015; ISO text not directly consulted. [Uncertain — secondary source only.]\n\n","title":"Power spectral density (PSD, P_ss)"},{"category":"section","location":"explanations/glossary/#Sound-pressure-level-(SPL,-*L*_p)","page":"Glossary","text":"The sound pressure level is the RMS sound pressure expressed in decibels relative to the underwater acoustic reference of 1 µPa:\n\nL_p = 20 · log₁₀(p_rms / p_ref)     p_ref = 1 µPa  (underwater)\n\nIn EcoAcoustics.jl, SPL is obtained by integrating the calibrated PSD over an explicitly specified frequency band, then converting to dB. Third-octave-level (TOL), decidecade, and arbitrary-band SPL all follow the same approach. There is no function named \"broadband SPL\" — callers always pass an explicit frequency range to compute_spl; see DD-27.\n\nReference level note. The underwater reference (1 µPa) differs from the airborne reference (20 µPa). A value in dB re 1 µPa is not comparable to a value in dB re 20 µPa; always check the reference when comparing across literature sources or measurement systems.\n\nISO 18405:2017 §3.1.3 — definition via Merchant 2015; ISO text not directly consulted. [Uncertain — secondary source only.]\n\n","title":"Sound pressure level (SPL, L_p)"},{"category":"section","location":"explanations/glossary/#Temporal-Observation-Window-(TOW)-and-Temporal-Analysis-Window-(TAW)","page":"Glossary","text":"Terms introduced in the ADEON Data Processing Specification (Ainslie et al. 2018, §2.1.1) to distinguish two nested time scales in soundscape reporting.\n\nTemporal Analysis Window (TAW): The short window over which a single spectral estimate (PSD frame) is computed. In EcoAcoustics.jl this is the window_seconds parameter passed to compute_psd or compute_spl. The DPS standard TAW durations are 1 s, 60 s, 1 h, 1 d, 1 month, and 1 year. The package default of window_seconds = 1.0 matches the DPS primary TAW.\n\nTemporal Observation Window (TOW): The longer period over which multiple TAW frames are aggregated into a single reported metric (mean, percentiles, CDF). In EcoAcoustics.jl, all PSD frames computed from a single Audiodata object or compute_spl call form one TOW; the aggregate statistics in BandSPL (mean, median, percentiles) summarise that TOW. For archive-scale reporting, the TOW is determined by how process_chunks groups chunks and collects results — the package does not currently name a TOW parameter explicitly.\n\n","title":"Temporal Observation Window (TOW) and Temporal Analysis Window (TAW)"},{"category":"section","location":"explanations/glossary/#Decidecade","page":"Glossary","text":"A decidecade band spans one-tenth of a decade on a logarithmic frequency axis: a frequency ratio of 10^(1/10) ≈ 1.259. This is numerically indistinguishable from a third-octave band (ratio 2^(1/3) ≈ 1.260); the two terms refer to the same practical band width.\n\n\"Decidecade\" is the ISO 18405:2017 preferred term for underwater acoustics. \"Third-octave\" (ANSI S1.11) remains in common use in older literature and cross-domain tools. EcoAcoustics.jl provides both tol_bands (legacy name) and decidecade_bands (ISO name) as identical functions; output keys use the :tol_N prefix for compatibility with PAMGuide, MANTA, and Merchant 2015.\n\nThe ISO 18405 symbol for decidecade-band sound pressure level is Lp,ddec. EcoAcoustics.jl computes this via `computedecidecadeorcompute_tol`.\n\nEdge formula note: EcoAcoustics.jl uses ANSI S1.11 base-2 edges (f_c × 2^(±1/6)). The ADEON DPS specifies base-10 edges (f_c × 10^(±1/20)). The relative difference is ~0.04%; at 1 Hz PSD resolution the band boundaries are identical to within one bin. See DD-20.\n\n","title":"Decidecade"},{"category":"section","location":"explanations/glossary/#Energetic-mean","page":"Glossary","text":"The energetic mean of a set of SPL values {L₁, L₂, …, Lₙ} is:\n\nL_energetic = 10 × log₁₀(1/n × Σ 10^(Lᵢ/10))\n\nThis averages in the linear power domain and then converts back to dB. It is the physically correct average when the quantity of interest is mean power over time.\n\nThe arithmetic mean of dB values ((L₁ + L₂ + … + Lₙ) / n) does not preserve total power and is incorrect for acoustic averaging. Merchant 2015 fig. 4 reports the energetic mean; the ADEON DPS calls it \"the arithmetic mean computed on linear values\" (meaning: compute the mean on the linear quantities 10^(Lᵢ/10), then express the result in dB).\n\nIn EcoAcoustics.jl, BandSPL.mean_dB is the energetic mean of the per-frame SPL time series within the observation window.\n\n","title":"Energetic mean"},{"category":"section","location":"explanations/glossary/#System-weighted-vs-frequency-flat-broadband-SPL","page":"Glossary","text":"The ADEON DPS Figure 1 shows two parallel paths to broadband sound pressure level (L_p,broadband):\n\nFrequency-flat path (what EcoAcoustics.jl implements): Compute the calibrated PSD using the full frequency-dependent calibration, then integrate over the band: L_p = 10 × log₁₀(Σ_k P(fₖ) × Δf). The result uses the correct calibration at every frequency bin.\n\nSystem-weighted path (not implemented in v1): Apply a single representative sensitivity value — typically the hydrophone sensitivity at 250 Hz — directly to the time-domain signal, then take RMS. This is faster (no FFT required for the broadband value alone) but uses only one point on the calibration curve. For instruments with flat frequency response the two paths agree exactly. For instruments with frequency-dependent response (e.g. Rockhopper with a TF calibration curve), the frequency-flat path is the physically correct choice.\n\nSee DD-26 for the rationale for implementing only the frequency-flat path in v1.\n\nNo \"broadband SPL\" function. EcoAcoustics.jl does not expose a compute_broadband_spl function or a default broadband band. Callers always pass an explicit frequency range: compute_spl(psd; bands = Dict(:full => (10.0, 24000.0))). This makes the lower edge — which depends on the instrument's calibrated response — explicit and auditable rather than silently assumed. See DD-27.\n\n","title":"System-weighted vs frequency-flat broadband SPL"},{"category":"section","location":"explanations/glossary/#ISO-level-notation","page":"Glossary","text":"ISO 18405:2017 defines standardised symbols for underwater acoustic levels. The symbols used in ADEON DPS documentation and in EcoAcoustics.jl docstrings:\n\nSymbol Name EcoAcoustics.jl function / field\nL_p Sound pressure level (broadband) compute_spl, BandSPL.mean_dB\nL_pk Peak sound pressure level not implemented (v1)\nL_p,ddec Decidecade-band sound pressure level compute_decidecade / compute_tol\nL_E Sound exposure level not implemented (v1)\nL_E,ddec Decidecade-band sound exposure level not implemented (v1)\nβ Kurtosis (impulsiveness metric) not implemented (v1)\n\nAll levels are relative to 1 µPa (underwater). In-air equivalents use 20 µPa; set environment = :air in EcoAcoustics.jl to switch reference pressures.\n\nDefinitions from ISO 18405:2017. [ISO text not directly consulted; sourced via ADEON DPS (Ainslie et al. 2018) and Merchant 2015.]\n\n","title":"ISO level notation"},{"category":"section","location":"explanations/glossary/#References-for-this-glossary","page":"Glossary","text":"Merchant, N.D., Fristrup, K.M., Johnson, M.P., Tyack, P.L., Witt, M.J., Blondel, P. & Parks, S.E. (2015). Measuring acoustic habitats. Methods in Ecology and Evolution, 6, 257–265.\nNOAA Passive Acoustic Monitoring Data Standards documentation. Exact edition not recorded at time of writing.\nISO 18405:2017 Underwater acoustics — Terminology. Definitions noted as [uncertain — secondary source only] were sourced from the above secondary literature, not from the ISO text directly.\nAinslie MA, Miksis-Olds JL, Martin B, Heaney K, de Jong CAF, von Benda-Beckmann AM, Lyons AP (2018) ADEON Underwater Soundscape and Modeling Metadata Standard. Soundscape Specification deliverable v1.0.","title":"References for this glossary"},{"category":"section","location":"#EcoAcoustics.jl","page":"Home","text":"EcoAcoustics.jl is a Julia package for passive acoustic monitoring analysis. It is designed for recordings from underwater hydrophone deployments — its initial target is cetacean acoustics in the Mid-Atlantic Bight — but the architecture is general-purpose and not species-specific.\n\nCore workflow:\n\nRead audio — read_audio loads a WAV or FLAC file and returns an Audiodata struct containing the signal, sample rate, timestamps, location, and calibration parameters. Filename metadata (timestamps, recorder identity, deployment site) is parsed automatically for supported recorder families.\nBuild an index — for archive-scale work, build_index scans a directory of audio files once and writes a lightweight Arrow index (one row per file). IndexedFileSource then loads audio by time window on demand, reading only the files that overlap the requested range.\nApply calibration — apply_calibration! converts raw ADC counts to physical units (dB re 1 µPa). Calibration is stored with the recording but never applied at I/O time, so raw and calibrated data cannot be confused.\nCompute metrics — soundscape indices (SPL, PSD, ACI, entropy, LTSA) are computed via chunked streaming over any AbstractAudioSource. Implementations are in progress for v1.\n\nSupported recorders: Rockhopper (Embedded Ocean Systems), SM3M (Wildlife Acoustics), LS1X (Loggerhead Instruments), SNAP (NOAA/OSU). Adding a new recorder requires one file and one line in src/EcoAcoustics.jl.\n\nSupported formats: WAV and FLAC. Files in other formats (AIF, OGG) should be converted to FLAC with sox before indexing.\n\n","title":"EcoAcoustics.jl"},{"category":"section","location":"#API-Reference","page":"Home","text":"","title":"API Reference"}]
}
